---
phase: 02
plan: 03
type: execute
wave: 1
depends_on: [01]
files_modified:
  - src/styles/global.css
autonomous: true
requirements: []
gaps_addressed: [G-08, G-09, G-11, G-03, G-06]
must_haves:
  truths:
    - "Mobile container padding at global.css line 599 is `0 20px !important` (not `0 18px !important`); aligns with declared 20px token at line 69"
    - "All five `@font-face` blocks in global.css declare `font-display: swap` (G-08 verified)"
    - "src/crm/seed.ts has ≥1 entry in the `feed[]` array, and FeedStrip.tsx:6 returns null only when `state.feed.length === 0` (G-09 verified — no code change required)"
    - "src/sections/Catalog.tsx filter pill at line 39 uses `borderRadius: 999` (G-03 verified — no code change required)"
    - "src/sections/Reviews.tsx star color at line 30 uses `var(--coral)` (G-06 verified — no code change required)"
    - "No new font sizes or weights introduced"
    - "Sharp corners except pills (border-radius 0 / 999px / 50% only)"
    - "House easing `cubic-bezier(.2,.7,.2,1)` — not relevant here (CSS-token fixes only, no new transitions)"
    - "No new component library, no shadcn init, no CSS-in-JS, no Tailwind"
    - "Phase 2 does NOT touch src/crm/, quizSpec.ts, fonts CDN, real founder content, or multi-market flags"
    - "Opportunistic fix budget = 5min wall-clock; over-budget items go to 02-DEFERRED-POLISH.md"
    - "Golden test continues to pass at DIFF_THRESHOLD=0.22 (G-11 fix invisible at desktop 1280 viewport per RESEARCH.md §10 R-8; G-08/G-09 are not visual changes)"
  artifacts:
    - path: "src/styles/global.css"
      provides: "Mobile container padding aligned to declared 20px token (G-11 fix)"
      contains: "padding: 0 20px !important"
  key_links:
    - from: "src/styles/global.css line 599"
      to: "src/styles/global.css line 69 (canonical mobile padding declaration)"
      via: "value alignment — same `0 20px` token"
      pattern: "padding:\\s*0\\s*20px"
---

<objective>
Bundled Wave 1 token-fix plan covering five gap IDs that all share the property of being CSS-token / data-state checks (RESEARCH.md §10 R-8 + §8 recommend bundling):

- **G-11 (CODE CHANGE):** Fix `src/styles/global.css:599` from `padding: 0 18px !important` to `padding: 0 20px !important` — aligns with declared 20px token at line 69 and UI-SPEC §Spacing Scale Exceptions which explicitly notes 18px is "not a multiple of 4 and conflicts with the 20px value declared at line 69".
- **G-08 (VERIFY-ONLY):** Confirm all five `@font-face` blocks in global.css already declare `font-display: swap`. No code change. Self-hosting Gilroy is explicitly Phase 8 (UI-SPEC G-08 + CONTEXT.md `<deferred>`).
- **G-09 (VERIFY-ONLY):** Confirm `src/crm/seed.ts` has ≥1 entry in `feed[]` (currently has 5) and `FeedStrip.tsx:6` returns null only at length 0. RESEARCH.md §8 verdict: VERIFY-ONLY, no code change required.
- **G-03 (VERIFY-ONLY):** Confirm `src/sections/Catalog.tsx:39` filter-pill button uses `borderRadius: 999`. UI-SPEC matches; no code change.
- **G-06 (VERIFY-ONLY):** Confirm `src/sections/Reviews.tsx:30` star color is `var(--coral)`. UI-SPEC matches; no code change.

Purpose: Close all five gaps in a single small commit. Total code change is one character (line 599: `18` → `20`); the rest are evidence-grep verifications captured in this plan's acceptance criteria. Per CONTEXT.md D-04a, the verify-only gaps are otherwise covered by the puppeteer golden as a structural-drift signal.

Output: One single-character edit in `src/styles/global.css:599` plus a commit message documenting the bundled verifications.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-redesign-from-screenshot/02-CONTEXT.md
@.planning/phases/02-redesign-from-screenshot/02-RESEARCH.md
@.planning/phases/02-redesign-from-screenshot/02-PATTERNS.md
@.planning/phases/02-redesign-from-screenshot/02-UI-SPEC.md
@.planning/phases/02-redesign-from-screenshot/02-VALIDATION.md

<interfaces>
<!-- Pre-existing state confirmed during planning (2026-05-07). -->

From `src/styles/global.css:67-69` (canonical mobile padding declaration):
```css
.container { width: 100%; max-width: 1480px;
  margin: 0 auto; padding: 0 56px;
}
@media (max-width: 720px){ .container { padding: 0 20px; } }
```

From `src/styles/global.css:598-599` (the drift to fix):
```css
section { padding: 64px 0 !important; }
.container { padding: 0 18px !important; }
```

From `src/styles/global.css:1-27` (5 `@font-face` blocks, all already have `font-display: swap` — verified during planning):
```css
@font-face { font-family: 'Gilroy'; ... font-weight: 300; font-display: swap; }
@font-face { font-family: 'Gilroy'; ... font-weight: 400; font-display: swap; }
@font-face { font-family: 'Gilroy'; ... font-weight: 500; font-display: swap; }
@font-face { font-family: 'Gilroy'; ... font-weight: 700; font-display: swap; }
@font-face { font-family: 'Gilroy'; ... font-weight: 900; font-display: swap; }
```

From `src/crm/seed.ts:103-109` (5 entries verified — count via `grep -c "id: 'feed" src/crm/seed.ts` returns 5).

From `src/sections/FeedStrip.tsx:6`: `if (state.feed.length === 0) return null;` (verified).

From `src/sections/Catalog.tsx:39`: `padding: '10px 18px', borderRadius: 999,` (verified).

From `src/sections/Reviews.tsx:30`: `<div style={{ display: 'flex', gap: 4, marginBottom: 20, color: 'var(--coral)' }}>` (verified).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix G-11 mobile padding + run G-08/G-09/G-03/G-06 verification greps</name>
  <files>src/styles/global.css</files>
  <read_first>
    - src/styles/global.css (FULL FILE — needed to see line 1-27 @font-face blocks for G-08 verify, line 67-69 for canonical mobile padding token, line 598-599 for the G-11 drift; one read pass; do NOT re-read)
    - .planning/phases/02-redesign-from-screenshot/02-PATTERNS.md §"src/styles/global.css:599 (MODIFY — G-11, config CSS, render)"
    - .planning/phases/02-redesign-from-screenshot/02-PATTERNS.md §"src/styles/global.css `@font-face` blocks (VERIFY G-08, config CSS, render)"
    - .planning/phases/02-redesign-from-screenshot/02-PATTERNS.md §"src/crm/seed.ts + src/sections/FeedStrip.tsx:6 (READ-ONLY VERIFY — G-09)"
    - .planning/phases/02-redesign-from-screenshot/02-PATTERNS.md §"src/sections/Catalog.tsx (READ-ONLY VERIFY — G-03)"
    - .planning/phases/02-redesign-from-screenshot/02-PATTERNS.md §"src/sections/Reviews.tsx (READ-ONLY VERIFY — G-06)"
    - .planning/phases/02-redesign-from-screenshot/02-RESEARCH.md §10 R-8 (G-11 mobile padding fix invisible at desktop golden — explains why grep verification is the gate)
    - .planning/phases/02-redesign-from-screenshot/02-UI-SPEC.md §Spacing Scale §Exceptions (the line "global.css line 599 contains `.container { padding: 0 18px !important }` ... The spec corrects this to **20px**")
    - src/sections/Catalog.tsx (lines 30-55 — verify G-03 evidence)
    - src/sections/Reviews.tsx (lines 20-50 — verify G-06 evidence)
    - src/sections/FeedStrip.tsx (full file, ~10 lines — verify G-09 rendering condition)
    - src/crm/seed.ts (lines 100-115 — verify G-09 feed[] entries)
  </read_first>
  <action>
**Single CSS edit (G-11):** In `src/styles/global.css:599`, change `padding: 0 18px !important` to `padding: 0 20px !important`. This is a single-character substitution: `18` → `20`. Use the Edit tool with:

- `old_string`: `  .container { padding: 0 18px !important; }`
- `new_string`: `  .container { padding: 0 20px !important; }`

(Two-space leading indentation must match the existing line — verified during planning.)

**Anti-pattern guards (do NOT do):**
- Do NOT remove `!important` — line 599 lives inside `@media (max-width: 720px)` and overrides line 67's desktop padding via specificity-and-importance; removing `!important` would break the cascade against inline styles in section files.
- Do NOT change line 67 (`padding: 0 56px;` desktop) — that's the locked desktop value.
- Do NOT change line 69 (the canonical `0 20px` declaration without `!important`) — leave it alone; line 599 just needs to align.
- Do NOT touch any `@font-face` block — G-08 is verify-only.
- Do NOT touch `src/crm/seed.ts` — G-09 is verify-only (5 entries already present).
- Do NOT touch `src/sections/Catalog.tsx` — G-03 is verify-only (`borderRadius: 999` already at line 39).
- Do NOT touch `src/sections/Reviews.tsx` — G-06 is verify-only (`color: 'var(--coral)'` already at line 30).
- Do NOT touch `src/sections/FeedStrip.tsx` — G-09 condition (`state.feed.length === 0`) is correct.

**After the edit, run all five verification greps** (these MUST all pass — see acceptance criteria below for exact commands and expected outputs). The golden test (Wave 0) is run as the regression check; it operates at the desktop 1280 viewport so the G-11 mobile fix is invisible to it (RESEARCH.md §10 R-8) — but the test must still pass to confirm no other regression was introduced.

**If a verification grep returns an unexpected result:**
- For G-08 (font-display count < 5): a `@font-face` block was inadvertently changed. Restore from git and investigate.
- For G-09 (feed entries missing): seed.ts was inadvertently changed. Restore from git.
- For G-03 / G-06 (token mismatch): UI-SPEC contract violated. Restore from git.
- For G-11 (line 599 still says `0 18px`): the Edit didn't apply. Re-run.

If anything outside this plan's `files_modified` (single file: `src/styles/global.css`) shows a diff in `git status`, revert it.

**No `02-DEFERRED-POLISH.md` items expected** — all five gaps are either single-character or evidence-only.
  </action>
  <verify>
    <automated>pnpm test landing-page-golden.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - **G-11 (code change applied):** `sed -n '599p' src/styles/global.css` outputs the exact line `  .container { padding: 0 20px !important; }`
    - **G-11 (no remnants):** `grep -n '0 18px' src/styles/global.css` returns 0 hits
    - **G-11 (positive grep):** `grep -nE 'padding:[[:space:]]*0[[:space:]]*20px[[:space:]]*!important' src/styles/global.css` returns at least 1 hit (line 599)
    - **G-08:** `grep -c 'font-display:[[:space:]]*swap' src/styles/global.css` returns at least 5 (one per `@font-face` block)
    - **G-09 (data):** `grep -c "id: 'feed" src/crm/seed.ts` returns at least 1 (currently 5)
    - **G-09 (rendering condition):** `grep -n 'state.feed.length === 0' src/sections/FeedStrip.tsx` returns at least 1 hit (line 6)
    - **G-03:** `grep -nE 'borderRadius:[[:space:]]*999\|border-radius:[[:space:]]*999px' src/sections/Catalog.tsx` returns at least 1 hit (line 39)
    - **G-06:** `grep -c 'var(--coral)' src/sections/Reviews.tsx` returns at least 1 (lines 30 and 35)
    - **No regressions in `files_modified`:** `git diff --stat` shows only `src/styles/global.css` changed; total change = 1 line modified, 1 line added/removed = 0 net (single character substitution)
    - **Golden test still green:** `pnpm test landing-page-golden.test.ts` exits 0 with "1 passed"
    - **Full suite green:** `pnpm test` exits 0
    - **Typecheck green:** `pnpm typecheck:server` exits 0
    - **No accidental edits:** `git diff src/crm/seed.ts src/sections/FeedStrip.tsx src/sections/Catalog.tsx src/sections/Reviews.tsx` returns nothing
  </acceptance_criteria>
  <done>
    G-11 closed (single-char fix: 18 → 20 at line 599). G-08, G-09, G-03, G-06 verified-only and documented in this plan's verification commands. Five gaps closed in one small commit. Golden test passes at threshold 0.22.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Single-character CSS value change + four evidence-grep verifications. No new IO, no new auth surface, no new user-controlled inputs touched, no new dependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | (n/a) | src/styles/global.css line 599 token alignment | accept | No new attack surface — frontend visual changes only; CSS token alignment to a value declared elsewhere in the same file; no new external IO; no new secrets; no new user-controlled inputs touched. |
</threat_model>

<verification>
- `pnpm test landing-page-golden.test.ts` exits 0 (no regression)
- `pnpm test` exits 0 (full suite)
- `pnpm typecheck:server` exits 0
- All five gap-specific greps in `<acceptance_criteria>` pass
- `git diff` is limited to one line in `src/styles/global.css`
</verification>

<success_criteria>
- G-11 mobile padding aligned to 20px token (single-character fix at line 599)
- G-08 verified (font-display: swap on 5 @font-face blocks)
- G-09 verified (5 feed entries in seed.ts; FeedStrip render condition correct)
- G-03 verified (Catalog filter pill borderRadius: 999)
- G-06 verified (Reviews star color var(--coral))
- Five gap IDs closed in one commit
- Golden test continues to pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-redesign-from-screenshot/02-03-SUMMARY.md` documenting:
- Confirmation of the single-char G-11 fix at line 599
- Output of all five verification greps (G-08, G-09 data + condition, G-03, G-06, G-11 positive + negative) — pasted verbatim from terminal
- Diff ratio reported by golden test post-change (should hold at ~18.7–18.9% baseline; G-11 invisible at desktop golden per RESEARCH.md §10 R-8)
- Confirmation that no other files were touched (`git diff --stat`)
</output>
