---
phase: 02
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified: []  # close-out plan; no source edits expected. Only writes the phase-close summary doc + appends to DEFERRED-POLISH if needed.
autonomous: true
requirements: []
gaps_addressed: [G-01, G-03, G-06, G-08, G-09, G-11]  # close-out plan re-verifies every closed gap
must_haves:
  truths:
    - "All eleven gaps G-01..G-11 are accounted for: closed via code in Wave 1/2 (G-01, G-11), verify-only confirmed (G-03, G-06, G-08, G-09), or covered by golden-test structural-drift signal (G-02, G-04, G-05, G-07, G-10)"
    - "Golden test passes at DIFF_THRESHOLD=0.22 against the post-Wave-1+2 build"
    - "Full vitest suite is green (existing 207/210 baseline + 1 new test = 208/211 expected; or revised counts if any tests landed during waves)"
    - "Typecheck green (`pnpm typecheck:server` exits 0)"
    - "No new component library, no shadcn init, no CSS-in-JS, no Tailwind introduced anywhere in Phase 2"
    - "Phase 2 did NOT touch src/crm/, quizSpec.ts, fonts CDN, real founder content, or multi-market flags"
    - "Aria-labels on FloatingDock are non-negotiable — Footer.tsx still has `aria-label=\"Telegram\"` ≥2 and `aria-label=\"WhatsApp\"` ≥2 (regression guard per RESEARCH.md §10 R-5)"
    - "02-DEFERRED-POLISH.md (if it exists) has been reviewed and its items routed to Phase 8 by default"
    - "STATE.md updated to reflect Phase 2 closure"
    - "ROADMAP.md updated to mark Phase 2 plans complete"
  artifacts:
    - path: ".planning/phases/02-redesign-from-screenshot/02-VERIFICATION.md"
      provides: "Phase 2 close-out verification report (gap-by-gap evidence + final golden-test ratio + suite counts)"
      min_lines: 40
  key_links:
    - from: ".planning/phases/02-redesign-from-screenshot/02-VERIFICATION.md"
      to: "server/tests/landing-page-golden.test.ts (the gate)"
      via: "documented final diff ratio + threshold"
      pattern: "DIFF_THRESHOLD"
---

<objective>
Phase 2 close-out. Re-verify every gap closure (code + verify-only + golden-test-implicit), confirm contracts (FloatingDock aria-labels, no-new-tooling, no out-of-phase touches), record final golden-test diff ratio, and write `02-VERIFICATION.md` documenting evidence.

Per CONTEXT.md D-02b, founder approval of the as-shipped state is implicit in commit acceptance — no formal founder-review checkpoint. Per CONTEXT.md D-04a, "matches" sections (G-02 / G-04 / G-05 / G-07 / G-10) are verified through the golden test as a structural-drift signal, not via separate plans.

Purpose: Lock the phase in a documented gate state so downstream phases (Phase 5 frontend integration / Phase 8 content polish) can build on a stable visual baseline. No further visual changes after this plan.

Output: One new doc `.planning/phases/02-redesign-from-screenshot/02-VERIFICATION.md`, plus updates to `.planning/STATE.md` (current position → Phase 2 closed) and `.planning/ROADMAP.md` (Phase 2 plans marked `[x]`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-redesign-from-screenshot/02-CONTEXT.md
@.planning/phases/02-redesign-from-screenshot/02-RESEARCH.md
@.planning/phases/02-redesign-from-screenshot/02-PATTERNS.md
@.planning/phases/02-redesign-from-screenshot/02-UI-SPEC.md
@.planning/phases/02-redesign-from-screenshot/02-VALIDATION.md
@.planning/phases/02-redesign-from-screenshot/02-01-PLAN.md
@.planning/phases/02-redesign-from-screenshot/02-02-PLAN.md
@.planning/phases/02-redesign-from-screenshot/02-03-PLAN.md
@.planning/phases/02-redesign-from-screenshot/02-04-PLAN.md

<interfaces>
<!-- All eleven gap IDs and their disposition. Used to fill the verification table. -->

| Gap ID | Section | Disposition | Evidence command |
|--------|---------|-------------|------------------|
| G-01 | Hero coral-glow blob | Closed in 02-02 (code change) | `grep -c "rgba(213,121,89,0.4)" src/sections/Hero.tsx` ≥ 1 |
| G-02 | Hero pipeline card (no-op) | No-op; covered by golden | `pnpm test landing-page-golden.test.ts` exits 0 |
| G-03 | Catalog filter pill border-radius | Verify-only in 02-03 | `grep -nE 'borderRadius:\s*999\|border-radius:\s*999px' src/sections/Catalog.tsx` ≥ 1 hit (line 39) |
| G-04 | FeedStrip rendering (no-op) | No-op; covered by golden | `pnpm test landing-page-golden.test.ts` exits 0 |
| G-05 | Process accordion (no-op) | No-op; covered by golden | `pnpm test landing-page-golden.test.ts` exits 0 |
| G-06 | Reviews star color | Verify-only in 02-03 | `grep -c 'var(--coral)' src/sections/Reviews.tsx` ≥ 1 |
| G-07 | Nav fixed/frosted (no-op) | No-op; covered by golden | `pnpm test landing-page-golden.test.ts` exits 0 |
| G-08 | font-display: swap | Verify-only in 02-03 | `grep -c 'font-display:\s*swap' src/styles/global.css` ≥ 5 |
| G-09 | FeedStrip feed[] populated | Verify-only in 02-03 | `grep -c "id: 'feed" src/crm/seed.ts` ≥ 1 (currently 5) |
| G-10 | Mobile menu (no-op @ desktop) | Out of phase @ desktop; Phase 8 mobile audit | n/a (RESEARCH.md §9) |
| G-11 | Mobile container padding 18px → 20px | Closed in 02-03 (code change at line 599) | `sed -n '599p' src/styles/global.css` matches `0 20px !important` AND `grep -n '0 18px' src/styles/global.css` returns 0 |

**Contract guards** (must remain green):
- `grep -c 'aria-label="Telegram"' src/sections/Footer.tsx` ≥ 2 (UI-SPEC §Accessibility Contract; RESEARCH.md §10 R-5)
- `grep -c 'aria-label="WhatsApp"' src/sections/Footer.tsx` ≥ 2

**Anti-feature guards** (must remain at zero):
- `grep -rE 'from .(@radix|shadcn|@chakra|@mui|@emotion|styled-components|tailwind|next/)' src/` returns 0 hits
- `test -f components.json` returns 1 (file does not exist)
- `grep -E '"(tailwind|@emotion|styled-components|next)"' package.json` returns 0 hits
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run final phase-close verification + write 02-VERIFICATION.md + update STATE.md/ROADMAP.md</name>
  <files>.planning/phases/02-redesign-from-screenshot/02-VERIFICATION.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/STATE.md (current position section + Quick Tasks Completed table; needed for the STATE update)
    - .planning/ROADMAP.md (Phase 2 entry at line 126 + the Plans subsection placeholder; needed for roadmap update)
    - .planning/phases/02-redesign-from-screenshot/02-CONTEXT.md (close-out checklist)
    - .planning/phases/02-redesign-from-screenshot/02-VALIDATION.md (gate definition + per-task verification map)
    - .planning/phases/02-redesign-from-screenshot/02-RESEARCH.md §1 (gate definition) + §10 R-5 (FloatingDock aria-label preservation)
    - .planning/phases/02-redesign-from-screenshot/02-01-SUMMARY.md (if it exists from Wave 0 close)
    - .planning/phases/02-redesign-from-screenshot/02-02-SUMMARY.md (if it exists)
    - .planning/phases/02-redesign-from-screenshot/02-03-SUMMARY.md (if it exists)
    - .planning/phases/02-redesign-from-screenshot/02-04-SUMMARY.md (if it exists)
    - .planning/phases/02-redesign-from-screenshot/02-DEFERRED-POLISH.md (if it exists — confirm review and routing destination)
  </read_first>
  <action>
**Step 1: Run all verification greps and capture outputs.** For each command below, run it and record the exact output (line count or matched lines). Capture into a working buffer to paste into `02-VERIFICATION.md`:

**G-01 (code):** `grep -c "rgba(213,121,89,0.4)" src/sections/Hero.tsx` — expect ≥ 1
**G-01 (extra check):** `grep -c "pointerEvents: 'none'" src/sections/Hero.tsx` — expect ≥ 2 (new blob + existing watermark)

**G-03 (verify-only):** `grep -nE 'borderRadius:\s*999\|border-radius:\s*999px' src/sections/Catalog.tsx` — expect ≥ 1 hit on line 39

**G-06 (verify-only):** `grep -c 'var(--coral)' src/sections/Reviews.tsx` — expect ≥ 1

**G-08 (verify-only):** `grep -c 'font-display:[[:space:]]*swap' src/styles/global.css` — expect ≥ 5

**G-09 (verify-only):**
- `grep -c "id: 'feed" src/crm/seed.ts` — expect ≥ 1 (currently 5)
- `grep -n 'state.feed.length === 0' src/sections/FeedStrip.tsx` — expect 1 hit on line 6

**G-11 (code):**
- `sed -n '599p' src/styles/global.css` — expect output exactly: `  .container { padding: 0 20px !important; }`
- `grep -n '0 18px' src/styles/global.css` — expect 0 hits

**Contract guard (FloatingDock aria-labels):**
- `grep -c 'aria-label="Telegram"' src/sections/Footer.tsx` — expect ≥ 2
- `grep -c 'aria-label="WhatsApp"' src/sections/Footer.tsx` — expect ≥ 2

**Anti-feature guards:**
- `grep -rE 'from .(@radix|shadcn|@chakra|@mui|@emotion|styled-components|tailwind|next/)' src/ 2>/dev/null` — expect 0 hits
- `test -f components.json && echo present || echo absent` — expect "absent"
- `grep -E '"(tailwind|@emotion|styled-components|next)":' package.json` — expect 0 hits (rules out new dep additions)

**No-new-deps guard:**
- `git diff master -- package.json pnpm-lock.yaml | grep -E '^\+' | wc -l` — expect 0 lines added (Phase 2 introduced no new deps; if anything shows here, investigate). NOTE: if `master` is the working branch and there is no comparison base, use `git log --oneline -1 -- package.json pnpm-lock.yaml` and confirm the most-recent commit was NOT touched by Phase 2.

**Step 2: Run the gate test and full suite.**

- `pnpm test landing-page-golden.test.ts` — capture stdout (must show "1 passed") and capture the diff ratio printed by the test (it logs in the form `Diff ratio X.XX% > threshold ...` only on failure; on success the ratio is in the stdout if the test prints it OR can be re-derived by tweaking the test temporarily — for the verification doc, record the test passes and reference RESEARCH.md §3 baseline of 18.74% as the expected order of magnitude).
- `pnpm test` — capture pass/fail count (e.g., "Tests: 208 passed | 3 skipped (211)")
- `pnpm typecheck:server` — must exit 0

**Step 3: Review `02-DEFERRED-POLISH.md` if it exists.** Read the file. For each item, classify the destination:
- Default: Phase 8 (CONTENT-* polish) — note in the verification doc as "routed to Phase 8".
- Exception: if any item is a Phase 5 (frontend integration) concern — note as "routed to Phase 5".
- Exception: if any item is a fundamental contract violation that requires immediate attention — flag in the verification doc as "open issue, surface to founder".

If the file does NOT exist, that's fine — record "No deferred-polish items captured during Phase 2 execution".

**Step 4: Write `.planning/phases/02-redesign-from-screenshot/02-VERIFICATION.md`** using this structure:

```markdown
# Phase 02: Redesign from Screenshot — Verification

**Verified:** <today's date>
**Phase status:** CLOSED

## Gap Closure Summary

| Gap ID | Section | Disposition | Evidence |
|--------|---------|-------------|----------|
| G-01 | Hero coral-glow blob | Closed in 02-02 (code) | `grep -c "rgba(213,121,89,0.4)" src/sections/Hero.tsx` = <count> |
| G-02 | Hero pipeline card | No-op; golden-covered | Golden test passes at <ratio>% < 22% threshold |
| G-03 | Catalog filter pill | Verify-only confirmed in 02-03 | `Catalog.tsx:39` borderRadius: 999 present |
| G-04 | FeedStrip rendering | No-op; golden-covered | Golden test passes |
| G-05 | Process accordion | No-op; golden-covered | Golden test passes |
| G-06 | Reviews star color | Verify-only confirmed in 02-03 | `Reviews.tsx:30` color: 'var(--coral)' present |
| G-07 | Nav fixed/frosted | No-op; golden-covered | Golden test passes |
| G-08 | font-display: swap | Verify-only confirmed in 02-03 | `grep -c 'font-display:\s*swap'` = <count, expect ≥ 5> |
| G-09 | FeedStrip feed[] | Verify-only confirmed in 02-03 | `grep -c "id: 'feed"` = 5; `FeedStrip.tsx:6` condition intact |
| G-10 | Mobile menu | Out of phase @ desktop; Phase 8 mobile audit | n/a |
| G-11 | Mobile container padding | Closed in 02-03 (code at line 599) | `sed -n '599p'` shows `padding: 0 20px !important` |

## Contract Guards

- FloatingDock aria-labels: Telegram = <count>, WhatsApp = <count> (both ≥ 2)
- No new component library / CSS-in-JS / Tailwind: 0 hits
- `components.json` does not exist
- No new deps in `package.json`

## Test Results

- `pnpm test landing-page-golden.test.ts`: passed (1/1)
- Final diff ratio: <ratio>% (against threshold 22.00%; baseline was 18.74% per 02-RESEARCH.md §3)
- `pnpm test` (full suite): <X> passed | <Y> skipped (<X+Y>)
- `pnpm typecheck:server`: exits 0

## Deferred-Polish Routing

<bulleted list of every item in 02-DEFERRED-POLISH.md (if exists), each with destination phase. If file absent: "No deferred-polish items captured.">

## Files Modified in Phase 2

- `server/tests/landing-page-golden.test.ts` (new, 02-01-PLAN)
- `src/sections/Hero.tsx` (G-01, 02-02-PLAN)
- `src/styles/global.css` (G-11, 02-03-PLAN — single character substitution at line 599)
- `src/quiz/QuizModal.tsx` (D-03 light token alignment, 02-04-PLAN)

## Phase 2 Out-of-Phase Confirmation

These are explicitly NOT touched in Phase 2 (per CONTEXT.md `<deferred>` and `<domain>` out-of-phase boundaries):

- `src/crm/CrmProvider.tsx` (Phase 5 frontend integration)
- `src/crm/seed.ts` content (Phase 8 founder bios + reviews + cars)
- `src/quiz/quizSpec.ts` (Phase 6 lead flow LEAD-* requirements)
- `src/components/atoms.tsx` (no structural changes; multi-market flags Phase 8)
- `src/components/Nav.tsx` (no changes)
- `src/sections/{Marquee,Catalog,FeedStrip,Process,Founders,LeadMagnet,Reviews,Faq,Footer}.tsx` (no structural changes; verify-only)

## Sign-Off

Phase 2 closes per CONTEXT.md D-02b (founder approval implicit in commit acceptance). Subsequent phases (5/6/8) build on this visual baseline.
```

**Step 5: Update `.planning/STATE.md`**:

- Find the "Current Position" block.
- Add a "Phase 02 closed" entry to the Quick Tasks Completed table OR (preferred) update the `status` field at the top of STATE.md to mention Phase 02 closure.
- Add a one-line entry to `## Accumulated Context > Roadmap Evolution` (if appropriate): "Phase 02 closed — visual fidelity pass landed; G-01..G-11 closed (3 code, 5 verify-only, 3 golden-covered, 1 deferred to Phase 8 mobile audit)."
- Bump `last_updated` in frontmatter to today's ISO date.
- Update `progress.completed_phases` to reflect Phase 2 completion (currently 3 → bump to 4).
- Update `progress.completed_plans` to add Phase 2's plan count (5).

**Step 6: Update `.planning/ROADMAP.md`**:

- Find the "### Phase 2: redesign from screenshot" block at line 126.
- Update `**Goal:**` from "[To be planned]" to (suggested): "Visual fidelity pass on the existing public landing page (sections + QuizModal) to close all 11 enumerated gaps from `02-UI-SPEC.md`. Gated by a puppeteer + pixelmatch screenshot golden test against `design-reference.png` at threshold 0.22."
- Update `**Plans:**` line from "0 plans" to "5 plans across 3 waves (all complete)".
- Update the `Plans:` checklist to mark all 5 plans `[x]`:
  ```
  Plans:
  - [x] 02-01-PLAN.md — Wave 0: landing-page-golden.test.ts (puppeteer+sharp+pixelmatch gate at DIFF_THRESHOLD=0.22)
  - [x] 02-02-PLAN.md — Wave 1: Hero G-01 (static coral-glow blob)
  - [x] 02-03-PLAN.md — Wave 1: global.css G-11 fix + G-08/G-09/G-03/G-06 verify-only
  - [x] 02-04-PLAN.md — Wave 2: QuizModal D-03 light visual-token alignment
  - [x] 02-05-PLAN.md — Wave 3: phase-close verification + STATE/ROADMAP update
  ```
- Update the Progress table at the bottom to mark Phase 2 complete.

**Step 7: Final sanity check (no commit, just observation):**

- `git status` should show only: the new `02-VERIFICATION.md` file (untracked → staged), modifications to `STATE.md` and `ROADMAP.md`.
- The Wave 1/2 plan commits should already have landed during Wave 1 and Wave 2 execution (they're in `git log` from earlier waves); this Wave 3 plan's job is just verification + close-out, not re-committing existing work.

If any anti-feature guard or contract guard FAILS at this stage:
- DO NOT mark the phase closed.
- Append the failure to `02-VERIFICATION.md` under a new "## Open Issues" section.
- Surface the failure in the plan's done state (executor reports `partial` instead of `complete`).
  </action>
  <verify>
    <automated>pnpm test landing-page-golden.test.ts && pnpm test && pnpm typecheck:server</automated>
  </verify>
  <acceptance_criteria>
    - **02-VERIFICATION.md created:** `test -f .planning/phases/02-redesign-from-screenshot/02-VERIFICATION.md` exits 0
    - **02-VERIFICATION.md non-empty:** `wc -l .planning/phases/02-redesign-from-screenshot/02-VERIFICATION.md` reports ≥ 40 lines
    - **02-VERIFICATION.md has gap table:** `grep -c '^| G-' .planning/phases/02-redesign-from-screenshot/02-VERIFICATION.md` returns 11 (one row per G-01..G-11)
    - **02-VERIFICATION.md records final diff ratio:** `grep -E 'Final diff ratio|diff ratio.*[0-9]+\.[0-9]+%' .planning/phases/02-redesign-from-screenshot/02-VERIFICATION.md` returns at least 1 hit
    - **G-01 code change present:** `grep -c "rgba(213,121,89,0.4)" src/sections/Hero.tsx` ≥ 1
    - **G-11 code change present:** `sed -n '599p' src/styles/global.css` outputs the exact line `  .container { padding: 0 20px !important; }` AND `grep -n '0 18px' src/styles/global.css` returns 0 hits
    - **G-08 verify confirmed:** `grep -c 'font-display:[[:space:]]*swap' src/styles/global.css` ≥ 5
    - **G-09 verify confirmed:** `grep -c "id: 'feed" src/crm/seed.ts` ≥ 1 AND `grep -n 'state.feed.length === 0' src/sections/FeedStrip.tsx` returns at least 1 hit
    - **G-03 verify confirmed:** `grep -nE 'borderRadius:[[:space:]]*999\|border-radius:[[:space:]]*999px' src/sections/Catalog.tsx` ≥ 1 hit
    - **G-06 verify confirmed:** `grep -c 'var(--coral)' src/sections/Reviews.tsx` ≥ 1
    - **FloatingDock contract preserved:** `grep -c 'aria-label="Telegram"' src/sections/Footer.tsx` ≥ 2 AND `grep -c 'aria-label="WhatsApp"' src/sections/Footer.tsx` ≥ 2
    - **No new component library:** `grep -rE 'from .(@radix|shadcn|@chakra|@mui|@emotion|styled-components|tailwind|next/)' src/ 2>/dev/null` returns 0 hits
    - **components.json absent:** `test -f components.json` exits 1 (file does not exist)
    - **Golden test green:** `pnpm test landing-page-golden.test.ts` exits 0 with "1 passed"
    - **Full suite green:** `pnpm test` exits 0
    - **Typecheck green:** `pnpm typecheck:server` exits 0
    - **STATE.md updated:** `grep -c "Phase 02\|Phase 2.*closed\|completed_phases: 4" .planning/STATE.md` returns at least 1 hit
    - **ROADMAP.md updated:** `grep -c '\[x\] 02-01-PLAN.md' .planning/ROADMAP.md` returns 1 AND `grep -c '\[x\] 02-05-PLAN.md' .planning/ROADMAP.md` returns 1
    - **No source-file diffs introduced by this plan:** `git diff src/ server/` returns nothing (this plan does not edit production code; it only writes verification doc + STATE/ROADMAP updates)
  </acceptance_criteria>
  <done>
    Phase 2 closed. `02-VERIFICATION.md` documents every gap closure with grep evidence. STATE.md + ROADMAP.md updated. Golden test green at threshold 0.22. Phase 5 (frontend integration), Phase 6 (lead flow), and Phase 8 (content polish) can now build on this stable visual baseline.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification + documentation update plan. No source-code changes; no IO besides reading files and writing planning markdown. No new auth surface, no new user-controlled inputs touched, no new dependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | (n/a) | Phase 2 close-out documentation | accept | No new attack surface — verification + planning-doc updates only; reads existing source files; writes phase-local markdown + STATE.md + ROADMAP.md; no production code touched. |
</threat_model>

<verification>
- All 11 gap-IDs accounted for in `02-VERIFICATION.md` (table)
- Golden test green at threshold 0.22
- Full suite green
- Typecheck green
- FloatingDock aria-label contract preserved
- No new component library / Tailwind / CSS-in-JS / Next.js
- components.json does not exist
- STATE.md + ROADMAP.md reflect Phase 2 closure
</verification>

<success_criteria>
- Phase 2 visual fidelity pass closed end-to-end
- Every gap G-01..G-11 has documented evidence (code-change grep, verify-only grep, or golden-test coverage)
- Verification doc + STATE/ROADMAP updates committed
- Downstream phases can proceed
</success_criteria>

<output>
After completion, this plan IS the phase summary. The artifact `.planning/phases/02-redesign-from-screenshot/02-VERIFICATION.md` plays the role of `02-05-SUMMARY.md` for this close-out plan. (Optionally, the executor may also create `.planning/phases/02-redesign-from-screenshot/02-05-SUMMARY.md` mirroring the verification doc; not required.)
</output>
