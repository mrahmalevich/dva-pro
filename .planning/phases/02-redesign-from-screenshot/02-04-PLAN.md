---
phase: 02
plan: 04
type: execute
wave: 2
depends_on: [02, 03]
files_modified:
  - src/quiz/QuizModal.tsx
autonomous: true
requirements: []
gaps_addressed: []  # D-03 (QuizModal alignment) is not a UI-SPEC G-* gap; CONTEXT.md scopes it as a separate decision
must_haves:
  truths:
    - "QuizModal uses the same global tokens as the landing page (color, button styles, spacing, typography)"
    - "No copy changes (no edits to user-visible text strings between quotes)"
    - "No quiz-step structure changes (no edits to QUIZ items in quizSpec.ts; this plan does NOT touch quizSpec.ts)"
    - "No logic changes (no edits to useState, useEffect, useCallback, useRef, event handlers, or modal lifecycle)"
    - "No new font sizes or weights introduced (UI-SPEC §Typography Scale Note)"
    - "Sharp corners except pills (border-radius 0 / 999px / 50% only); no new intermediate radii introduced"
    - "House easing `cubic-bezier(.2,.7,.2,1)` for any new transitions added — NO `ease-in-out` for entrance animations"
    - "No new component library, no shadcn init, no CSS-in-JS, no Tailwind"
    - "Aria-labels on FloatingDock are non-negotiable (this plan does NOT touch FloatingDock)"
    - "Phase 2 does NOT touch src/crm/, quizSpec.ts, fonts CDN, real founder content, or multi-market flags"
    - "Opportunistic fix budget = 5min wall-clock per individual change; this plan is bounded by D-03 stop conditions list (6 items), not the 5-min cap"
    - "Golden test continues to pass at DIFF_THRESHOLD=0.22 (modal is closed during landing-page capture; modal changes are invisible to the gate)"
  artifacts:
    - path: "src/quiz/QuizModal.tsx"
      provides: "QuizModal with token-aligned inline styles and className references (no logic/copy/structure changes)"
  key_links:
    - from: "src/quiz/QuizModal.tsx"
      to: "src/styles/global.css tokens (--coral, --cyan, --ink, --line, --line-strong, --mute, --mute-2, etc.)"
      via: "inline `style={{ color: 'var(--token)' }}` references"
      pattern: "var\\(--"
---

<objective>
Light visual-token alignment pass on `src/quiz/QuizModal.tsx` (436 lines) per CONTEXT.md D-03. Walk the modal once, fix any clear visual divergences against the UI-SPEC tokens (color, button styles, spacing, typography). **No copy changes, no quiz-step structure changes, no logic changes.**

Purpose: Bring the quiz modal's visual fidelity into alignment with the same token system used by the landing page sections. The screenshot golden test does NOT cover the quiz modal (no reference image exists for it) — the bound is "uses the same global tokens as the landing page".

Output: Targeted edits to inline `style={...}` props and `className=` references in `QuizModal.tsx` only. No edits to `quizSpec.ts`, `CrmProvider.tsx`, or `useQuizMatch.ts`. Total file delta should be ≤ ~30 lines changed (mostly token replacements: hex/rgb literals → `var(--token)`; `ease-in-out` → `cubic-bezier(.2,.7,.2,1)` if any are present).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-redesign-from-screenshot/02-CONTEXT.md
@.planning/phases/02-redesign-from-screenshot/02-RESEARCH.md
@.planning/phases/02-redesign-from-screenshot/02-PATTERNS.md
@.planning/phases/02-redesign-from-screenshot/02-UI-SPEC.md
@.planning/phases/02-redesign-from-screenshot/02-VALIDATION.md

<interfaces>
<!-- Token reference and house style. Extracted from global.css and section files (verified 2026-05-07). -->

**Allowed color tokens** (from `src/styles/global.css:1-46`):
- `var(--ink)` (#0A0A09 — dominant background)
- `var(--ink-1)` (#0F0F0E — secondary surface)
- `var(--ink-3)` (#1C1C1A — hover state)
- `var(--ink-4)` (#2A2A28 — deeply nested)
- `var(--paper)` / `var(--paper-2)` (light surfaces)
- `var(--coral)` / `var(--coral-deep)` (primary accent)
- `var(--cyan)` / `var(--cyan-deep)` (secondary accent)
- `var(--line)` / `var(--line-strong)` / `var(--line-bright)` (borders)
- `var(--mute)` / `var(--mute-2)` / `var(--mute-3)` (text mutes)

**Allowed font weights:** 400 / 500 / 700 / 900. **Forbidden:** 300, 600, 800 (UI-SPEC §Typography Scale Note).

**Allowed border-radius values:** `0` (sharp corners), `999` / `999px` (pills), `50%` (circles). **Forbidden:** any intermediate radius (e.g., `4px`, `8px`, `12px`, `16px`).

**House easing for any new transitions:** `cubic-bezier(.2,.7,.2,1)`. **Forbidden:** `ease-in-out` for entrance animations.

**Allowed `rgba()` literals (documented exceptions, do NOT replace):**
- `rgba(255,255,255,0.03)` — card surface bg (Hero pipeline card line 94, used elsewhere)
- `rgba(255,255,255,0.06)` — input bg
- `rgba(255,255,255,0.08)` — line / ghost button hover
- `rgba(213,121,89,...)` — coral with alpha (LeadMagnet-verbatim pattern)
- `rgba(10,10,9,...)` — ink with alpha (Hero gradient overlays)

**Reference token usage examples** (extracted from sections):

`src/sections/Hero.tsx:80, 94, 100, 117-122` — pipeline card surface, mono labels, stat-num inline styling
`src/sections/LeadMagnet.tsx:30-35, 38, 71` — benefit-list inline styling, button class usage, mono label
`src/sections/Reviews.tsx:23-32, 35` — review-card surface, avatar inline-style

**House utility classes** (from `src/styles/global.css:117-200`):
- `.btn .btn-primary` — white bg, ink text; coral on hover
- `.btn .btn-coral` — coral bg, white text; coral-deep on hover
- `.btn .btn-cyan` — cyan bg, white text; cyan-deep on hover
- `.btn .btn-ghost` — transparent bg, white text; rgba(255,255,255,0.08) on hover
- `.btn .btn-dark` — ink bg, white text; black on hover
- Sizes: default / `.btn-lg` / `.btn-xl`
- `.input` — `rgba(255,255,255,0.03)` bg, `var(--line-strong)` border, focus → coral border
- `.pill` — `rgba(255,255,255,0.03)` bg, `var(--line-strong)` border, `border-radius: 999px`
- `.tag` — `rgba(255,255,255,0.06)` bg, JetBrains Mono 10px, uppercase, sharp corners
- `.mono` — JetBrains Mono mono accent
- `.eyebrow` / `.num-marker` — uppercase eyebrow labels

**Stop conditions list (D-03)** — copied from PATTERNS.md §"Stop conditions for the QuizModal pass":

```
1. ONLY edit inline `style={...}` props and `className=` references.
2. NO logic changes (no edits to `useState`, `useEffect`, `useCallback`, `useRef`, event handlers).
3. NO copy changes (do not edit any string between quotes that renders user-visible text).
4. NO quiz-step structure changes (do not add/remove/reorder `QUIZ` items in `quizSpec.ts`).
5. NO changes to the modal lifecycle (`open`/`onClose`/`document.body.style.overflow` block).
6. STOP if a single fix exceeds 5 minutes wall-clock — log to `02-DEFERRED-POLISH.md` and continue.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Light visual-token alignment pass on QuizModal.tsx (D-03)</name>
  <files>src/quiz/QuizModal.tsx</files>
  <read_first>
    - src/quiz/QuizModal.tsx (FULL FILE — 436 lines is over the no-rereads cap; read in TWO slices: lines 1-218 first, then lines 219-436; do NOT re-read either slice; extract everything in one pass per slice)
    - src/sections/Hero.tsx (lines 90-130 — the canonical pipeline-card surface treatment + stat-num inline styling reference)
    - src/sections/LeadMagnet.tsx (full file, 49 lines — benefit-list + button-class-usage reference)
    - src/sections/Reviews.tsx (lines 20-50 — review-card surface + avatar inline-style reference)
    - src/styles/global.css (lines 1-46 — token definitions; lines 117-200 — `.btn` utility class system; lines 230-340 — input / pill / tag / mono / eyebrow class system; one read pass over each range)
    - .planning/phases/02-redesign-from-screenshot/02-PATTERNS.md §"src/quiz/QuizModal.tsx (MODIFY — D-03 light visual-token alignment, component, render)"
    - .planning/phases/02-redesign-from-screenshot/02-PATTERNS.md §"Inline-Style + Utility-Class Convention (applies to ALL section/component edits)"
    - .planning/phases/02-redesign-from-screenshot/02-CONTEXT.md `<decisions>` D-03
    - .planning/phases/02-redesign-from-screenshot/02-RESEARCH.md §10 R-9 (D-03 scope-creep mitigation — the 6-item stop conditions list)
    - .planning/phases/02-redesign-from-screenshot/02-UI-SPEC.md §Color §Surface Treatments §Animation (token contracts the modal must follow)
  </read_first>
  <action>
**Walk QuizModal.tsx ONCE with the 6-item stop-conditions list constantly applied.** For each visual divergence found, apply the minimum fix that aligns to UI-SPEC tokens. This is NOT a refactor — it is a token-alignment pass.

**Fixes to make (any combination of):**

1. **Hex / rgb() color literals → `var(--token)`** wherever a hex literal duplicates a declared token. Examples (illustrative — actual finds depend on file content):
   - `#0a0a09` (verbatim) → `var(--ink)` — exact same color, but token form documents intent and survives token-system changes
   - `#0F0F0E` → `var(--ink-1)`
   - `#D57959` → `var(--coral)` (only when alpha is NOT needed; the LeadMagnet-verbatim pattern `rgba(213,121,89,...)` stays as-is when alpha is needed)
   - Any `rgb(...)` literal that duplicates a declared token → `var(--token)` form
   - Documented exception: leave `rgba(255,255,255,X)` and `rgba(10,10,9,X)` and `rgba(213,121,89,X)` as-is (these are legitimate alpha-needed exceptions per UI-SPEC §Color and PATTERNS.md "Inline-Style + Utility-Class Convention")

2. **`ease-in-out` for entrance animations → `cubic-bezier(.2,.7,.2,1)`** wherever found. Hovers and exits may keep their existing easing if not entrance-related, BUT prefer house easing for consistency.

3. **Forbidden border-radius values** (anything besides `0`, `999`, `999px`, `50%`) → revert to nearest allowed value. Most likely finds: `borderRadius: 4` / `borderRadius: 8` / `borderRadius: 12` → change to `0` (sharp corners).

4. **Forbidden font weights** (300 / 600 / 800) → change to nearest allowed weight (400 / 500 / 700 / 900). Most likely find: `fontWeight: 600` → change to `700`.

5. **Inline button styles bypassing the class system** → if a `<button>` has inline `style={{ background, color, padding, fontWeight, fontSize, letterSpacing, textTransform, ... }}` that duplicates `.btn .btn-primary` (or any `.btn-*` variant), prefer adding `className="btn btn-primary"` (or correct variant) and removing the duplicated inline styles. ONLY do this if the button is NOT receiving custom inline styling for layout (`marginTop`, `width`, `flex`, etc.) — if it's purely visual styling, swap to the class. If unsure, leave as-is and note in `02-DEFERRED-POLISH.md`.

6. **Mono label inconsistency** — if you see uppercase letterspaced labels with `letterSpacing: '0.08em'..'0.1em'` and font-size 11–13px BUT WITHOUT `className="mono"` or with hex-color-instead-of-`var(--mute)`, align to the house pattern (`className="mono"` + `var(--mute-2)` for tertiary text or `var(--mute)` for secondary).

7. **Card surface treatment** — if a card-shaped div uses any background other than the canonical `rgba(255,255,255,0.03)` + `border: 1px solid var(--line-strong)`, align to the canonical pattern.

**STRICT do-NOT list (matches the 6-item stop conditions):**

- Do NOT touch any `useState`, `useEffect`, `useCallback`, `useRef` call.
- Do NOT touch any event handler (`onClick`, `onChange`, `onSubmit`, `onKeyDown`, etc.) — only their inline-style props on the JSX element.
- Do NOT edit any string between quotes that renders as user-visible text (Russian copy is locked in `quizSpec.ts` + literals in QuizModal — DO NOT touch).
- Do NOT add/remove/reorder QUIZ items, steps, or fields. The structure is locked in `quizSpec.ts`; this plan does NOT touch `quizSpec.ts` (verified — `quizSpec.ts` is NOT in `files_modified`).
- Do NOT touch the modal lifecycle: `open` prop, `onClose` callback, `document.body.style.overflow = 'hidden'` block.
- Do NOT add/remove imports unless strictly required by a token swap (none expected).
- Do NOT touch the success-step copy (Phase 6 owns LEAD-* requirements per CONTEXT.md `<decisions>` D-03).
- Do NOT touch any aria-label (none expected in QuizModal but contract preserved if encountered).
- Do NOT introduce new className that doesn't already exist in `global.css` — only re-use existing utility classes.

**5-minute wall-clock gate per fix:** If ANY single change starts to feel like more than 5 minutes (e.g., requires understanding a multi-step state machine, requires refactoring a render-prop pattern, requires a new utility class), STOP that fix, append it to `.planning/phases/02-redesign-from-screenshot/02-DEFERRED-POLISH.md` (create the file if it doesn't exist; format: bullet list with file:line + description), and move on.

**After the pass, run:**

1. `pnpm typecheck:server` — must exit 0 (no TS errors introduced)
2. `pnpm test landing-page-golden.test.ts` — must exit 0 with "1 passed" (modal is closed during landing-page capture; if this regresses something landed during the modal pass, investigate)
3. `pnpm test` (full suite) — must exit 0
4. Token-purity grep: `grep -nE '#[0-9A-Fa-f]{3,6}' src/quiz/QuizModal.tsx | grep -v 'rgba(' | grep -v '//'` — should return ZERO non-rgba hex literals (or only literals that are explicitly UI-SPEC-permitted; document any remainders in 02-DEFERRED-POLISH.md). Note: this grep excludes `rgba()` and comments to focus on raw `#` literals which are UI-SPEC anti-pattern in new code.
5. Manual: open `pnpm dev`, click the "Подобрать за 5 минут" CTA, walk all 5 quiz steps, confirm visual coherence with the surrounding landing page (no jarring color/spacing/font-weight discontinuities).
  </action>
  <verify>
    <automated>pnpm typecheck:server && pnpm test landing-page-golden.test.ts && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - **Scope guard:** `git diff --stat` shows ONLY `src/quiz/QuizModal.tsx` changed for this plan (NO changes to `src/quiz/quizSpec.ts`, `src/quiz/useQuizMatch.ts`, `src/crm/CrmProvider.tsx`, `src/styles/global.css`, or any other file)
    - **No logic changes:** `git diff src/quiz/QuizModal.tsx | grep -E '^[+-].*useState|useEffect|useCallback|useRef|onClick|onChange|onSubmit|onKeyDown|document.body.style'` returns 0 hits (only `style={...}` and `className=` lines should be in the diff; if a logic line appears, revert it)
    - **No copy changes:** `git diff src/quiz/QuizModal.tsx | grep -E "^[+-].*'[А-Яа-я]"` returns 0 hits — no Russian text strings touched
    - **Token purity (best-effort):** `grep -nE '#[0-9A-Fa-f]{3,6}' src/quiz/QuizModal.tsx | grep -v 'rgba(' | grep -v '^\s*//'` returns ZERO hits, OR each remaining hit is documented in `02-DEFERRED-POLISH.md` with rationale
    - **No forbidden border-radius:** `grep -nE 'borderRadius:\s*[0-9]+(px)?' src/quiz/QuizModal.tsx | grep -vE 'borderRadius:\s*(0|999|50%)' | grep -v "borderRadius: '50%'"` returns 0 hits (no intermediate radii)
    - **No forbidden font weights:** `grep -nE 'fontWeight:\s*(300|600|800)' src/quiz/QuizModal.tsx` returns 0 hits
    - **No ease-in-out for entrance:** `grep -n 'ease-in-out' src/quiz/QuizModal.tsx` returns 0 hits (or any remaining hit is documented in `02-DEFERRED-POLISH.md` as not-an-entrance-animation)
    - **Typecheck green:** `pnpm typecheck:server` exits 0
    - **Golden test green:** `pnpm test landing-page-golden.test.ts` exits 0 with "1 passed"
    - **Full suite green:** `pnpm test` exits 0
    - **No new component library:** `grep -nE 'from .(@radix|shadcn|@chakra|@mui|@emotion|styled-components|tailwind)' src/quiz/QuizModal.tsx` returns 0 hits
    - **File line count delta:** `git diff --stat src/quiz/QuizModal.tsx` shows ≤ 60 lines changed (rough upper bound; light pass, not refactor)
    - **Manual smoke test:** opening `pnpm dev`, clicking "Подобрать за 5 минут", and walking all 5 quiz steps shows no visual jarring discontinuity vs the landing page (this is a manual confirmation; record observation in the plan summary)
  </acceptance_criteria>
  <done>
    QuizModal visual tokens aligned with the landing-page UI-SPEC. No copy / logic / structure changes. Token purity check passes (or remaining items documented in `02-DEFERRED-POLISH.md`). All automated tests green. Phase 2 visual fidelity now consistent across landing page + quiz overlay.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Token alignment within an existing modal component. The modal's lifecycle, event handlers, and form-submit logic are EXPLICITLY off-limits per D-03 stop conditions. No new IO, no new auth surface, no new user-controlled inputs touched, no new dependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | (n/a) | src/quiz/QuizModal.tsx visual token alignment | accept | No new attack surface — frontend visual changes only; CSS token swaps from hex/rgb literals to declared `var(--token)` forms; lifecycle and event handlers unchanged per stop condition #2 + #5; no new external IO; no new secrets; no new user-controlled inputs touched. |
</threat_model>

<verification>
- `pnpm typecheck:server` exits 0
- `pnpm test landing-page-golden.test.ts` exits 0
- `pnpm test` exits 0
- All token-purity / forbidden-pattern greps in `<acceptance_criteria>` pass
- `git diff --stat` is limited to `src/quiz/QuizModal.tsx`
- Manual smoke test (5 quiz steps walked) shows visual coherence
</verification>

<success_criteria>
- QuizModal token-aligned with UI-SPEC
- 6-item stop-conditions list honored (no logic / copy / structure / lifecycle / aria / external-tooling changes)
- Token-purity grep passes (or deferred items captured in `02-DEFERRED-POLISH.md`)
- Golden test continues to pass at threshold 0.22
- Phase 2 visual fidelity consistent across landing page + quiz
</success_criteria>

<output>
After completion, create `.planning/phases/02-redesign-from-screenshot/02-04-SUMMARY.md` documenting:
- Bullet list of every token swap / class swap made (file:line + before → after); aim for brevity (1 line per change)
- Output of token-purity grep (`#hex` literals not in `rgba()`) — should be empty or each item documented
- Output of forbidden-radius / forbidden-weight / ease-in-out greps — should be empty
- Confirmation of `git diff --stat` showing only `src/quiz/QuizModal.tsx` changed
- Diff ratio reported by golden test post-change (should hold at ~18.7–18.9% baseline)
- Any deferred-polish items observed during the pass (appended to `02-DEFERRED-POLISH.md`); record their count here
- Manual smoke-test observation (5 quiz steps walked; coherent / not coherent + rationale)
</output>
