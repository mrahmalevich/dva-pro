---
phase: 02
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - server/tests/landing-page-golden.test.ts
autonomous: true
requirements: []
gaps_addressed: []  # Wave 0 builds the gate that verifies G-01..G-11; doesn't close any gap directly
must_haves:
  truths:
    - "A vitest integration test file exists at server/tests/landing-page-golden.test.ts that captures the running SPA, downscales to 605x1280, and pixelmatches against design-reference.png"
    - "The test self-orchestrates the Vite dev server bring-up via child_process.spawn (no external orchestration required)"
    - "The test passes against the as-shipped baseline at DIFF_THRESHOLD=0.22 (empirical floor 18.74% per RESEARCH.md §3 + 3.26pp safety band)"
    - "The first-run-bootstrap auto-write path from bmw-pilot-viewer.test.ts is intentionally NOT cloned — design-reference.png is locked at phase open and the test must fail loudly if it's missing"
    - "No new component library, no shadcn init, no CSS-in-JS, no Tailwind (UI-SPEC §Design System; CLAUDE.md anti-feature)"
    - "Phase 2 does NOT touch src/crm/, quizSpec.ts, fonts CDN, real founder content, or multi-market flags (CONTEXT.md out-of-phase + deferred lists)"
    - "Opportunistic fix budget = 5min wall-clock; over-budget items go to 02-DEFERRED-POLISH.md (CONTEXT.md D-01a)"
  artifacts:
    - path: "server/tests/landing-page-golden.test.ts"
      provides: "Phase 2 structural-drift gate (puppeteer + sharp.resize + pixelmatch against design-reference.png)"
      min_lines: 70
      contains: "DIFF_THRESHOLD = 0.22"
  key_links:
    - from: "server/tests/landing-page-golden.test.ts"
      to: ".planning/phases/02-redesign-from-screenshot/design-reference.png"
      via: "readFile(REFERENCE) + PNG.sync.read"
      pattern: "design-reference\\.png"
    - from: "server/tests/landing-page-golden.test.ts"
      to: "Vite dev server at http://127.0.0.1:5173/"
      via: "child_process.spawn('pnpm', ['exec', 'vite', '--port', '5173', '--host', '127.0.0.1']) + page.goto"
      pattern: "127\\.0\\.0\\.1:5173"
---

<objective>
Create the Phase 2 structural-drift gate: a vitest integration test that brings up the Vite dev server, captures the running SPA via puppeteer (1280×4000 fullPage), downscales the capture to 605×1280 via sharp, runs pixelmatch against `.planning/phases/02-redesign-from-screenshot/design-reference.png`, and asserts `mismatched / (605 * 1280) <= 0.22`.

This is the gate that closes Phase 2 in Wave 3. Wave 1 and Wave 2 commits each run it post-task to catch regressions.

Purpose: Lock the as-shipped visual baseline so subsequent gap-closure commits can't silently regress the structural shape of the landing page. CONTEXT.md D-02/D-02a require this gate.

Output: One new test file (~70-90 lines, ~80% cloned from `server/tests/bmw-pilot-viewer.test.ts`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-redesign-from-screenshot/02-CONTEXT.md
@.planning/phases/02-redesign-from-screenshot/02-RESEARCH.md
@.planning/phases/02-redesign-from-screenshot/02-PATTERNS.md
@.planning/phases/02-redesign-from-screenshot/02-UI-SPEC.md
@.planning/phases/02-redesign-from-screenshot/02-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase + RESEARCH.md §6. -->
<!-- Executor uses these directly — no codebase exploration needed. -->

From `package.json` (verified 2026-05-07): all required deps already installed, no `pnpm add` needed
- `puppeteer@^24.42.0` (devDep)
- `pixelmatch@^7.2.0` (devDep)
- `pngjs@^7.0.0` (devDep)
- `@types/pixelmatch@^5.2.6`, `@types/pngjs@^6.0.5` (devDep)
- `sharp@^0.34.5` (runtime dep — already pre-built per `pnpm.onlyBuiltDependencies`)
- `vitest@^3.2.4` (devDep)

From `vite.config.ts:6`: `server: { host: true, port: 5173 }` — confirms `pnpm exec vite --port 5173 --host 127.0.0.1` is the canonical bring-up.

From `.planning/phases/02-redesign-from-screenshot/design-reference.png`: 605×1280 RGBA PNG (verified via `file`).

From `.gitignore:24`: `server/tests/__snapshots__/*.diff.png` — already covers the new diff PNG via wildcard. NO `.gitignore` edit required.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create landing-page-golden.test.ts (Phase 2 structural-drift gate)</name>
  <files>server/tests/landing-page-golden.test.ts</files>
  <read_first>
    - server/tests/bmw-pilot-viewer.test.ts (FULL FILE, 318 lines — the canonical analog; 80% of new file is cloned from this; read once, in full pass)
    - .planning/phases/02-redesign-from-screenshot/02-RESEARCH.md §6 ("Phase 01.1 Harness Reuse Map") + §6 paste-ready outline (lines 304-396)
    - .planning/phases/02-redesign-from-screenshot/02-PATTERNS.md §"server/tests/landing-page-golden.test.ts (CREATE — test, request-response)"
    - .planning/phases/02-redesign-from-screenshot/02-RESEARCH.md §3 (threshold tuning evidence — 18.74% as-shipped baseline)
    - .planning/phases/02-redesign-from-screenshot/02-RESEARCH.md §10 R-1 (dev-server bring-up flake mitigation)
    - .planning/phases/02-redesign-from-screenshot/02-RESEARCH.md §10 R-7 (pixelmatch threshold:0.1 vs DIFF_THRESHOLD distinction)
    - .gitignore (verify line 24 wildcard `server/tests/__snapshots__/*.diff.png` already exists — no edit needed)
    - vite.config.ts (verify port 5173 + host 127.0.0.1 binding)
  </read_first>
  <action>
Create `server/tests/landing-page-golden.test.ts` by cloning `server/tests/bmw-pilot-viewer.test.ts` and applying the 6 specialization changes documented in RESEARCH.md §6:

1. **Drop these analog imports:** `mkdir`, `mkdtempSync` (from `node:fs/promises`/`node:fs`), `tmpdir` (from `node:os`), `join` (from `node:path`), `writeReportHtml` (`../scrapers/shared/report-html.js`), `ModelRecord`/`ReportSummary` (`../scrapers/shared/types.js`).

2. **Add these new imports:**
   ```ts
   import sharp from 'sharp';
   import { spawn, type ChildProcess } from 'node:child_process';
   ```

3. **Replace constants** (analog lines 32-35) with:
   ```ts
   const REFERENCE = resolve('.planning/phases/02-redesign-from-screenshot/design-reference.png');
   const DIFF_PATH = resolve('server/tests/__snapshots__/landing-page-golden.diff.png');
   const VIEWPORT = { width: 1280, height: 4000 };
   const REF_W = 605;
   const REF_H = 1280;
   const DIFF_THRESHOLD = 0.22; // 22% — empirical as-shipped floor 18.74% + 3.26pp safety band; see 02-RESEARCH.md §3
   const DEV_URL = 'http://127.0.0.1:5173/';
   ```

4. **Add `bringUpDevServer()` helper** (verbatim from RESEARCH.md §6):
   ```ts
   async function bringUpDevServer(): Promise<ChildProcess> {
     const child = spawn('pnpm', ['exec', 'vite', '--port', '5173', '--host', '127.0.0.1'], {
       stdio: 'pipe',
     });
     for (let i = 0; i < 30; i++) {
       try {
         const res = await fetch(DEV_URL);
         if (res.ok) return child;
       } catch { /* not ready yet */ }
       await new Promise(r => setTimeout(r, 1000));
     }
     child.kill();
     throw new Error('dev server did not start within 30s');
   }
   ```

5. **Replace the test body** (analog `bmw-pilot-viewer.test.ts:240-316`) with the structure from RESEARCH.md §6 lines 344-396 — the full paste-ready test outline. Key elements (in order):
   - `const dev = await bringUpDevServer();`
   - `let browser: Awaited<ReturnType<typeof puppeteer.launch>> | null = null;`
   - `try { browser = await puppeteer.launch({ headless: true, args: ['--font-render-hinting=none', '--disable-font-subpixel-positioning'] }); ... } finally { await browser?.close(); dev.kill(); }`
   - Inside try: `const page = await browser.newPage(); await page.setViewport(VIEWPORT); await page.goto(DEV_URL, { waitUntil: 'networkidle0', timeout: 30_000 }); await new Promise(r => setTimeout(r, 1500)); /* font + Reveal/Counter settle */`
   - `const screenshotData = await page.screenshot({ type: 'png', fullPage: true });`
   - **Buffer-cast guard** (verbatim from analog `bmw-pilot-viewer.test.ts:270-272` — puppeteer 24.x returns `Uint8Array`): `const captureBuf = Buffer.isBuffer(screenshotData) ? screenshotData : Buffer.from(screenshotData as Uint8Array);`
   - **sharp.resize** (NEW vs analog): `const resizedBuf = await sharp(captureBuf).resize(REF_W, REF_H, { kernel: 'cubic', fit: 'fill' }).png().toBuffer();`
   - `const actualPng = PNG.sync.read(resizedBuf); const expectedPng = PNG.sync.read(await readFile(REFERENCE)); expect(actualPng.width).toBe(expectedPng.width); expect(actualPng.height).toBe(expectedPng.height);`
   - `const diff = new PNG({ width: REF_W, height: REF_H }); const mismatched = pixelmatch(actualPng.data, expectedPng.data, diff.data, REF_W, REF_H, { threshold: 0.1 }); const ratio = mismatched / (REF_W * REF_H);`
   - Diff-PNG-on-failure write (verbatim from analog `bmw-pilot-viewer.test.ts:304-310`, path adapted): `if (ratio > DIFF_THRESHOLD) { await writeFile(DIFF_PATH, PNG.sync.write(diff)); console.error(\`[landing-page-golden] Diff ratio \${(ratio * 100).toFixed(2)}% > threshold \${DIFF_THRESHOLD * 100}%. Diff: \${DIFF_PATH}\`); }`
   - `expect(ratio).toBeLessThanOrEqual(DIFF_THRESHOLD);`
   - Test timeout: **60_000** (bumped from analog 30_000 — RESEARCH.md §10 R-1: dev server bring-up + networkidle0 headroom)

6. **DO NOT clone these analog blocks (intentionally dropped):**
   - The first-run bootstrap path at `bmw-pilot-viewer.test.ts:277-287` (`if (!goldenBuf) { await writeFile(GOLDEN, actualBuf); ... }`) — `design-reference.png` is locked at phase open per CONTEXT.md D-02; auto-generation defeats the contract.
   - The `DejaVu Sans` font override on `bmw-pilot-viewer.test.ts:266` — SPA uses Gilroy from `fonts.cdnfonts.com` and there's no cross-platform-stable font baseline; living with the ~19% diff floor (RESEARCH.md §6).
   - The tmpdir + writeReportHtml setup — test target is the running SPA, not a static HTML render.

7. **Header docblock comment** (top of file, before imports):
   ```ts
   // server/tests/landing-page-golden.test.ts
   // Phase 02 D-02 / D-02a: structural-drift guard for the public landing page.
   // Threshold tuned to 0.22 against as-shipped baseline of 18.74% (see 02-RESEARCH.md §3).
   // Test self-orchestrates Vite dev server bring-up; no external orchestration required.
   ```

8. **Per-pixel pixelmatch threshold = 0.1** (NOT the same as DIFF_THRESHOLD = 0.22). The 0.1 is per-pixel YIQ tolerance (pixelmatch standard for screenshot diffs); 0.22 is the ratio of mismatched / total pixels. RESEARCH.md §10 R-7 documents the distinction. Do NOT collapse the two values.

9. **describe block name:** `'landing-page-golden screenshot golden (Phase 02 D-02)'`. **it block name:** `'SPA matches design-reference.png within 22% structural-drift threshold'`.

10. **No `.gitignore` edit needed** — `.gitignore:24` already has `server/tests/__snapshots__/*.diff.png` which matches `landing-page-golden.diff.png` via wildcard (verified during planning).

After writing the file, run `pnpm test landing-page-golden.test.ts` and confirm it exits 0 and prints `1 passed`. The diff ratio printed in the test output should be in the range 18.7–18.9% per RESEARCH.md §3 baseline measurements.
  </action>
  <verify>
    <automated>pnpm test landing-page-golden.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f server/tests/landing-page-golden.test.ts` exits 0
    - `wc -l server/tests/landing-page-golden.test.ts` reports between 70 and 130 lines (the analog is 318; ~80 line target after dropping bootstrap + report-html setup)
    - `grep -c "DIFF_THRESHOLD = 0.22" server/tests/landing-page-golden.test.ts` returns exactly 1
    - `grep -c "REF_W = 605" server/tests/landing-page-golden.test.ts` returns exactly 1
    - `grep -c "REF_H = 1280" server/tests/landing-page-golden.test.ts` returns exactly 1
    - `grep -c "VIEWPORT = { width: 1280, height: 4000 }" server/tests/landing-page-golden.test.ts` returns exactly 1
    - `grep -c "DEV_URL = 'http://127.0.0.1:5173/'" server/tests/landing-page-golden.test.ts` returns exactly 1
    - `grep -c "bringUpDevServer" server/tests/landing-page-golden.test.ts` returns at least 2 (definition + invocation)
    - `grep -c "kernel: 'cubic', fit: 'fill'" server/tests/landing-page-golden.test.ts` returns exactly 1
    - `grep -c "design-reference.png" server/tests/landing-page-golden.test.ts` returns exactly 1
    - `grep -c "writeFile(GOLDEN, actualBuf)" server/tests/landing-page-golden.test.ts` returns 0 (the dropped first-run bootstrap path; we never auto-write the reference)
    - `grep -c "DejaVu" server/tests/landing-page-golden.test.ts` returns 0 (the dropped font override)
    - `grep -c "fullPage: true" server/tests/landing-page-golden.test.ts` returns exactly 1
    - `grep -cE 'threshold:[[:space:]]*0\.1' server/tests/landing-page-golden.test.ts` returns at least 1 (per-pixel tolerance, distinct from DIFF_THRESHOLD; whitespace-tolerant ERE handles formatter line-breaks)
    - `pnpm test landing-page-golden.test.ts` exits 0 with "1 passed" in output
    - `pnpm test` (full suite) exits 0 — existing 207/210 baseline must remain green; the new test adds one more passing test
    - `pnpm typecheck:server` exits 0 (no TS errors introduced)
    - `grep -n 'server/tests/__snapshots__/\*\.diff\.png' .gitignore` returns 1 line at line ~24 (already in place — no `.gitignore` edit performed)
  </acceptance_criteria>
  <done>
    The Phase 2 structural-drift gate is in place. `pnpm test landing-page-golden.test.ts` runs in ≤45 seconds (dev-server bring-up ~5-8s + page load + 1500ms settle + screenshot + resize + diff). Test asserts `ratio ≤ 0.22` and currently measures ~18.7–18.9% as-shipped per RESEARCH.md §3. Wave 1 plans now have a regression guard for every commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Phase 2 is a frontend visual fidelity pass and a test-file addition. No new endpoints, no new auth surface, no new data flows, no new dependencies introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | (n/a) | Phase 2 visual-fidelity test infra | accept | No new attack surface — frontend visual changes only; new test file imports only already-installed deps (puppeteer, sharp, pixelmatch, pngjs) and reads a phase-local PNG; no new external IO; no new secrets; no new user-controlled inputs touched. |
</threat_model>

<verification>
- `pnpm test landing-page-golden.test.ts` exits 0 (≤45s wall-clock)
- `pnpm test` exits 0 (existing baseline + 1 new test all green)
- `pnpm typecheck:server` exits 0
- The diff PNG at `server/tests/__snapshots__/landing-page-golden.diff.png` is NOT created during a passing run (only written on failure)
- `.gitignore` already covers the diff PNG via wildcard at line 24 — no edit performed
</verification>

<success_criteria>
- `server/tests/landing-page-golden.test.ts` exists and is ~70–130 lines
- Test passes against as-shipped baseline at threshold 0.22
- Test self-orchestrates Vite dev server (no external orchestration step in any plan)
- All Wave 1 plans can run `pnpm test landing-page-golden.test.ts` as their primary regression check
- Wave 3 phase-close plan can use this single test as the gate
</success_criteria>

<output>
After completion, create `.planning/phases/02-redesign-from-screenshot/02-01-SUMMARY.md` documenting:
- Final line count of the new test file
- Diff ratio measured on the as-shipped baseline (should be 18.7–18.9% per RESEARCH.md §3; record actual)
- Wall-clock runtime of `pnpm test landing-page-golden.test.ts`
- Confirmation that `.gitignore` wildcard already covers the diff PNG
- Confirmation that no new dependencies were installed
</output>
