---
phase: 02
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - src/sections/Hero.tsx
autonomous: true
requirements: []
gaps_addressed: [G-01]
must_haves:
  truths:
    - "Hero section renders a static radial coral-glow blob in the top-right region (matches design-reference.png)"
    - "The blob does NOT intercept pointer events on hero CTAs or the right-column pipeline card"
    - "The coral-glow CSS pattern is verbatim from LeadMagnet.tsx:6-9 with `pointerEvents: 'none'` added"
    - "No new font sizes or weights introduced (UI-SPEC §Typography Scale Note)"
    - "Sharp corners except pills (border-radius 0 / 999px / 50% only); blob uses borderRadius: '50%' which is already in the allowed set"
    - "House easing `cubic-bezier(.2,.7,.2,1)` — not relevant here (static element, no transition)"
    - "No new component library, no shadcn init, no CSS-in-JS, no Tailwind"
    - "Aria-labels on FloatingDock are non-negotiable (this plan doesn't touch FloatingDock; no regression)"
    - "Phase 2 does NOT touch src/crm/, quizSpec.ts, fonts CDN, real founder content, or multi-market flags"
    - "Opportunistic fix budget = 5min wall-clock; over-budget items go to 02-DEFERRED-POLISH.md"
    - "Golden test passes at DIFF_THRESHOLD=0.22 post-change (G-01 closure should reduce or hold the diff, not increase it)"
  artifacts:
    - path: "src/sections/Hero.tsx"
      provides: "Hero section with static coral-glow blob (G-01)"
      contains: "rgba(213,121,89,0.4)"
  key_links:
    - from: "src/sections/Hero.tsx"
      to: "var(--coral) #D57959 (CSS token via rgba(213,121,89,0.4) pattern)"
      via: "inline radial-gradient style"
      pattern: "radial-gradient.*213,121,89"
---

<objective>
Close G-01: add a static radial coral-glow blob to the top-right of the Hero section, matching the LeadMagnet treatment per UI-SPEC §Gap Analysis. The screenshot shows a coral glow concentrated in the top-right of hero; the current implementation only has a mouse-driven `.spotlight` and is missing the static blob.

Purpose: Direct visual fidelity gain for the hero region. The blob is the largest single visible diff between as-shipped and design-reference.png in the hero region.

Output: One inline `<div>` insertion in `src/sections/Hero.tsx` between the existing gradient overlays (line 48) and the giant `DVApro` outline-italic watermark (line 50).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-redesign-from-screenshot/02-CONTEXT.md
@.planning/phases/02-redesign-from-screenshot/02-RESEARCH.md
@.planning/phases/02-redesign-from-screenshot/02-PATTERNS.md
@.planning/phases/02-redesign-from-screenshot/02-UI-SPEC.md
@.planning/phases/02-redesign-from-screenshot/02-VALIDATION.md

<interfaces>
<!-- Verbatim coral-glow pattern from LeadMagnet.tsx:6-9 (verified 2026-05-07). -->
<!-- Executor pastes this directly with the documented `pointerEvents: 'none'` addition. -->

From `src/sections/LeadMagnet.tsx:6-9`:
```tsx
<div style={{
  position: 'absolute', top: -100, right: -150, width: 700, height: 700, borderRadius: '50%',
  background: 'radial-gradient(circle, rgba(213,121,89,0.4), transparent 60%)', filter: 'blur(80px)',
}} />
```

From `src/styles/global.css:30` (verified): `--coral: #D57959;` — RGB equivalent `213,121,89`.

From `src/sections/Hero.tsx:37`: parent `<header>` has `position: 'relative'` and `overflow: 'hidden'` — required for absolute-positioned children to render correctly.

From `src/sections/Hero.tsx:50-57`: existing `DVApro` watermark already uses `pointerEvents: 'none'` — confirms convention. The new blob is layered BEFORE the watermark in DOM order.

From `src/sections/Hero.tsx:59`: foreground content wrapper uses `<div style={{ position: 'relative', zIndex: 3 }}>` — this layers above all absolutes (no z-index war).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add static coral-glow blob to Hero (G-01)</name>
  <files>src/sections/Hero.tsx</files>
  <read_first>
    - src/sections/Hero.tsx (FULL FILE — needed to understand the layering of background image, gradient overlays, watermark, and zIndex:3 content wrapper; one read pass; identify lines 48 and 50 for insertion point)
    - src/sections/LeadMagnet.tsx lines 1-49 (the canonical analog; coral-glow pattern at lines 6-9)
    - .planning/phases/02-redesign-from-screenshot/02-RESEARCH.md §7 ("Hero Coral-Glow CSS Pattern (G-01)")
    - .planning/phases/02-redesign-from-screenshot/02-PATTERNS.md §"src/sections/Hero.tsx (MODIFY — G-01, component, render)"
    - .planning/phases/02-redesign-from-screenshot/02-UI-SPEC.md §Color §"Coral (`--coral`) reserved for" (line "LeadMagnet coral glow blob" — confirms the treatment is reserved for this purpose)
    - server/tests/landing-page-golden.test.ts (just created in 02-01-PLAN; needed to know what regression check runs after the change)
  </read_first>
  <action>
**Insert a single `<div>` block** in `src/sections/Hero.tsx` between line 48 (the last gradient overlay) and line 50 (the start of the giant `DVApro` outline-italic watermark). The insertion is paste-ready:

```tsx
{/* G-01: static radial coral glow blob (matches LeadMagnet treatment per UI-SPEC).
    Position chosen to match design-reference.png: top-right of hero.
    Pattern verbatim from LeadMagnet.tsx:6-9; pointerEvents added so it doesn't
    intercept clicks on the right-column pipeline card. */}
<div style={{
  position: 'absolute', top: -100, right: -150, width: 700, height: 700, borderRadius: '50%',
  background: 'radial-gradient(circle, rgba(213,121,89,0.4), transparent 60%)', filter: 'blur(80px)',
  pointerEvents: 'none',
}} />
```

**Insertion point precision:** the new `<div>` is a sibling of (a) the background image div at lines 40-46, (b) the two gradient overlay divs at lines 47 and 48, and (c) the DVApro watermark div at lines 50-57. It MUST be placed AFTER line 48 and BEFORE the watermark div on line 50. This produces the layering: `[bg image] → [linear gradients] → [coral glow] → [DVApro watermark] → [content wrapper z-index 3]`.

**Strictly do NOT:**
- Do NOT add `mix-blend-mode` (UI-SPEC §Color split locks the visual treatment; LeadMagnet doesn't use it).
- Do NOT add `z-index` on the glow div — the parent `.container` foreground wrapper at Hero line 59 already uses `zIndex: 3` to layer above all absolutes.
- Do NOT replace `rgba(213,121,89,0.4)` with `var(--coral)` — gradient stops need alpha; UI-SPEC §Color tolerates the documented exception (LeadMagnet-verbatim `rgba(213,121,89,...)` form).
- Do NOT introduce a new CSS class for the glow — UI-SPEC §Design System: inline styles for one-off layout values; the glow is a one-off (a class would imply reuse and we don't want it).
- Do NOT change anything else in Hero.tsx (no copy edits, no layout edits, no other inline-style tweaks). If you spot something else that looks off, log it to `.planning/phases/02-redesign-from-screenshot/02-DEFERRED-POLISH.md` per CONTEXT.md D-01a (5-min budget).

**After the edit, run** `pnpm test landing-page-golden.test.ts` to confirm:
1. The test still passes (ratio ≤ 0.22).
2. The diff ratio reported in test output should be ≤ the as-shipped baseline (~18.7–18.9%) — closing G-01 should reduce drift, not increase it. If the ratio JUMPS above 22%, revert and investigate (likely cause: blob covering content / wrong layering / DOM overflow).
  </action>
  <verify>
    <automated>pnpm test landing-page-golden.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rgba(213,121,89,0.4)" src/sections/Hero.tsx` returns exactly 1
    - `grep -c "radial-gradient(circle, rgba(213,121,89" src/sections/Hero.tsx` returns exactly 1
    - `grep -c "filter: 'blur(80px)'" src/sections/Hero.tsx` returns exactly 1 (the blob's blur)
    - `grep -c "pointerEvents: 'none'" src/sections/Hero.tsx` returns at least 2 (the new blob + the existing DVApro watermark on line 50)
    - `grep -c "borderRadius: '50%'" src/sections/Hero.tsx` returns exactly 1 (the new blob)
    - `grep -nE "var\(--coral\)" src/sections/Hero.tsx | wc -l` returns at least 2 (existing usages preserved at line 73 + 74-equivalent)
    - The new `<div>` is positioned in DOM order between line 48's gradient overlay div and the DVApro watermark div (visual inspection during PR review; or `awk '/style={{/,/<\/div>/' src/sections/Hero.tsx` shows the new block in correct position)
    - `pnpm test landing-page-golden.test.ts` exits 0 with "1 passed"
    - The diff ratio printed by the test is ≤ 22% (passes the gate); ideally ≤ baseline of 18.9% (G-01 closure should reduce drift)
    - `pnpm test` (full suite) exits 0
    - `pnpm typecheck:server` exits 0 (no TS errors)
    - No mix-blend-mode, no new className, no new CSS rule in global.css introduced — `git diff src/styles/global.css` returns nothing for this plan
    - `grep -c "mix-blend-mode\|mixBlendMode" src/sections/Hero.tsx` returns 0
    - File line count delta: +5 to +9 lines (single `<div>` insertion + comment block); `git diff --stat src/sections/Hero.tsx` shows +5..+9 lines
  </acceptance_criteria>
  <done>
    Hero section renders the coral-glow blob in the top-right corner. Visually matches the LeadMagnet glow treatment. Pixel diff against design-reference.png is ≤ 22% (and ideally lower than the as-shipped baseline). G-01 closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure visual change to a presentational React component. No new IO, no new auth surface, no new user-controlled inputs touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | (n/a) | src/sections/Hero.tsx visual change | accept | No new attack surface — frontend visual changes only; inline-style addition uses already-allowed CSS properties; no new external IO; no new secrets; no new dependencies. |
</threat_model>

<verification>
- `pnpm test landing-page-golden.test.ts` exits 0
- `pnpm test` exits 0 (full baseline preserved)
- `pnpm typecheck:server` exits 0
- Manual: open dev server (`pnpm dev`), confirm coral glow visible in top-right of hero, confirm hero CTAs (Подобрать за 5 минут / Каталог) still clickable, confirm right-column pipeline card still clickable
</verification>

<success_criteria>
- G-01 closed (paste-ready snippet matches PATTERNS.md / RESEARCH.md §7 verbatim)
- Coral glow blob renders in correct DOM position (between gradient overlays and DVApro watermark)
- `pointerEvents: 'none'` prevents click-interception
- Golden test continues to pass (ratio ≤ 0.22)
</success_criteria>

<output>
After completion, create `.planning/phases/02-redesign-from-screenshot/02-02-SUMMARY.md` documenting:
- Confirmation that the blob was inserted between line 48 (post-gradient) and line 50 (pre-watermark)
- Diff ratio reported by golden test post-change (compare to ~18.7–18.9% baseline; should hold or decrease)
- Confirmation that no other Hero.tsx code was touched
- Any deferred-polish items observed during the change (if any) appended to `02-DEFERRED-POLISH.md`
</output>
