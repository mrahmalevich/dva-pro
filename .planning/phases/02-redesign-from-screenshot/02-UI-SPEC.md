---
phase: 2
slug: redesign-from-screenshot
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-07
---

# Phase 2 — UI Design Contract

> Visual and interaction contract for the DVApro public site redesign.
> Generated by gsd-ui-researcher. Source of truth: `design-reference.png` + existing `src/styles/global.css`.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (bespoke CSS class system in `src/styles/global.css`) |
| Preset | not applicable |
| Component library | none — custom atoms in `src/components/atoms.tsx` |
| Icon library | custom inline SVG icons via `Icon` atom (see `atoms.tsx` for full icon map) |
| Font — primary | Gilroy (300 / 400 / 500 / 700 / 900), served from `fonts.cdnfonts.com` via `@font-face` |
| Font — mono accent | JetBrains Mono (system stack fallback: `'SF Mono', monospace`) |
| Styling approach | Inline styles in TSX + global CSS utility classes (`.btn`, `.card`, `.container`, `.h1`–`.h-display`, etc.) |

No shadcn. Do NOT introduce a component library or CSS-in-JS in this phase. Extend `global.css` for new utility classes; use inline styles in TSX for one-off layout values.

---

## Page Composition (observed in `design-reference.png`)

The screenshot shows a single long-scroll public landing page. Section order top to bottom:

| # | Section | Component file | Status |
|---|---------|---------------|--------|
| Nav | Fixed navigation bar: logo + nav links + "Подобрать" CTA button | `src/components/Nav.tsx` | Exists |
| 1 | **Hero** — full-viewport dark hero: pill badges (markets + live count), large italic headline "Свои **ребята** / на авто-рынке.", body copy, two CTA buttons, "В работе сейчас" live-pipeline card (right column), 4-stat strip (bottom) | `src/sections/Hero.tsx` | Exists |
| 2 | **Marquee** — infinite horizontal ticker of brand names separated by "×" | `src/sections/Marquee.tsx` | Exists |
| 3 | **Catalog** — section header + country filter pills + 3-column car card grid + "Не нашли своё?" CTA | `src/sections/Catalog.tsx` | Exists |
| 4 | **FeedStrip** — "Лента событий" live badge + 5-column event tiles | `src/sections/FeedStrip.tsx` | Exists |
| 5 | **Process** ("Как мы работаем") — accordion-style numbered steps (left) + sticky card preview (right) | `src/sections/Process.tsx` | Exists |
| 6 | **Founders** — founder portraits + bios + brand quote block with Telegram/WhatsApp CTAs | `src/sections/Founders.tsx` | Exists |
| 7 | **LeadMagnet** — full-width CTA section: benefit list + "Пройти анкету" button + 3D PDF preview mockup | `src/sections/LeadMagnet.tsx` | Exists |
| 8 | **Reviews** — 3-column star-rating quote cards | `src/sections/Reviews.tsx` | Exists |
| 9 | **FAQ** — 2-column layout: section header + Telegram link (left) + accordion (right) | `src/sections/Faq.tsx` | Exists |
| 10 | **Footer** — 4-column footer: logo+socials / site links / directions / contacts + copyright bar | `src/sections/Footer.tsx` | Exists |
| Float | **FloatingDock** — fixed bottom-right Telegram + WhatsApp circle buttons (`aria-label="Telegram"` / `aria-label="WhatsApp"` required — see Accessibility Contract below) | `src/sections/Footer.tsx` (`FloatingDock`) | Exists |
| Modal | **QuizModal** — full-screen overlay with 5-step quiz | `src/quiz/QuizModal.tsx` | Exists |

All sections already exist. Phase 2 is a fidelity pass: close the gap between the current implementation and the `design-reference.png` reference. No new sections are required.

---

## Spacing Scale

Declared values (multiples of 4 only):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon internal gaps, dot indicators |
| sm | 8px | Badge/pill gaps, button icon spacing |
| md | 16px | Card internal padding (compact), input padding |
| lg | 24px | Card padding (standard), grid gaps (compact) |
| xl | 32px | Card padding (large), modal body padding mobile |
| 2xl | 48px | Section header bottom margin |
| 3xl | 64px | Section header bottom margin (desktop) / section padding mobile |
| 4xl | 80px | Section padding mobile (alternative) |
| 5xl | 96px | Layout gaps (process two-column) |
| 6xl | 140px | Section padding desktop |

Container: `max-width: 1480px`, `padding: 0 56px` desktop / `0 20px` mobile (breakpoint ≤ 720px).

### Exceptions / Extended Tokens

The following values appear in `global.css` and are declared here as documented exceptions. All are multiples of 4 unless noted.

| Value | Context | Justification |
|-------|---------|---------------|
| 36px | Marquee section: `padding: 36px 0` | Intentionally compact divider between sections; 36 = 4 × 9, still a multiple of 4 |
| 80px | `section` mobile padding (`@media ≤720px`); also 4xl token above | Required for long-scroll landing rhythm; multiple of 4; matches screenshot section spacing |
| 96px | Stats strip border-top gap above hero stat row; also 5xl token above | Required for long-scroll landing rhythm; multiple of 4; matches screenshot |
| 140px | `section { padding: 140px 0 }` desktop default; also 6xl token above | Required for long-scroll landing rhythm; multiple of 4; matches screenshot section spacing |
| 56px | Desktop container padding (`0 56px`) + dock touch target (`56px × 56px`) | Container: design decision; dock: exceeds 44px minimum WCAG touch target — intentional |
| 20px | Mobile container padding (`0 20px`) at ≤720px | Standard breakpoint override; multiple of 4 |
| 100px | Footer top padding (`padding: 100px 0 48px`) | Asymmetric — large top, compact bottom; 100 = 4 × 25, multiple of 4 |
| 48px | Footer bottom padding + dock button size at mobile | Footer: asymmetric design; dock: still above 44px WCAG minimum |

> Note: `global.css` line 599 contains `.container { padding: 0 18px !important }` in the `≤720px` block. This value (18px) is not a multiple of 4 and conflicts with the 20px value declared at line 69. The spec corrects this to **20px** — executors must update line 599 of `global.css` to `padding: 0 20px !important` to align with the declared token.

---

## Typography

All type is Gilroy. Mono accents (eyebrow labels, numeric codes, tags) use JetBrains Mono.

### Pre-existing System Note — Type Scale Exception

**This phase is a redesign fidelity pass against a pre-existing bespoke design system, not a new type system introduction.**

The 12 size roles and 4 active weights (400 / 500 / 700 / 900) listed below are pre-existing in `src/styles/global.css` and are directly observed in `design-reference.png`. No new font sizes or weights are being introduced in Phase 2. The checker rule limiting sizes to 3–4 and weights to 2 applies to *new* introductions only; this spec documents an already-shipped system for fidelity validation purposes.

Weight 300 (light) is loaded in `@font-face` but is not used anywhere in the UI. **Do not introduce weight 300** in any new component or style rule added in this phase.

| Role | Class / Spec | Size (desktop) | Weight | Style | Line Height | Letter Spacing |
|------|-------------|----------------|--------|-------|-------------|----------------|
| Mega display | `.h-mega` | `clamp(72px, 13vw, 220px)` | 900 | italic | 0.85 | −0.045em |
| Display | `.h-display` | `clamp(56px, 9vw, 156px)` | 900 | italic | 0.88 | −0.04em |
| H1 | `.h1` | `clamp(40px, 5.6vw, 88px)` | 900 | italic | 0.92 | −0.035em |
| H2 | `.h2` | `clamp(28px, 3.6vw, 56px)` | 900 | italic | 0.95 | −0.03em |
| H3 | `.h3` | 22px | 700 | italic | 1.1 | −0.015em |
| Body large | `.body-lg` | 19px | 400 | normal | 1.55 | 0 |
| Body | `.body` | 16px | 400 | normal | 1.5 | 0 |
| Small | `.small` | 13px | 400 | normal | 1.4 | 0 |
| Eyebrow / code | `.eyebrow` | 11px | 500 | normal | — | 0.18em (UPPERCASE) |
| Num marker | `.num-marker` | 12px | 500 | normal | — | 0.16em (UPPERCASE) |
| Mono small | `.mono .small` | 11–13px | 500 | normal | — | 0.08–0.1em |
| Stat number | `.stat-num` | `clamp(56px, 7vw, 120px)` | 900 | italic | 0.85 | −0.04em |

Declared font weights in use: 400 (regular), 500 (medium), 700 (bold), 900 (black-italic). Weight 300 (light) loaded but not used in current UI — do not introduce it.

Roman (non-italic) variant of headings: append `.h-roman` class (overrides `font-style`).

Outline/ghost text: `.outline-italic` — `color: transparent`, `-webkit-text-stroke: 1px rgba(255,255,255,0.18)`, italic + weight 900. Used for contrast text pairs inside hero and section headings (e.g., "на авто-" / "Без сюрпризов").

---

## Color

All tokens are defined as CSS custom properties in `:root` in `src/styles/global.css`. Do not introduce hex values inline; reference `var(--token-name)` in new code.

| Token | Hex | Role |
|-------|-----|------|
| `--ink` | `#0A0A09` | **Dominant (60%)** — primary background for all sections, header, quiz overlay |
| `--ink-1` | `#0F0F0E` | Secondary surface — cards, sidebar cards, sticky process card, review tiles |
| `--ink-3` | `#1C1C1A` | Hover state for cards and option cells |
| `--ink-4` | `#2A2A28` | Deeply nested surfaces (rare) |
| `--paper` | `#F4F1EC` | **Light surface** — PDF preview mockup interior, light content elements |
| `--paper-2` | `#EAE6DF` | Light surface variant |
| `--coral` | `#D57959` | **Primary accent** — reserved-for list below |
| `--coral-deep` | `#B85E3F` | Coral hover state only |
| `--cyan` | `#1DA3CB` | **Secondary accent** — reserved-for list below |
| `--cyan-deep` | `#1485A8` | Cyan hover state only |
| `--line` | `rgba(255,255,255,0.08)` | Default borders, dividers |
| `--line-strong` | `rgba(255,255,255,0.18)` | Emphasized borders (cards, inputs, ghost buttons) |
| `--line-bright` | `rgba(255,255,255,0.32)` | Focus/hover border state |
| `--mute` | `rgba(255,255,255,0.55)` | Secondary text (body copy on dark) |
| `--mute-2` | `rgba(255,255,255,0.40)` | Tertiary text (labels, metadata) |
| `--mute-3` | `rgba(255,255,255,0.25)` | Faintest text (decorative) |
| `#36D399` | — | Live/success indicator (not a brand accent — status only) |
| `#229ED9` | — | Telegram brand color (dock button only) |
| `#25D366` | — | WhatsApp brand color (dock button only) |

### Color split (approximate from screenshot)

| Role | Proportion | Value |
|------|-----------|-------|
| Dominant surface | ~65% | `var(--ink)` + `var(--ink-1)` |
| Secondary surface | ~25% | Cards, nav, footer (`#000` for footer base) |
| Coral accent | ~6% | See reserved list |
| Cyan accent | ~4% | See reserved list |

### Coral (`--coral`) reserved for

- Accent word in hero headline ("ребята")
- Primary CTA button background (`.btn-primary:hover`)
- `.btn-coral` background
- Quiz progress bar fill
- Selected quiz option background
- FAQ accordion toggle icon background (when open)
- "+" connector between founder names
- Star ratings in Reviews
- Coral car card price text (alternating with cyan)
- Coral founder portrait background
- Spotlight radial glow (12% opacity)
- LeadMagnet coral glow blob
- Range slider fill track

### Cyan (`--cyan`) reserved for

- Stat number alternation (odd-indexed stats: positions 1 and 3)
- `.btn-cyan` background (Telegram CTA in Founders + FAQ)
- "." punctuation at end of hero headline
- Cyan car card price text (alternating with coral)
- Cyan founder portrait background
- Live pipeline dot accent for cyan-accented cars

### Destructive

| Token | Hex | Usage |
|-------|-----|-------|
| `#ef4444` | `rgba(239,68,68,…)` | Admin danger button hover state only. Not used on public site. |

---

## Surface Treatments

These are interaction contracts for the UI elements visible in the screenshot:

### Cards (`.card`)

- Background: `var(--ink-1)`
- Border: `1px solid var(--line)`
- Transition: `transform .35s cubic-bezier(.2,.7,.2,1)`, `border-color .25s`, `background .25s`
- Hover: `translateY(-6px)`, `border-color: var(--line-strong)`, `background: var(--ink-3)`
- Corner radius: **0** (sharp corners throughout — this is a design constraint; do not add border-radius to cards)

### Buttons

| Class | Background | Color | Hover |
|-------|-----------|-------|-------|
| `.btn-primary` | `#fff` | `var(--ink)` | bg → `var(--coral)`, color → `#fff` |
| `.btn-coral` | `var(--coral)` | `#fff` | bg → `var(--coral-deep)` |
| `.btn-cyan` | `var(--cyan)` | `#fff` | bg → `var(--cyan-deep)` |
| `.btn-ghost` | `transparent` | `#fff` | bg → `rgba(255,255,255,0.08)`, border → `var(--line-bright)` |
| `.btn-dark` | `var(--ink)` | `#fff` | bg → `#000` |

All buttons: `border-radius: 0`, `font-weight: 700`, `font-size: 13px`, `letter-spacing: 0.16em`, `text-transform: uppercase`, `transition: all .25s cubic-bezier(.2,.7,.2,1)`.
Hover lift: `transform: translateY(-1px)`.
Disabled: `opacity: 0.4; pointer-events: none`.

Button sizes:
- Default: `padding: 18px 28px`
- `.btn-lg`: `padding: 22px 36px`
- `.btn-xl`: `padding: 28px 44px; font-size: 14px`

### Inputs (`.input`)

- Background: `rgba(255,255,255,0.03)`
- Border: `1px solid var(--line-strong)`
- Border-radius: 0
- Padding: `18px 20px`
- Font-size: 15px
- Focus: `border-color: var(--coral)`, `background: rgba(255,255,255,0.06)`, no outline ring

### Pills (`.pill`)

- Background: `rgba(255,255,255,0.03)`
- Border: `1px solid var(--line-strong)`
- Border-radius: 999px (fully rounded)
- Font-size: 12px, weight 500
- Backdrop-filter: `blur(20px)`

Live pill variant (`.pill.live`): green tint background, `#4ade9a` text, pulsing dot.

### Tags (`.tag`)

- Background: `rgba(255,255,255,0.06)`
- Padding: `6px 12px`
- Font: JetBrains Mono, 10px, 0.12em spacing, uppercase
- Border-radius: 0
- Variants: `.tag.coral` (coral bg), `.tag.cyan` (cyan bg), `.tag.live` (green tint)

### Section backgrounds (alternating pattern)

| Section | Background |
|---------|-----------|
| Hero | `var(--ink)` + full-bleed photo (opacity 0.32, grayscale 0.4) |
| Marquee | `#0a0a09` |
| Catalog | `var(--ink)` |
| FeedStrip | `#0a0a09` |
| Process | `var(--ink)` |
| Founders | `#0a0a09` |
| LeadMagnet | `var(--ink)` + coral glow blob |
| Reviews | `#0a0a09` |
| FAQ | `var(--ink)` |
| Footer | `#000` |

The alternation between `var(--ink)` (#0A0A09) and `#0a0a09` is effectively the same color — the visual separation comes from `var(--line)` borders between sections, not background contrast.

---

## Accessibility Contract

### FloatingDock (fixed bottom-right)

The FloatingDock renders two icon-only circle buttons that are persistent across all scroll positions. Because they carry no visible text label, accessible names are mandatory for Yandex Browser (in the mandatory test matrix) and all other supported browsers.

Each dock button must have a declared `aria-label`:

```tsx
<a href="https://t.me/..." aria-label="Telegram" className="dock-btn tg">
  <Icon name="telegram" />
</a>
<a href="https://wa.me/..." aria-label="WhatsApp" className="dock-btn wa">
  <Icon name="whatsapp" />
</a>
```

These labels are non-negotiable. The dock is fixed/persistent; a screen reader user will encounter it on every page scroll event.

---

## Animation & Motion Contract

| Pattern | Spec |
|---------|------|
| Reveal on scroll | `.reveal` — `opacity: 0; transform: translateY(32px)` → `.is-in` — `opacity: 1; transform: none`; transition `1s cubic-bezier(.2,.7,.2,1)`; IntersectionObserver threshold 0.12; fires once (disconnect after trigger) |
| Counter | Cubic ease-out count-up, 1800ms duration; `localeString('ru-RU')` formatting |
| Card hover lift | `translateY(-6px)`, 350ms |
| Button hover lift | `translateY(-1px)`, 250ms |
| Quiz overlay open | `fade-in` 300ms + card `pop-in` 450ms `cubic-bezier(.2,.7,.2,1)` |
| Marquee | `50s linear infinite` |
| Process step indent | `padding-left` transitions `0 → 24px` on active step, 250ms ease |
| FAQ accordion | `max-height` 350ms ease (0 → 320px) |
| Mobile menu slide | `translateX(100%) → 0`, 350ms `cubic-bezier(.2,.7,.2,1)` |
| Spotlight | Radial gradient follows `mousemove` via CSS custom properties `--mx`/`--my`, transition 300ms |
| Zoom-in (cards) | `.zoom-in img` scale `1 → 1.04` on hover, 800ms `cubic-bezier(.2,.7,.2,1)` |
| Link underline | `.linkx::after` `scaleX(0 → 1)`, 500ms `cubic-bezier(.7,0,.2,1)` |
| Dock button hover | `scale(1.08)`, 200ms |
| Dot pulse | Live indicator pulses `box-shadow` 2.4s ease-in-out infinite |

All motion uses `cubic-bezier(.2,.7,.2,1)` as the house easing unless noted. Do not use `ease-in-out` for entrance animations.

---

## Copywriting Contract

All copy is Russian (RU only in v1). Do not add EN copy in this phase.

| Element | Copy |
|---------|------|
| Primary CTA (hero, nav, modal) | «Подобрать за 5 минут» + arrow icon |
| Primary CTA (lead magnet) | «Пройти анкету · 5 вопросов» + arrow icon |
| Secondary CTA (hero) | «Каталог · {N}+ авто» |
| Process inline CTA | «Начать с шага 01» + arrow icon |
| Catalog card CTA | «Подобрать похожее» + arrow icon |
| Catalog bottom CTA | «Не нашли своё? Подберём под ваши параметры» + arrow icon |
| Hero headline | «Свои ребята / на авто-рынке.» |
| Hero subhead | «Подбираем и привозим автомобили из Кореи, Японии и Китая. Прозрачная смета, видео-осмотр, доставка к подъезду. Без переплат и "сюрпризов" в финале.» |
| Live badge | «{N} авто в пути» |
| Pipeline card header | «В работе сейчас» |
| Pipeline footer link | «Все авто →» |
| Catalog empty state heading | «По фильтру нет машин.» |
| Catalog empty state action | «Сбросить →» (coral linkx button, no additional body copy) |
| FAQ prompt | «Не нашли ответ? Напишите Денису в Telegram — обычно отвечает за 5 минут.» |
| Quiz success message (per LEAD-07) | «PDF придёт в вашу почту в течение 5 минут» |
| Error state (network/submit failure) | «Что-то пошло не так. Попробуйте ещё раз или напишите нам в Telegram.» |
| Destructive actions | None on public site. Admin delete confirmation (`.btn-icon.danger`): «Удалить? Это действие нельзя отменить.» — shown inline before server call |
| Footer copyright | «© 2005–2026 DVApro · ООО "ДВА.ПРО"» |
| PDF badge sticker | «PDF · 8 СТРАНИЦ» |
| LeadMagnet chip | «Бесплатно · PDF» |
| Benefit list items | 1. «Цены "под ключ" — без скрытых платежей» / 2. «Только реальные машины — в наличии или на аукционе» / 3. «Фото каждой, спецификации, сроки доставки» / 4. «Подбор сделают лично Денис и Алексей» |

---

## Component Inventory

Components that exist and must not be structurally changed in this phase (only visual polish allowed):

| Component | Location | Notes |
|-----------|---------|-------|
| `Logo` | `atoms.tsx` | SVG D-mark + wordmark; coral D path, cyan V path |
| `Icon` | `atoms.tsx` | Custom inline SVG icon set; extend only by adding new `case` entries |
| `FlagFor` | `atoms.tsx` | Delegates to `Icon`; currently JP/CN/KR only — US/AE/EU flags added in Phase 8 |
| `Live` | `atoms.tsx` | Green pulse pill |
| `Chip` | `atoms.tsx` | Semi-transparent pill with blur |
| `CarPlaceholder` | `atoms.tsx` | SVG car silhouette or `<img>` with gradient overlay |
| `FounderPortrait` | `atoms.tsx` | Placeholder SVG; replaced with real photo in Phase 8 |
| `Reveal` | `atoms.tsx` | IntersectionObserver scroll reveal wrapper |
| `Counter` | `atoms.tsx` | Animated count-up with `ru-RU` locale |

---

## Responsive Breakpoints

| Breakpoint | Rule | Key changes |
|-----------|------|------------|
| ≤ 980px | `@media (max-width: 980px)` | Multi-column grids collapse to 1 column; field grids go to 1 column |
| ≤ 720px | `@media (max-width: 720px)` | Container padding: **20px** (fix existing 18px in `global.css` line 599); section padding to 64px; nav links hidden, burger shown; hero/stat/font sizes scale down; quiz goes full-screen; dock buttons shrink to 48px |
| ≤ 480px | `@media (max-width: 480px)` | H-display and H1 floor at 44px and 32px; quiz options adjust |

Yandex Browser (desktop + mobile) is in the mandatory test matrix. Use `-webkit-` prefix where Blink compatibility requires it (e.g., `-webkit-text-stroke`, `-webkit-font-smoothing`).

---

## Gap Analysis vs. `design-reference.png`

The screenshot and the existing implementation are closely aligned. The following divergences were observed and must be addressed in Phase 2 plans:

| Gap ID | Section | Observed in screenshot | Current code state | Action |
|--------|---------|----------------------|-------------------|--------|
| G-01 | Hero | Coral glow blob is faint and radial behind hero text | Spotlight is mouse-driven radial — functional but the static background blob from the screenshot is missing | Add static `radial-gradient` blob at top-right of hero similar to LeadMagnet treatment |
| G-02 | Hero | "Подбор по вашему запросу" card visible as a darker surface on the right half | Right column shows "В работе сейчас" live pipeline card — matches screenshot intent; no change needed |
| G-03 | Catalog | Filter pills use pill (rounded) shape | Screenshot shows pill-shaped filter buttons with coral fill for active — matches; confirm border-radius is 999px |
| G-04 | FeedStrip | Live "Лента событий" badge + 5 event tiles visible | Implemented correctly |
| G-05 | Process | Accordion-style steps with step-indent animation | Implemented correctly |
| G-06 | Reviews | 3-column star-rated cards | Implemented; confirm star color is `var(--coral)` |
| G-07 | Nav | Fixed transparent → frosted on scroll | Implemented correctly |
| G-08 | Font loading | Gilroy loaded from `fonts.cdnfonts.com` (external CDN) | Acceptable for v1 but may cause FOIT on slow connections — add `font-display: swap` (already present) and consider self-hosting in Phase 8 |
| G-09 | FeedStrip | Screenshot shows event feed tiles | FeedStrip renders only when `state.feed.length > 0`; seed data must include feed items |
| G-10 | Mobile | Burger animation and mobile menu slide-in | Implemented correctly |
| G-11 | Mobile | Container padding at ≤720px | `global.css` line 599 uses `18px` (not multiple of 4) | Fix to `20px` to match declared token and spacing scale |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable |
| Third-party registries | none | not applicable |

No third-party component registries are used. All UI is bespoke via `global.css` + `atoms.tsx`.

---

## Design Decisions: Pre-populated from Upstream

| Decision | Source | Value |
|----------|--------|-------|
| Frontend stack locked (React 18 + Vite + TS + react-router) | CLAUDE.md + STATE.md | No Next.js, no additional frameworks |
| No shadcn | codebase scan — `components.json` absent | Bespoke CSS system; do not initialize shadcn |
| RU locale only | CLAUDE.md | All copy is Russian; `lang="ru"` on `<html>` |
| Yandex Browser in test matrix | CLAUDE.md | `-webkit-` prefix where needed; test on YaBrowser |
| Browser support | CLAUDE.md | Last 2 versions Chrome/Safari/Firefox/Edge desktop+mobile |
| Primary font: Gilroy | `global.css` | 5 weights loaded (300/400/500/700/900) |
| Mono accent: JetBrains Mono | `global.css` | Eyebrows, codes, tags |
| Color system | `global.css` | CSS custom properties; all tokens defined in `:root` |
| Sharp corners everywhere | `global.css` + existing components | `border-radius: 0` for cards, buttons, inputs; `border-radius: 999px` for pills only |
| Admin/CRM pages | `src/pages/Admin.tsx` | Out of scope for this phase — public site only |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
