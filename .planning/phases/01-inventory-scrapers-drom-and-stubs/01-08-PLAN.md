---
phase: 01-inventory-scrapers-drom-and-stubs
plan: 08
type: execute
wave: 5
depends_on: [07]
files_modified:
  - data/scraped/SCHEMA.md
  - data/scraped/README.md
autonomous: true
requirements: []
tags: [docs, schema, readme, phase-3-handoff]
must_haves:
  truths:
    - "data/scraped/SCHEMA.md documents every field of models.json (drom) per D-10"
    - "data/scraped/SCHEMA.md describes Phase 4 importer contract (upsert key = (brand_slug, model_slug, generation))"
    - "data/scraped/SCHEMA.md notes the drom = models.json vs others = cars.json terminology distinction"
    - "data/scraped/README.md documents `pnpm scrape:drom` invocation, exit codes, output dir layout, Phase 4 consume contract"
    - "data/scraped/README.md documents the Phase 4 'always re-resolve current/' rule (Pitfall 7)"
    - "data/scraped/README.md notes Windows is not supported (symlinks)"
    - "data/scraped/README.md notes Phase 1 requires zero secrets"
    - "data/scraped/README.md documents manual prune command for old run dirs"
  artifacts:
    - path: "data/scraped/SCHEMA.md"
      provides: "Field-by-field documentation of models.json + Phase 4 importer contract"
      contains: "brand_slug"
    - path: "data/scraped/README.md"
      provides: "How to run + where output lands + Phase 4 consume contract + OS support note"
      contains: "pnpm scrape"
  key_links:
    - from: "data/scraped/SCHEMA.md"
      to: "server/scrapers/shared/types.ts"
      via: "Reference: 'edit shared/types.ts; SCHEMA.md is derived'"
      pattern: "shared/types.ts"
    - from: "data/scraped/README.md"
      to: "data/scraped/drom/current/"
      via: "Phase 4 import contract"
      pattern: "current/"
---

<objective>
Phase 1 plan 08 — Documentation: SCHEMA.md + README.md.

Two committed Markdown files that complete the Phase 1 → Phase 4 handoff contract:
- `data/scraped/SCHEMA.md` — exhaustive field-by-field doc of `models.json` (drom = master models). Includes a worked BMW X5 G05 example. Lists Phase 4 importer's upsert contract: `(brand_slug, model_slug, generation)` is the unique key. Also documents the future `cars.json` shape (specific listings) for the v1.x stubbed sources, even though only drom is real in Phase 1.
- `data/scraped/README.md` — runbook: how to invoke `pnpm scrape:drom`, exit codes, where output lands, the `current/` symlink contract, manual prune command, OS support, secrets policy, the Phase 4 "always re-resolve `current/` per invocation" rule (Pitfall 7).

Purpose: Phase 1's primary deliverable beyond code is the SCHEMA contract. Phase 4's importer is a near-pure JSON→SQL mapping, but only IF SCHEMA.md is precise. Plan 09's live smoke run depends on README's runbook being correct.
Output: 2 Markdown files in Russian + English (Russian for prose where natural, English for code/file paths/tooling — matching CONTEXT.md style).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-inventory-scrapers-drom-and-stubs/01-CONTEXT.md
@.planning/phases/01-inventory-scrapers-drom-and-stubs/01-RESEARCH.md
@.planning/phases/01-inventory-scrapers-drom-and-stubs/01-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@server/scrapers/shared/types.ts

<interfaces>
SCHEMA.md must mirror the zod schema in `server/scrapers/shared/types.ts`. Per RESEARCH.md Pitfall 6 (lines 844-850): keep the TS schema as single source of truth and either generate SCHEMA.md from it OR maintain it manually with a comment "edit `server/scrapers/shared/types.ts` instead". Plan 08 takes the manual-with-pointer approach (no codegen for v1).

README.md must cover (per CONTEXT.md `<code_context>` line 141 + RESEARCH.md line 156, 792, Pitfall 7, Open Question 3):
1. How to run: `pnpm scrape:drom` invocation, exit codes 0/1/2/3.
2. Output dir layout (per D-06).
3. Phase 4 consume contract: ALWAYS re-resolve `current/` per invocation; never cache realpath.
4. Manual prune: `find data/scraped/drom -maxdepth 1 -name "20*Z" -mtime +30 -exec rm -rf {} +`.
5. OS support: macOS dev + Linux CI; Windows not supported (symlinks).
6. Secrets: NONE required.
7. Crash recovery: brand-boundary cursor; resume policy.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create data/scraped/SCHEMA.md</name>
  <files>data/scraped/SCHEMA.md (NEW)</files>
  <read_first>
    - /Users/mikhailra/Developer/dva.pro/.planning/phases/01-inventory-scrapers-drom-and-stubs/01-CONTEXT.md D-10 (full ModelRecord field list)
    - /Users/mikhailra/Developer/dva.pro/.planning/phases/01-inventory-scrapers-drom-and-stubs/01-CONTEXT.md `<specifics>` lines 152-153 (drom = models.json; future stubs = cars.json)
    - /Users/mikhailra/Developer/dva.pro/.planning/research/ARCHITECTURE.md (line 555 — `models` table SQL sketch that records mirror)
    - /Users/mikhailra/Developer/dva.pro/server/scrapers/shared/types.ts (the single source of truth — SCHEMA.md mirrors this)
    - /Users/mikhailra/Developer/dva.pro/.planning/phases/01-inventory-scrapers-drom-and-stubs/01-PATTERNS.md §SCHEMA.md (lines 1060-1068)
  </read_first>
  <action>
Create `data/scraped/SCHEMA.md` with the following content (this file is committed — verify against the .gitignore negation `!data/scraped/SCHEMA.md` from plan 01):

```markdown
# data/scraped/ — Schema Reference

**Last updated:** when plan 01-08 lands.
**Single source of truth:** `server/scrapers/shared/types.ts` (zod schema). This document mirrors it for human consumption. **If a field name in this doc disagrees with the TS schema, the TS schema wins** — edit `server/scrapers/shared/types.ts` and re-sync this doc.

---

## Overview

Phase 1 produces two record shapes — only one is real in Phase 1; the other is reserved by IScraper contract for v1.x:

| File | Shape | Source(s) | Real in Phase 1? |
|------|-------|-----------|------------------|
| `data/scraped/drom/<run_id>/models.json` | `ModelRecord[]` (master models — brand × model × generation) | drom.ru/catalog | ✅ yes |
| `data/scraped/<source>/<run_id>/cars.json` | `CarListing[]` (specific listings with VIN, mileage, photos) | encar / beforward / che168 / autohome | ❌ no — stubs only; deferred to v1.x |

The terminology distinction matters: **drom outputs MASTER MODELS** (one row per brand+model+generation, with year ranges, body types, engine variants), **NOT specific car listings**. Phase 4's importer writes drom records to the `models` table; the (future) v1.x scrapers will write to the `cars` table.

---

## `ModelRecord` (drom — `models.json`)

**Uniqueness key for Phase 4 upsert:** `(brand_slug, model_slug, generation)`. This matches `models.UNIQUE` in `.planning/research/ARCHITECTURE.md:555`.

| Field | Type | Required | Notes |
|-------|------|----------|-------|
| `brand` | `string` | yes | Display name (Cyrillic preferred where drom exposes it; otherwise Latin), e.g. `BMW` |
| `brand_slug` | `string` | yes | Lowercase ASCII slug, filesystem-safe, e.g. `bmw` |
| `model` | `string` | yes | Display name, e.g. `X5` |
| `model_slug` | `string` | yes | Lowercase ASCII slug, e.g. `x5` |
| `generation` | `string` | yes | Drom generation identifier, e.g. `g_2018_8395`. May also be `g_<YYYYMM>_<id>` for some models (per RESEARCH A4) |
| `year_from` | `number \| null` | yes (nullable) | 4-digit year of generation start; `null` if drom did not surface a year |
| `year_to` | `number \| null` | yes (nullable) | 4-digit year of generation end; `null` for currently-produced generations (drom shows «н.в.») |
| `body_types` | `string[]` | yes | Non-empty array, e.g. `["SUV"]` or `["SUV", "Crossover"]`. Strict zod validation rejects empty arrays — Pitfall 1 DOM-regression detector |
| `engine_options` | `{cc: number; hp: number; fuel: 'gas' \| 'diesel' \| 'hybrid' \| 'electric'}[]` | yes | Non-empty array of engine variants per generation; e.g. `[{"cc": 1995, "hp": 190, "fuel": "diesel"}, {"cc": 2998, "hp": 340, "fuel": "gas"}]` |
| `drive_options` | `string[]` | yes | Drivetrain types, e.g. `["AWD"]` or `["RWD", "AWD"]` |
| `description_ru` | `string` | yes | Cyrillic editorial blurb from drom catalog page; non-empty |
| `price_min_rub` | `number \| null` | yes (nullable) | New-car price floor in RUB; `null` if drom does not show a price (older generations no longer in production) |
| `price_max_rub` | `number \| null` | yes (nullable) | New-car price ceiling in RUB; `null` if drom shows only a single price |
| `image_paths` | `string[]` | yes | Relative paths under run dir, exactly 0 or 1 element per record. Per D-11: one hero WebP, format `images/<brand_slug>-<model_slug>-<generation>-hero.webp`. Empty array if drom page had no usable image |
| `source` | `'drom-catalog'` (literal) | yes | Always this exact string for Phase 1 |
| `source_url` | `string` (URL) | yes | Drom generation page URL |
| `scraped_at` | `string` (ISO-8601) | yes | UTC timestamp of when the record was parsed |

### Worked example — BMW X5 G05

```json
{
  "brand": "BMW",
  "brand_slug": "bmw",
  "model": "X5",
  "model_slug": "x5",
  "generation": "g_2018_8395",
  "year_from": 2018,
  "year_to": 2022,
  "body_types": ["SUV"],
  "engine_options": [
    { "cc": 1995, "hp": 190, "fuel": "diesel" },
    { "cc": 2998, "hp": 340, "fuel": "gas" }
  ],
  "drive_options": ["AWD"],
  "description_ru": "Четвёртое поколение премиального кроссовера...",
  "price_min_rub": 5470000,
  "price_max_rub": 9890000,
  "image_paths": ["images/bmw-x5-g_2018_8395-hero.webp"],
  "source": "drom-catalog",
  "source_url": "https://www.drom.ru/catalog/bmw/x5/g_2018_8395/",
  "scraped_at": "2026-04-28T12:00:00.000Z"
}
```

---

## `CarListing` (v1.x reservation — `cars.json`)

NOT EMITTED by Phase 1. Sketched here so Phase 4's importer schema and v1.x scraper authors share a contract. Will be defined alongside the first v1.x scraper landing (encar → cars.json).

Expected fields (NON-binding sketch):
- `vin: string` — vehicle identification number
- `mileage_km: number`
- `condition: 'new' | 'used' | 'auction'`
- `country: 'kr' | 'jp' | 'cn' | 'us' | 'ae' | 'eu'`
- `source: 'encar' | 'beforward' | 'che168' | 'autohome'`
- `source_id: string` — listing-specific id from the source platform
- ...

**Phase 1 stubs return `{status: 'not_implemented'}` and write nothing.** When v1.x lands a real implementation, this section becomes binding.

---

## Run telemetry — `report.json`

Every drom run writes `report.json` with these fields (D-17):

| Field | Type | Notes |
|-------|------|-------|
| `started_at` | ISO-8601 | When `drom.run()` began |
| `finished_at` | ISO-8601 | When the run terminated (success / blocked / error) |
| `duration_ms` | number | `finished_at - started_at` in ms |
| `pages_visited` | number | Total drom HTTP fetches (brand index + model lists + generation lists + generation pages) |
| `models_added` | number | Records that passed zod validation and were written to `models.json` |
| `models_updated` | number | Always `0` in Phase 1 (no upsert); placeholder for Phase 4 importer parity |
| `images_downloaded` | number | WebP files written under `images/` |
| `images_skipped` | number | Records with no hero or with image-fetch failure |
| `errors[]` | `{url, message}[]` | Per-record parse / image-fetch failures (NOT fatal — run continues) |
| `rate_limit_hits` | number | Reserved for Phase 1.x — Phase 1 does not currently surface 429s into this counter |
| `blocked_responses` | number | Increments on `BlockedError` (5+ thin OR captcha keyword); run halts when this hits 1 |
| `fx_stale` | boolean | `true` if `fetchFx` returned `source: 'cbr-cache'` (live failed; cached fallback used per D-12) |
| `cursor_resumed` | boolean | `true` if `.cursor.json` was read at run start |
| `final_status` | `'ok' \| 'blocked' \| 'error'` | Mirrors the `ScrapeResult.status` returned by the orchestrator |

---

## Brand-aliases — `data/scraped/drom/brand-aliases.json`

Cyrillic↔Latin lookup, idempotent merge by `brand_slug` (D-16, SCRAPE-10):

```json
{
  "audi": {
    "ru": "Ауди",
    "latin": "Audi",
    "models": {
      "a4": { "ru": "А4", "latin": "A4" }
    }
  },
  "bmw": {
    "ru": "БМВ",
    "latin": "BMW",
    "models": {
      "x3": { "ru": "Х3", "latin": "X3" },
      "x5": { "ru": "Х5", "latin": "X5" }
    }
  }
}
```

Keys are sorted alphabetically (brand-level + model-level) for deterministic byte-stable output across runs.

---

## CBR FX cache — `data/scraped/fx/cbr-<YYYY-MM-DD>.json`

Per-day RUB rates (D-12, SCRAPE-11):

```json
{
  "date": "2026-04-28",
  "rates": {
    "USD": 91.3145,
    "EUR": 97.5210,
    "JPY": 0.591234,
    "KRW": 0.0659012,
    "CNY": 12.5670,
    "AED": 24.8624
  },
  "source": "cbr-live"
}
```

`source` is `'cbr-live'` when the file was just fetched; `'cbr-cache'` when it was returned to the orchestrator from a prior file (D-12 fallback).

---

## Phase 4 importer contract

Phase 4 (`pnpm import:scraped`) reads `data/scraped/drom/current/models.json` and upserts into the Drizzle `models` table.

- **Upsert key:** `(brand_slug, model_slug, generation)` — already enforced as a `UNIQUE` constraint in the `models` table sketch (`.planning/research/ARCHITECTURE.md:555`).
- **Image rehost:** local `data/scraped/drom/<run_id>/images/*.webp` → Yandex Object Storage `images/models/<brand_slug>/<model_slug>/<generation>-hero.webp`. The local path is RELATIVE under the run dir; the importer resolves via `current/` symlink.
- **Pitfall 7 contract:** the importer MUST always re-resolve `current/` per invocation. Never cache `realpath()` across runs.

---

## Editing this file

To change the schema:
1. Edit `server/scrapers/shared/types.ts` first (zod schema is the source of truth).
2. Run `pnpm tsc -p tsconfig.server.json --noEmit` to confirm no type errors.
3. Update this file to mirror the new schema.
4. Bump the "Last updated" line at the top.

If a future v1.x phase adds codegen (e.g., `scripts/build-schema-md.ts`), this manual sync step goes away.
```
  </action>
  <verify>
    <automated>test -f data/scraped/SCHEMA.md &amp;&amp; grep -q "ModelRecord" data/scraped/SCHEMA.md &amp;&amp; grep -q "brand_slug" data/scraped/SCHEMA.md &amp;&amp; grep -q "model_slug" data/scraped/SCHEMA.md &amp;&amp; grep -q "generation" data/scraped/SCHEMA.md &amp;&amp; grep -q "image_paths" data/scraped/SCHEMA.md &amp;&amp; grep -q "Phase 4" data/scraped/SCHEMA.md &amp;&amp; grep -q "(brand_slug, model_slug, generation)" data/scraped/SCHEMA.md &amp;&amp; ! git check-ignore -q data/scraped/SCHEMA.md</automated>
  </verify>
  <acceptance_criteria>
    - `data/scraped/SCHEMA.md` exists and is committed (NOT gitignored — verify `git check-ignore -q data/scraped/SCHEMA.md` returns nonzero meaning "not ignored")
    - File documents every field of `ModelRecord`: brand, brand_slug, model, model_slug, generation, year_from, year_to, body_types, engine_options, drive_options, description_ru, price_min_rub, price_max_rub, image_paths, source, source_url, scraped_at (grep each field name)
    - File contains the BMW X5 G05 worked example with `g_2018_8395` (grep)
    - File documents Phase 4 upsert key `(brand_slug, model_slug, generation)` (grep)
    - File documents the drom = models.json vs others = cars.json distinction (grep "MASTER MODELS" OR similar)
    - File documents D-17 fields of `report.json` (grep at least 6 of the 14 D-17 fields)
    - File mentions `server/scrapers/shared/types.ts` as single source of truth (grep)
  </acceptance_criteria>
  <done>
SCHEMA.md authoritative documentation in place; Phase 4 importer contract published.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create data/scraped/README.md</name>
  <files>data/scraped/README.md (NEW)</files>
  <read_first>
    - /Users/mikhailra/Developer/dva.pro/.planning/phases/01-inventory-scrapers-drom-and-stubs/01-CONTEXT.md D-06 (output dir layout), D-08 (re-run policy), D-15 (cursor)
    - /Users/mikhailra/Developer/dva.pro/.planning/phases/01-inventory-scrapers-drom-and-stubs/01-RESEARCH.md Pitfall 7 (lines 853-858)
    - /Users/mikhailra/Developer/dva.pro/.planning/phases/01-inventory-scrapers-drom-and-stubs/01-RESEARCH.md Open Question 3 (lines 1113-1116) — manual prune command
    - /Users/mikhailra/Developer/dva.pro/.planning/phases/01-inventory-scrapers-drom-and-stubs/01-RESEARCH.md line 871 — "Phase 1 requires zero secrets"
    - /Users/mikhailra/Developer/dva.pro/.planning/phases/01-inventory-scrapers-drom-and-stubs/01-RESEARCH.md line 156, 792 — OS support note
    - /Users/mikhailra/Developer/dva.pro/.planning/phases/01-inventory-scrapers-drom-and-stubs/01-PATTERNS.md §README.md (lines 1070-1077)
  </read_first>
  <action>
Create `data/scraped/README.md`:

```markdown
# data/scraped/ — Runbook

This directory holds output of Phase 1 inventory scrapers. Phase 1 ships **one real scraper (drom.ru/catalog)** plus 4 stubs (encar / beforward / che168 / autohome). Live implementations of the 4 stubs are deferred to v1.x.

For schema documentation see `SCHEMA.md` in this directory. For the project-level workflow rules see `/CLAUDE.md` and `.planning/`.

---

## Quick start

```bash
# One-shot drom backfill (1–2 weeks of polite scraping; runs in foreground)
pnpm scrape:drom

# Each stub source — exits 2 (not_implemented)
pnpm scrape:encar
pnpm scrape:beforward
pnpm scrape:che168
pnpm scrape:autohome

# Generic dispatcher
pnpm scrape <drom|encar|beforward|che168|autohome>
```

### Exit codes (D-09)

| Code | Meaning | When |
|------|---------|------|
| `0` | `ok` | Run completed successfully; `current/` updated; `.cursor.json` deleted |
| `1` | `error` | Unhandled error or DOM regression > 10% (Pitfall 1) |
| `2` | `not_implemented` | Stub source — drom is the only real scraper in Phase 1 |
| `3` | `blocked` | 5 thin responses or captcha keyword detected (D-13); run halted, cursor preserved for resume |

---

## Output directory layout

```
data/scraped/
├── SCHEMA.md                                   # ← committed
├── README.md                                   # ← this file, committed
├── drom/
│   ├── brand-aliases.json                      # ← committed (small Cyrillic↔Latin seed)
│   ├── current/                                # ← symlink to most recent successful run (gitignored)
│   └── 2026-04-28T07-30-00Z/                   # ← per-run dir (gitignored)
│       ├── models.json                         # ← drom master-model records
│       ├── images/                             # ← hero WebP files
│       │   ├── bmw-x5-g_2018_8395-hero.webp
│       │   └── ...
│       ├── report.json                         # ← run telemetry (D-17)
│       └── .cursor.json                        # ← present only if run unfinished
└── fx/
    └── cbr-2026-04-28.json                     # ← per-UTC-day FX cache (gitignored)
```

`run_id` format (D-07): ISO-8601 UTC with `:` and `.` replaced by `-`, e.g. `2026-04-28T07-30-00Z`. Sortable, filesystem-safe, unambiguous.

### `current/` symlink (D-08)

After a successful run, `data/scraped/drom/current/` atomically points at the new run dir. Phase 4's importer reads from `current/` and never embeds knowledge of the timestamped dir.

**Phase 4 contract (Pitfall 7):** the importer MUST always re-resolve `current/` per invocation. Never cache `realpath()` across runs — the symlink target rotates with each successful scrape.

---

## Running drom: what happens

1. **FX feed** — fetches CBR daily XML, decodes windows-1251, caches as `fx/cbr-<YYYY-MM-DD>.json`. First run with no cache MUST succeed; subsequent runs fall back to most recent cached file (D-12).
2. **Brand index** — fetches `https://www.drom.ru/catalog/`, parses ~70 brand anchors.
3. **Model list per brand** — for each brand, fetches `/catalog/<brand>/`, parses ~5–30 model anchors.
4. **Generation list per model** — for each model, fetches `/catalog/<brand>/<model>/`, parses ~1–10 generation anchors.
5. **Generation page** — for each generation, fetches `/catalog/<brand>/<model>/g_<YYYY>_<id>/`, extracts a full `ModelRecord` (zod-validated).
6. **Hero image** — for each record, downloads source JPEG, transcodes to WebP via sharp (quality 80, original dims), atomically writes under `<run_dir>/images/`.
7. **Brand-boundary checkpoint** — after every model, writes `.cursor.json` with `{lastBrandSlug, lastModelSlug, completedAt}`.
8. **End-of-brand alias merge** — appends Cyrillic↔Latin lookup into `data/scraped/drom/brand-aliases.json` (idempotent merge by `brand_slug`).
9. **Run end** — writes `models.json` + `report.json` atomically, updates `current/` symlink, deletes `.cursor.json`.

### Polite rate limit (D-14)

- 1 HTTP request per 10 seconds, ±20 % jitter.
- Honors `Crawl-delay` from drom's `robots.txt` if larger.
- `p-limit(1)` for HTTP fetches; `p-limit(4)` for sharp WebP encoding.
- Total expected runtime for full backfill: **1–2 weeks**.

### Crash recovery (D-15)

If the run dies mid-brand, `pnpm scrape:drom` re-launched will:
1. Read `.cursor.json` (if present) and resume from the next model after `lastModelSlug` in brand `lastBrandSlug`.
2. Worst case: re-fetch ≤ 1 brand's worth of pages (~7 hours wasted at 10 s/req × ~30 pages/model × ~50 models). Brand-boundary granularity is a deliberate trade-off; finer cursors are a Phase 1.x candidate.
3. On clean completion, `.cursor.json` is deleted.

---

## Pruning old runs

`run_id` directories are append-only — Phase 1 NEVER deletes a previous run. To prune manually after a successful new run:

```bash
# Delete drom run dirs older than 30 days (keeps current/ symlink intact)
find data/scraped/drom -maxdepth 1 -name "20*Z" -mtime +30 -exec rm -rf {} +

# Delete FX cache files older than 30 days
find data/scraped/fx -maxdepth 1 -name "cbr-*.json" -mtime +30 -delete
```

A `pnpm scrape:prune` automation is reserved for Phase 1.x.

---

## OS support

- **macOS** (Apple Silicon + Intel) — primary dev target.
- **Linux** (x86_64 + arm64) — CI + production.
- **Windows** — not supported in v1. The `current/` symlink uses POSIX `rename`-over-symlink atomicity which Windows does not implement (Junction Points required). If you need to run on Windows, use Docker: `docker run --rm -v $(pwd):/work -w /work node:22-alpine pnpm scrape:drom`.

---

## Secrets

Phase 1 requires **NO environment variables, NO API keys, NO `.env` file**. drom is public; CBR is public. If `pnpm scrape:drom` ever prompts for a secret, something has gone wrong — file a bug.

(Phase 3 introduces the `.env` for Yandex Cloud + Unisender Go; that's a different scope.)

---

## When things go wrong

### Run exits with code 3 (blocked)

Drom returned 5 consecutive thin responses or a captcha keyword (per D-13). Do NOT immediately retry — wait at least 24 hours, then resume from `.cursor.json`. If repeated blocks happen, run from a residential IP (founder home connection) rather than a cloud VM.

### Run exits with code 1 (error) and `report.json` shows > 10 % validation drop-out

Drom DOM regression. Re-sanitize the 4 fixtures under `server/tests/fixtures/drom/` from current live drom HTML, fix the affected parsers, re-run unit + integration tests, then retry. The fixture sanitation procedure lives in plan `01-07`.

### `pnpm install --frozen-lockfile` fails with sharp native binary

Try `pnpm install --config.shamefully-hoist=true`. If still failing, drop to Docker: `docker run --rm -v $(pwd):/work -w /work node:22-alpine sh -c "corepack enable && pnpm install"`.

---

## Stub sources (deferred to v1.x)

Each stub returns `{status: 'not_implemented'}` and writes nothing. The TODO log line names the planned implementation strategy:

| Source | Strategy (v1.x) |
|--------|-----------------|
| `encar` (KR) | Crawlee + Playwright Firefox + KR residential proxy + Carapis fallback |
| `beforward` (JP) | HttpCrawler + Cheerio (mostly static) |
| `che168` (CN) | PlaywrightCrawler + CN residential proxy |
| `autohome` (CN) | PlaywrightCrawler + CN residential proxy |

The `IScraper` contract is locked: v1.x authors fill the body of `run()` and the existing CLI dispatcher consumes them without changes.

---

## See also

- `SCHEMA.md` — record contract (this directory)
- `server/scrapers/shared/types.ts` — TypeScript / zod single source of truth
- `.planning/phases/01-inventory-scrapers-drom-and-stubs/` — Phase 1 design docs
- `CLAUDE.md` — project-level constraints (Node 22, pnpm, RU-only locale, etc.)
```
  </action>
  <verify>
    <automated>test -f data/scraped/README.md &amp;&amp; grep -q "pnpm scrape:drom" data/scraped/README.md &amp;&amp; grep -q "Exit codes" data/scraped/README.md &amp;&amp; grep -q "current/" data/scraped/README.md &amp;&amp; grep -q "Phase 4" data/scraped/README.md &amp;&amp; grep -q "Windows" data/scraped/README.md &amp;&amp; grep -q "NO environment variables" data/scraped/README.md &amp;&amp; grep -q "find data/scraped/drom" data/scraped/README.md &amp;&amp; ! git check-ignore -q data/scraped/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `data/scraped/README.md` exists and is committed (`git check-ignore -q` returns nonzero)
    - File documents `pnpm scrape:drom` invocation (grep)
    - File documents all 4 exit codes (0/1/2/3) with their meanings (grep)
    - File documents `current/` symlink contract + Phase 4 "always re-resolve" rule (grep "Pitfall 7" OR "re-resolve")
    - File documents the manual prune command (grep `find data/scraped/drom`)
    - File documents Windows is not supported (grep "Windows" + "not supported" OR "Docker")
    - File explicitly states "NO environment variables" / "NO `.env` file" (grep)
    - File documents the 4 stub sources and their v1.x implementation strategy (grep "encar", "beforward", "che168", "autohome")
    - File references `SCHEMA.md` and `server/scrapers/shared/types.ts` as source of truth (grep each)
  </acceptance_criteria>
  <done>
README.md runbook in place; Phase 1 → Phase 4 handoff and operator workflow documented.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → human reader | Misleading docs cause operator error or Phase 4 importer bugs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Repudiation | Schema doc drifts from TS source of truth | mitigate | SCHEMA.md explicitly names `server/scrapers/shared/types.ts` as the source of truth and instructs editors to bump the TS first. Pitfall 6 is the policy. |
| T-08-02 | Tampering | Operator follows stale prune command and deletes a run that's still being read by Phase 4 | mitigate | Prune command uses `-mtime +30` (only files older than 30 days). `current/` symlink is intact (the find pattern excludes the symlink itself — `-name "20*Z"` matches the dir name pattern, not `current`). |
| T-08-03 | Information Disclosure | README accidentally reveals secrets | accept | README explicitly says Phase 1 has no secrets. No risk surface. |
</threat_model>

<verification>
Plan-level checks:

1. Both files committed (verify with `git check-ignore -q` returning nonzero on each).
2. SCHEMA.md mirrors all 17 ModelRecord fields and documents the Phase 4 upsert key.
3. README.md documents the runbook + the 4 stub sources + OS support + secrets policy.
4. README references SCHEMA.md and `server/scrapers/shared/types.ts`.
</verification>

<success_criteria>
Plan 08 is complete when:
- `data/scraped/SCHEMA.md` is the authoritative human-readable mirror of `server/scrapers/shared/types.ts`, with the BMW X5 G05 worked example and the Phase 4 upsert contract.
- `data/scraped/README.md` is the runbook: invocation, exit codes, output layout, crash recovery, prune command, OS support, secrets policy, and the 4 stub sources.
- Both files are committed (gitignore negation rules from plan 01 verified by `git check-ignore`).
- The Phase 4 author can read these two files and write the importer without re-asking Phase 1 questions.
</success_criteria>

<output>
After completion, create `.planning/phases/01-inventory-scrapers-drom-and-stubs/01-08-SUMMARY.md` covering:
- Confirmation that both files are committed (not gitignored).
- Note any divergences from the verbatim SCHEMA.md content above (e.g. additional fields if plan 07's parser added any).
- Phase 4 handoff readiness: the 17 ModelRecord fields + upsert key + image rehost contract are all explicit.
</output>
