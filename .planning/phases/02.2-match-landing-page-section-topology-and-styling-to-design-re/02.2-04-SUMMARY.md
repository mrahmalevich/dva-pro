---
phase: "02.2"
plan: "04"
subsystem: "frontend/pages"
tags: ["section-topology", "site-tsx", "integration", "wave-2"]
dependency_graph:
  requires: ["02.2-01", "02.2-02", "02.2-03"]
  provides: ["home-route-section-order"]
  affects: ["src/pages/Site.tsx"]
tech_stack:
  added: []
  patterns: ["section-composition", "static-import-ordering"]
key_files:
  created: []
  modified:
    - src/pages/Site.tsx
decisions:
  - "D-01: New section order Hero → Marquee → LeadMagnet → Catalog → Process → InTransit → Reviews → CtaShipFromAnywhere → Faq → Footer + FloatingDock"
  - "D-03: LeadMagnet moved to quiz-teaser slot between Marquee and Catalog"
  - "D-04: Founders removed from home route (file untouched for /about reuse)"
  - "FeedStrip removed from home route; InTransit takes the 'cars in transit' slot"
  - "Preserved existing useState/QuizModal/Nav modal contract — useQuizModal hook does not exist in this codebase"
metrics:
  duration: "~3 minutes"
  completed: "2026-05-07"
  tasks_completed: 3
  files_changed: 1
---

# Phase 02.2 Plan 04: Site.tsx Section Reorder Summary

Rewired `src/pages/Site.tsx` to match the design-reference topology: added Wave 1 components (InTransit, CtaShipFromAnywhere), removed FeedStrip and Founders from the home route, and moved LeadMagnet up to the quiz-teaser slot between Marquee and Catalog.

## Tasks Completed

| Task | Description | Commit |
|------|-------------|--------|
| 1 | Read Site.tsx and verify hook/modal/footer export names | (read-only, no commit) |
| 2 | Replace Site.tsx with new section order | 4ae4516 |
| 3 | TypeScript check + test run | (verification, no commit) |

## Decisions Made

- **Preserved existing modal contract:** The plan's interface showed `useQuizModal()` but the current codebase uses `useState` + `QuizModal` directly. Per plan instruction to preserve the hook/modal/footer behavior, the existing `useState`/`QuizModal`/`Nav` pattern was kept unchanged. Only section imports and JSX order were modified.
- **`InTransit` takes no props:** Confirmed `InTransit` does not accept `onOpenQuiz` — rendered as `<InTransit />` with no props.
- **`CtaShipFromAnywhere` takes `onOpenQuiz`:** Confirmed interface matches; rendered as `<CtaShipFromAnywhere onOpenQuiz={open} />`.

## Deviations from Plan

**1. [Rule 1 - Adaptation] Preserved useState modal pattern instead of useQuizModal**
- **Found during:** Task 1 (reading Site.tsx)
- **Issue:** Plan's interface used `useQuizModal()` hook which does not exist in this codebase. Current implementation uses `useState(false)` + `<QuizModal open={quizOpen} onClose={close} />` directly.
- **Fix:** Kept existing modal pattern verbatim. Plan explicitly instructed: "Do NOT change the modal/hook contract — just the section ordering and the two new imports."
- **Files modified:** src/pages/Site.tsx (no additional change — this was the correct behavior)
- **Commit:** 4ae4516

## Test Results

```
Test Files  20 passed (20)
Tests  208 passed | 3 skipped (211)
Duration  20.22s
```

Golden test: `SPA matches design-reference.png within 22% structural-drift threshold` — PASSED. The section reorder did not push the golden test over threshold. Plan 02.2-05 will re-tune if needed, but no regression occurred.

## Known Stubs

None. Site.tsx is a composition file — all data/content responsibility lies in the individual section components.

## Threat Flags

None. No new network endpoints, auth paths, file access patterns, or schema changes introduced. This is a pure frontend composition change.

## Self-Check: PASSED

- [x] `src/pages/Site.tsx` exists and has new section order
- [x] Commit `4ae4516` exists in git log
- [x] TypeScript clean (no output from `pnpm tsc -b --noEmit`)
- [x] All 208 tests pass
- [x] `<FeedStrip />` and `<Founders />` not present in new file
- [x] `<InTransit />` and `<CtaShipFromAnywhere onOpenQuiz={open} />` present in new file
- [x] Only `src/pages/Site.tsx` staged in task commit
