---
phase: 02.2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sections/Process.tsx
autonomous: true
requirements: []
decisions_addressed:
  - D-02
  - D-02a
  - D-02b
must_haves:
  truths:
    - "D-02: Process restructured from 6 vertical steps to 4 horizontal steps with clean numbered badges. 4-column grid on desktop (≥980 px); mobile (≤720 px) collapses to vertical stacking — must NOT regress."
    - "D-02a: 4 step labels are: 01 Заявка, 02 Подбор, 03 Покупка, 04 Доставка. Each gets a 1-line description (use existing timeline.sub strings as semantic source — read .slice(0, 4) defensively in case fixture not yet reduced)."
    - "D-02b: drop the sticky right-rail preview card. The new component renders only the 4-step horizontal row + heading."
    - "Numbered marker '03 / Как мы работаем' (the section's num-marker, NOT the step badges) preserved."
    - "`useCrm()` + `state.timeline.slice(0, 4)` — defensive slice means this plan is independent of plan 02.2-01 (it works whether timeline has 6 or 4 entries)."
    - "Existing atoms reused: `Reveal`, `Icon`. Drop unused imports (CarPlaceholder)."
  artifacts:
    - path: src/sections/Process.tsx
      provides: "4-horizontal-step Process section component"
      contains: "gridTemplateColumns: 'repeat(4, 1fr)'"
  key_links:
    - from: "src/sections/Process.tsx"
      to: "src/crm/CrmProvider.tsx"
      via: "useCrm() → state.timeline"
      pattern: "state.timeline"
---

<objective>
Restructure `src/sections/Process.tsx` from a 6-vertical-step layout (with sticky right-rail preview card) to a 4-horizontal-step layout matching the design reference. Each step is a numbered badge + label + 1-line description. The connecting hairline (top-border on each cell) provides visual rhythm.

Defensive: read `state.timeline.slice(0, 4)` so this plan is independent of plan 02.2-01 (works whether timeline has 6 or 4 entries). The displayed step *labels* are hardcoded fallbacks (`01 Заявка`, `02 Подбор`, `03 Покупка`, `04 Доставка`) when timeline doesn't provide good labels — implementation reads `state.timeline[i].title` if non-empty, else falls back to a hardcoded label.

Mobile breakpoint (≤720 px): grid collapses to single column. No CSS changes to global.css — handled inline via `@media`-equivalent at the section level using a `gridTemplateColumns` style that uses `minmax(...)` and `auto-fit` OR a simple desktop-only inline rule with mobile fallback in parent layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02.2-match-landing-page-section-topology-and-styling-to-design-re/02.2-CONTEXT.md
@src/sections/Process.tsx
@src/components/atoms.tsx

<interfaces>

### New Process.tsx (replace entire file body)

```tsx
import { Icon, Reveal } from '../components/atoms';
import { useCrm } from '../crm/CrmProvider';

const FALLBACK_STEPS = [
  { code: '01', title: 'Заявка',  sub: 'Короткий квиз — мы понимаем бюджет, тип кузова и сроки.' },
  { code: '02', title: 'Подбор',  sub: 'Денис собирает 6–8 машин с зарубежных аукционов под ваш профиль.' },
  { code: '03', title: 'Покупка', sub: 'Мы выкупаем выбранное авто — договор, инспекция, оплата.' },
  { code: '04', title: 'Доставка',sub: 'Логистика, таможня, постановка на учёт. Машина у вашего подъезда.' },
] as const;

export const Process = ({ onOpenQuiz }: { onOpenQuiz: () => void }) => {
  const { state } = useCrm();
  const sourceSteps = state.timeline.slice(0, 4);
  const steps = FALLBACK_STEPS.map((fb, i) => {
    const src = sourceSteps[i];
    return {
      code: fb.code,
      title: src?.title?.trim() || fb.title,
      sub:   src?.sub?.trim()   || fb.sub,
    };
  });

  return (
    <section id="process" style={{ background: 'var(--ink)', color: '#fff' }}>
      <div className="container">
        <Reveal>
          <div style={{ marginBottom: 32 }}>
            <div className="num-marker" style={{ marginBottom: 14 }}>03 / Как мы работаем</div>
            <h2 className="h1" style={{ maxWidth: 1000 }}>
              Четыре шага от&nbsp;заявки<br />до&nbsp;ключей. <span className="outline-italic">Без сюрпризов.</span>
            </h2>
          </div>
        </Reveal>

        <div
          className="process-grid"
          style={{
            display: 'grid',
            gridTemplateColumns: 'repeat(4, 1fr)',
            gap: 0,
            borderTop: '1px solid var(--line)',
          }}
        >
          {steps.map((s, i) => (
            <Reveal key={s.code} delay={i * 80}>
              <div style={{
                padding: '32px 28px 28px',
                borderRight: i < steps.length - 1 ? '1px solid var(--line)' : 'none',
                borderBottom: '1px solid var(--line)',
                height: '100%',
                display: 'flex',
                flexDirection: 'column',
                gap: 14,
              }}>
                <div className="mono" style={{ fontSize: 13, color: 'var(--coral)', fontWeight: 700, letterSpacing: '0.12em' }}>
                  {s.code}
                </div>
                <h3 className="h3" style={{ fontSize: 26, marginBottom: 0 }}>{s.title}</h3>
                <p style={{ fontSize: 14, color: 'var(--mute)', lineHeight: 1.55, margin: 0 }}>{s.sub}</p>
              </div>
            </Reveal>
          ))}
        </div>

        <div style={{ marginTop: 32, display: 'flex', justifyContent: 'flex-start' }}>
          <button className="btn btn-primary" onClick={onOpenQuiz}>
            Начать с шага 01 <Icon name="arrow-right" size={16} />
          </button>
        </div>
      </div>

      {/* Mobile: stack to single column */}
      <style>{`
        @media (max-width: 720px) {
          #process .process-grid { grid-template-columns: 1fr !important; }
          #process .process-grid > * > div { border-right: none !important; }
        }
      `}</style>
    </section>
  );
};
```

</interfaces>
</context>

<tasks>

### Task 1 — Replace Process.tsx body

1. Read current `src/sections/Process.tsx` to confirm imports/atom names.
2. Replace the entire file body with the new component (see interfaces).
3. Verify only `Icon` and `Reveal` are imported from `../components/atoms` (drop `CarPlaceholder` and `useState`).

Commit: `refactor(02.2-02): restructure Process from 6 vertical steps to 4 horizontal steps`

### Task 2 — Self-check

1. Run `pnpm tsc -b --noEmit 2>&1 | head -20` — TypeScript clean.
2. Run `pnpm test 2>&1 | tail -20` — currently-green tests should remain green; the golden test may regress (acceptable; re-tuned in plan 02.2-05).
3. Confirm `git diff --name-only` shows only `src/sections/Process.tsx`.

</tasks>

<acceptance>
- `src/sections/Process.tsx` renders 4 horizontal steps in `repeat(4, 1fr)` grid
- Hardcoded fallback labels: 01 Заявка, 02 Подбор, 03 Покупка, 04 Доставка
- Reads `state.timeline.slice(0, 4)` defensively (works whether timeline has 6 or 4 entries)
- No sticky right-rail preview card
- Mobile @media (max-width: 720px) collapses to single column via inline `<style>` block
- TypeScript clean
- Exactly 1 file touched
</acceptance>
