---
phase: 02.2-match-landing-page-section-topology-and-styling-to-design-re
plan: "02"
subsystem: ui
tags: [react, layout, grid, process-section, responsive]

# Dependency graph
requires:
  - phase: 02.1-tighten-visual-density-to-design-reference-fidelity
    provides: canonical padding scale (72px 0), CSS variables, global.css mobile breakpoints
provides:
  - 4-horizontal-step Process section with repeat(4, 1fr) grid, numbered badges, mobile collapse
affects:
  - 02.2-05 (golden test re-baseline will see new Process layout)
  - 02.2-01 (timeline slice defensiveness — this plan works with 6 or 4 entries)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Inline <style> block for section-scoped media queries (no global.css changes)"
    - "Defensive state.timeline.slice(0, 4) with hardcoded FALLBACK_STEPS constant"

key-files:
  created: []
  modified:
    - src/sections/Process.tsx

key-decisions:
  - "D-02: Process restructured from 6 vertical to 4 horizontal steps matching design reference"
  - "D-02a: Step labels 01 Заявка / 02 Подбор / 03 Покупка / 04 Доставка with 1-line descriptions"
  - "D-02b: Sticky right-rail preview card dropped — not in design reference"

patterns-established:
  - "Section-scoped mobile overrides via inline <style>{`@media...`}</style> avoids global.css edits"

requirements-completed: []

# Metrics
duration: 5min
completed: 2026-05-07
---

# Phase 02.2 Plan 02: Process Section 4-Horizontal-Step Restructure Summary

**Process.tsx restructured from 6-vertical-step/sticky-right-rail layout to a 4-column horizontal grid with numbered badges (01-04), connecting hairlines, and inline mobile collapse to single column**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-05-07T22:51:00Z
- **Completed:** 2026-05-07T22:52:30Z
- **Tasks:** 2 (Replace component + self-check)
- **Files modified:** 1

## Accomplishments
- Replaced 6-step vertical accordion + sticky preview card with 4-column `repeat(4, 1fr)` horizontal grid
- Hardcoded FALLBACK_STEPS constant with Russian copy; reads `state.timeline.slice(0, 4)` defensively
- Dropped `useState`, `CarPlaceholder` imports — only `Icon` and `Reveal` remain
- Mobile breakpoint `@media (max-width: 720px)` collapses grid to 1 column via inline `<style>` block
- All 208 tests pass including golden test (threshold 0.28 unchanged — golden re-baseline deferred to plan 02.2-05)

## Task Commits

1. **Task 1: Replace Process.tsx body** - `c4f9b03` (refactor)

## Files Created/Modified
- `src/sections/Process.tsx` - Replaced with 4-horizontal-step component; 59 insertions, 66 deletions

## Decisions Made
- Used inline `<style>` block for mobile media query to avoid touching `global.css` (per D-09 constraint)
- FALLBACK_STEPS typed `as const` for type safety
- `src?.title?.trim() || fb.title` merge pattern: live CRM data preferred, falls back to hardcoded copy

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Process section ready for visual verification
- Golden test may drift slightly from current 0.22 measurement — plan 02.2-05 will re-baseline threshold after all section topology changes land

## Self-Check: PASSED

- `src/sections/Process.tsx` exists and was modified
- Commit `c4f9b03` exists in git log
- `git diff --name-only` showed only `src/sections/Process.tsx`
- TypeScript clean (`pnpm tsc -b --noEmit` — no errors)
- All 208 tests pass (20 test files)

---
*Phase: 02.2-match-landing-page-section-topology-and-styling-to-design-re*
*Completed: 2026-05-07*
