---
phase: 02.2
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sections/CtaShipFromAnywhere.tsx
autonomous: true
requirements: []
decisions_addressed:
  - D-06
must_haves:
  truths:
    - "D-06: new section `src/sections/CtaShipFromAnywhere.tsx`. Dark background, large outline-italic headline (Привезём вам автомобиль из любого порта мира), short body line, single primary button (Открыть квиз) wired to onOpenQuiz."
    - "Visual style mirrors Hero hero-block: gradient radial background, coral accent. Acts as a visual bookend before Faq/Footer."
    - "Reuse existing atoms: `Icon`, `Reveal`. NO new atoms. NO new dependencies."
    - "Section padding follows the canonical 72px 0 set in Phase 02.1 — but this section can override with extra vertical breathing if needed via inline style (kept ~96px 0 here)."
  artifacts:
    - path: src/sections/CtaShipFromAnywhere.tsx
      provides: "Final CTA section component with onOpenQuiz prop"
      contains: "export const CtaShipFromAnywhere"
  key_links:
    - from: "src/sections/CtaShipFromAnywhere.tsx"
      to: "src/pages/Site.tsx"
      via: "<CtaShipFromAnywhere onOpenQuiz={open} />"
      pattern: "CtaShipFromAnywhere"
---

<objective>
Add a new final CTA section before Faq/Footer. The section visually echoes the Hero block (coral glow blob, gradient overlay, outline-italic emphasis on the headline) so the page reads as a bookend. Single primary action: open the quiz.

Component is self-contained (no fixture/data dependency) — safe to develop in parallel with other Wave 1 plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02.2-match-landing-page-section-topology-and-styling-to-design-re/02.2-CONTEXT.md
@src/sections/Hero.tsx
@src/components/atoms.tsx

<interfaces>

### New CtaShipFromAnywhere.tsx

```tsx
import { Icon, Reveal } from '../components/atoms';

export const CtaShipFromAnywhere = ({ onOpenQuiz }: { onOpenQuiz: () => void }) => (
  <section
    id="cta-ship-from-anywhere"
    className="grain"
    style={{
      background: 'var(--ink)',
      color: '#fff',
      position: 'relative',
      overflow: 'hidden',
      padding: '96px 0',
    }}
  >
    {/* Coral glow — mirrors Hero G-01 */}
    <div
      aria-hidden
      style={{
        position: 'absolute',
        top: -120,
        right: -160,
        width: 720,
        height: 720,
        borderRadius: '50%',
        background: 'radial-gradient(circle, rgba(213,121,89,0.45), transparent 60%)',
        filter: 'blur(90px)',
        pointerEvents: 'none',
      }}
    />
    {/* Faint gradient overlay to anchor text */}
    <div
      aria-hidden
      style={{
        position: 'absolute',
        inset: 0,
        background: 'linear-gradient(180deg, rgba(0,0,0,0.0) 0%, rgba(0,0,0,0.35) 100%)',
        pointerEvents: 'none',
      }}
    />

    <div className="container" style={{ position: 'relative', zIndex: 2, maxWidth: 1080 }}>
      <Reveal>
        <h2 className="h1" style={{ marginBottom: 24, lineHeight: 1.05 }}>
          Привезём вам автомобиль<br />
          из&nbsp;<span className="outline-italic">любого&nbsp;порта</span> мира.
        </h2>
      </Reveal>
      <Reveal delay={120}>
        <p className="body-lg" style={{ color: 'var(--mute)', marginBottom: 36, maxWidth: 720 }}>
          Корея, Япония, Китай, ОАЭ, США, Европа — 19&nbsp;лет на&nbsp;рынке, 1840+ автомобилей у&nbsp;клиентов. Расскажите, что ищете — соберём подборку с&nbsp;ценами «под&nbsp;ключ».
        </p>
      </Reveal>
      <Reveal delay={200}>
        <button className="btn btn-primary" onClick={onOpenQuiz} style={{ fontSize: 16 }}>
          Открыть квиз — 5 минут <Icon name="arrow-right" size={16} />
        </button>
      </Reveal>
    </div>
  </section>
);
```

If atom names (`Icon`, `Reveal`) or class names (`btn btn-primary`, `outline-italic`, `grain`, `body-lg`, `h1`, `container`) don't match what's in this codebase, read `src/components/atoms.tsx` and `src/styles/global.css` first and adjust — do NOT introduce new utilities.

</interfaces>
</context>

<tasks>

### Task 1 — Verify utility names exist in this codebase

1. Read `src/sections/Hero.tsx` to confirm class names: `grain`, `outline-italic`, `body-lg`, `h1`, `container`, `btn`, `btn-primary` are all in use.
2. Read `src/components/atoms.tsx` to confirm `Icon` and `Reveal` exports.

### Task 2 — Create the section file

1. Create `src/sections/CtaShipFromAnywhere.tsx` with the component pattern shown in interfaces.
2. Run `pnpm tsc -b --noEmit 2>&1 | head -20` — must be clean.
3. Confirm `git diff --name-only` shows only `src/sections/CtaShipFromAnywhere.tsx` (a NEW file).

Commit: `feat(02.2-03): add CtaShipFromAnywhere section (final hero-style CTA before Faq)`

### Task 3 — Self-check

1. `pnpm test 2>&1 | tail -20` — green count unchanged (this section is not yet rendered on home; that happens in plan 02.2-04).

</tasks>

<acceptance>
- `src/sections/CtaShipFromAnywhere.tsx` exports `CtaShipFromAnywhere({ onOpenQuiz })`
- Uses only existing atoms (`Icon`, `Reveal`) and existing classes
- No new dependencies
- TypeScript clean
- Exactly 1 NEW file
</acceptance>
