---
phase: 01.1-extend-drom-scrape-fields
plan: "01"
subsystem: scrapers/drom
tags: [scrapers, drom, devdeps, fixtures, wave-0]
status: partial  # Tasks 1-2 complete; Task 3 awaiting operator (human-verify checkpoint)

dependency-graph:
  requires: []
  provides:
    - puppeteer@24.42.0 in devDependencies (Wave 4 screenshot test)
    - pixelmatch@7.2.0 in devDependencies (Wave 4 screenshot diff)
    - pngjs@7.0.0 in devDependencies (Wave 4 PNG read/write)
    - server/scrapers/cli.ts --capture-fixture branch (Wave 0 fixture capture)
  affects:
    - All Wave 1+ plans in Phase 01.1 (depend on fixtures created in Task 3)
    - Wave 4 screenshot test plan (depends on puppeteer)

tech-stack:
  added:
    - puppeteer@24.42.0 (devDep) — headless Chromium for Wave 4 screenshot golden
    - pixelmatch@7.2.0 (devDep) — pixel-diff for screenshot golden comparison
    - pngjs@7.0.0 (devDep) — PNG read/write for screenshot golden
    - "@types/pixelmatch@5.2.6" (devDep) — TypeScript types
    - "@types/pngjs@6.0.5" (devDep) — TypeScript types
  patterns:
    - pnpm.onlyBuiltDependencies extended with puppeteer to allow postinstall Chromium download
    - Dynamic import() in cli.ts for lazy-load discipline (cold-path code stays out of startup)

key-files:
  created: []
  modified:
    - path: package.json
      what: Added 5 devDependencies + puppeteer in pnpm.onlyBuiltDependencies
    - path: pnpm-lock.yaml
      what: Lockfile updated with resolved versions for all 5 new devDeps
    - path: server/scrapers/cli.ts
      what: Added --capture-fixture <comp_id> branch before run dispatch

decisions:
  - puppeteer added to pnpm.onlyBuiltDependencies so postinstall Chromium download runs (pnpm v10 blocks scripts by default)
  - Dynamic import() used for fetchHtml/fs/path inside capture branch (lazy-load pattern per PATTERNS.md)
  - URL hardcoded to bmw/x5 per SPEC R-4 (BMW_PILOT_BRANDS hard-coded; multi-model is v1.x)
  - /^\d+$/ regex guards compId against path traversal and URL injection (T-01.1-01)

metrics:
  duration: "partial (Tasks 1-2 completed in ~5 min; Task 3 pending operator)"
  completed_date: "2026-04-29 (Tasks 1-2)"
  tasks_completed: 2
  tasks_total: 3
  files_changed: 3
---

# Phase 01.1 Plan 01: Wave 0 Setup (devDeps + Fixture CLI) Summary

## One-liner

Installed puppeteer@24.42.0 + pixelmatch + pngjs as devDeps (Chromium downloaded) and extended cli.ts with a regex-validated `--capture-fixture <comp_id>` branch that fetches BMW X5 per-comp pages through the existing fetchHtml() path.

## Status: Partial — Awaiting Task 3 Checkpoint

Tasks 1 and 2 are committed. Task 3 (operator live-capture of 5 BMW per-comp fixtures) is a `checkpoint:human-verify` gate requiring operator action.

---

## Task 1: Install devDependencies

### Resolved Versions (from pnpm-lock.yaml)

| Package | Pinned (package.json) | Resolved |
|---------|----------------------|---------|
| puppeteer | ^24.42.0 | 24.42.0 |
| pixelmatch | ^7.2.0 | 7.2.0 |
| pngjs | ^7.0.0 | 7.0.0 |
| @types/pixelmatch | latest | 5.2.6 |
| @types/pngjs | latest | 6.0.5 |

**Chromium download:**
- Location: `~/.cache/puppeteer/chrome/mac_arm-147.0.7727.57`
- Version: Chrome 147.0.7727.57 (Chromium)
- Also downloaded: `chrome-headless-shell` at the same version

**pnpm onlyBuiltDependencies:** Added `puppeteer` to allow postinstall to run (pnpm v10 blocks all build scripts by default unless explicitly permitted).

**Verification:** `pnpm test` 108/108 green; `pnpm typecheck:server` clean.

**Commit:** 7cba0aa

---

## Task 2: Extend cli.ts with --capture-fixture

**Added branch** intercepts `sourceArg === 'drom' && process.argv[3] === '--capture-fixture'` BEFORE the run dispatch at line 25 (after the `!SCRAPERS[sourceArg]` guard at line 20).

**Security:** `/^\d+$/` regex on `process.argv[4]` (compId) — mitigates T-01.1-01 (path traversal, URL injection).

**URL template:** `https://www.drom.ru/catalog/bmw/x5/${compId}/` — hardcoded per SPEC R-4.

**Dynamic imports used (3):**
1. `await import('./shared/http.js')` — fetchHtml
2. `await import('node:fs/promises')` — writeFile, mkdir
3. `await import('node:path')` — resolve

**Writes to:** `server/tests/fixtures/drom/complectation/<compId>.html`

**Verification:** `pnpm test` 108/108 green; `pnpm typecheck:server` clean.

**Commit:** 13daf6f

---

## Task 3: Operator Fixture Capture (PENDING)

**Status:** AWAITING OPERATOR — see checkpoint below.

The operator must run the following commands to capture 3 live fixtures and create 2 hand-edited broken fixtures:

```bash
pnpm scrape drom --capture-fixture 207354   # BMW xDrive 30d AT Base
pnpm scrape drom --capture-fixture 252766   # BMW xDrive 30d AT M Sport Plus
pnpm scrape drom --capture-fixture 265067   # BMW M Competition (top trim)
```

Then create two broken fixtures from 207354.html per the plan instructions.

**Expected outcome:** 5 files under `server/tests/fixtures/drom/complectation/`.

---

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added puppeteer to pnpm.onlyBuiltDependencies**
- **Found during:** Task 1
- **Issue:** pnpm v10 blocks build scripts by default unless the package is listed in `pnpm.onlyBuiltDependencies`. Running `pnpm add -D puppeteer@^24.42.0` succeeded but printed `Ignored build scripts: puppeteer@24.42.0` — Chromium was NOT downloaded.
- **Fix:** Added `"puppeteer"` to the `pnpm.onlyBuiltDependencies` array in `package.json`, then re-ran `pnpm install` to trigger the postinstall.
- **Files modified:** package.json
- **Commit:** 7cba0aa (included in the same Task 1 commit)

---

## Known Stubs

None in files created/modified by Tasks 1-2. The CLI branch writes real fetched HTML to disk (no stubs). Task 3 fixtures will be real HTML (not stubs) once the operator captures them.

---

## Self-Check: PARTIAL

Tasks 1-2 files:

- [x] package.json modified — contains puppeteer, pixelmatch, pngjs in devDependencies
- [x] pnpm-lock.yaml updated — contains resolved versions
- [x] server/scrapers/cli.ts modified — contains --capture-fixture branch

Commits:
- [x] 7cba0aa — chore(01.1-01): install puppeteer + pixelmatch + pngjs as devDependencies
- [x] 13daf6f — feat(01.1-01): extend cli.ts with --capture-fixture <comp_id> branch

Task 3 files pending operator:
- [ ] server/tests/fixtures/drom/complectation/207354.html
- [ ] server/tests/fixtures/drom/complectation/252766.html
- [ ] server/tests/fixtures/drom/complectation/265067.html
- [ ] server/tests/fixtures/drom/complectation/broken-missing-dimensions.html
- [ ] server/tests/fixtures/drom/complectation/broken-truncated.html
