---
phase: 02.1
slug: tighten-visual-density-to-design-reference-fidelity
status: draft
nyquist_compliant: false
wave_0_complete: true
created: 2026-05-07
---

# Phase 02.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: `02.1-RESEARCH.md` §1 Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 3.2 (already shipped from Phase 2) |
| **Config file** | `vitest.config.ts` (existing) |
| **Quick run command** | `pnpm test landing-page-golden.test.ts` |
| **Full suite command** | `pnpm test` |
| **Estimated runtime** | ~6 seconds (golden test alone); existing 208/211 passing baseline must remain green |

---

## Sampling Rate

- **After every task commit:** Run `pnpm test landing-page-golden.test.ts`
- **After every plan wave:** Run `pnpm test` + `pnpm typecheck:server` + `pnpm build`
- **Before `/gsd-verify-work`:** Full suite green + golden passes at NEW threshold + `git diff` confirms `@media (max-width: 720px)` untouched
- **Max feedback latency:** ~6 seconds (per-task golden); ~3 minutes (per-wave full suite)

---

## Per-Decision Verification Map

Phase 02.1 has **no REQ-\* IDs** (verified). Coverage is mapped via locked decisions D-01..D-04c.

| Decision | Subject | Wave | Test Type | Automated Command | Status |
|----------|---------|------|-----------|-------------------|--------|
| D-01 | Final page height in 7000–8000 px band | 2 | puppeteer measurement | Wave 2 close-out captures fullPage and asserts height ∈ [6500, 8500] | ⬜ pending |
| D-02 | `section { padding: 140 → 72 }` (or planner-chosen value within scale) | 1 | grep | `grep -nE '^\s*section\s*\{[^}]*padding:\s*72px\s*0' src/styles/global.css` returns one line | ⬜ pending |
| D-02a | Hero `min-height: 100vh → 640px` (planner-chosen within 640–720 band) | 1 | grep | `grep -nE 'minHeight:\s*[\047"]?(640|720)px' src/sections/Hero.tsx` returns ≥1 line | ⬜ pending |
| D-02b | Top-3 within-section trims on Catalog/Process/Founders only | 1 | per-file grep | Each plan asserts its specific line change via grep | ⬜ pending |
| D-03 | Pixelmatch threshold re-tuned (post-Wave-1 empirical floor + 3pp) | 2 | grep | `grep -nE 'DIFF_THRESHOLD\s*=\s*0\.(0[0-9]|1[0-6])' server/tests/landing-page-golden.test.ts` returns one line (target band 0.10–0.16) | ⬜ pending |
| D-03b | Floor did NOT regress above 18.74% | 2 | manual capture | Wave 2 plan captures + measures pre-merge; aborts to discuss-phase if floor > 18.74% | ⬜ pending |
| D-04a | Mobile `@media (max-width: 720px)` untouched | 2 | git diff | `git diff master..HEAD -- src/styles/global.css | grep -A 2 '@media (max-width: 720px)'` shows zero hunks | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky · 🟡 deferred*

---

## Wave 0 Requirements

- [x] No new test infrastructure required — `server/tests/landing-page-golden.test.ts` already shipped in Phase 2 commit `8a3eadb`.
- [x] Puppeteer / sharp / pngjs / pixelmatch already in `package.json`.
- [x] Vite dev server confirmed at port 5174 (`vite.config.ts:6`); test already targets 5174 (`landing-page-golden.test.ts:21`).
- [ ] Wave 2 close plan updates the test file header comment to reference `02.1-RESEARCH.md §6` and the new threshold rationale.

*Wave 0 of Phase 02.1 is the research document itself (already written) — no code-level Wave 0 plan needed.*

---

## Gate: Landing Page Screenshot Golden (re-tuned)

| Property | Value |
|----------|-------|
| Purpose | Same structural-drift guard as Phase 2 — only `DIFF_THRESHOLD` changes |
| File | `server/tests/landing-page-golden.test.ts` (UPDATED in place per CONTEXT.md D-03a — NOT a new file) |
| Pipeline | UNCHANGED from Phase 2 |
| Threshold (pre-densification, current) | `DIFF_THRESHOLD = 0.22` |
| Threshold (post-densification, target) | **`DIFF_THRESHOLD = (post-Wave-1 empirical floor) + 0.03`**, predicted band **0.10–0.14** |
| Pass condition | `mismatched / (605 × 1280) ≤ DIFF_THRESHOLD` (after threshold update) |
| Fail behavior | Vitest fails; diff PNG written to `server/tests/__snapshots__/landing-page-golden.diff.png` (gitignored) |
| Where it runs | `pnpm test` locally; future CI integration |
| Wave 2 phase gate | Phase 02.1 closes only when this test passes against the post-Wave-1 build at the new threshold AND fullPage height lands in 6500–8500 px band |
| Pre-densification safety check (D-03b) | Wave 2 plan captures pre-update floor first; if > 18.74%, STOP and route to discuss-phase (densification should only improve the floor, never regress it) |

---

## Manual-Only Verifications

| Behavior | Why Manual | Test Instructions |
|----------|------------|-------------------|
| Page height ∈ [6500, 8500] px | Not a unit test — requires live capture | Wave 2 close plan runs `node tmp/capture-current-state.mjs` (or equivalent), `file tmp/landing-current-fullpage.png` shows height in target band |
| Mobile `@media (max-width: 720px)` untouched | Layout-spec safety | `git diff master..HEAD -- src/styles/global.css` shows no hunks within or adjacent to mobile media query block |
| FloatingDock aria-labels preserved | Phase 2 contract carry-over | `grep -c 'aria-label="Telegram"' src/sections/Footer.tsx` ≥ 2; same for WhatsApp |
| Cross-browser (Yandex Browser, Firefox, Safari) | Golden runs on Chromium only | Phase 8 owns cross-browser audit |
| Founder visual review of densified state | D-02b's "no broad cuts" judgment call | Founder reviews fullPage capture before phase close; no formal sign-off step |

---

## Validation Sign-Off

- [ ] All Wave 1+ tasks have golden-test or grep verification mapped above
- [ ] Sampling continuity: every Wave 1 commit runs `pnpm test landing-page-golden.test.ts` (~6 s)
- [ ] Wave 0 covers all MISSING references (Wave 0 is research-only — no code Wave 0)
- [ ] No watch-mode flags used in CI commands
- [ ] Feedback latency < 6 s (per-task) / < 3 min (per-wave full suite)
- [ ] Mobile breakpoint untouched (D-04a) — verified via `git diff` at Wave 2 close
- [ ] Pre-densification floor capture in Wave 2 confirms no regression (D-03b safety check)
- [ ] `nyquist_compliant: true` set in frontmatter once Wave 1 lands and the new threshold is empirically pinned

**Approval:** pending
