---
phase: 02.1
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sections/Founders.tsx
autonomous: true
requirements: []
decisions_addressed:
  - D-02b
must_haves:
  truths:
    - "D-01: post-densification page height target 7000–8000 px (acceptable band 6500–8500 per D-01a). NO section consolidation, NO section dropping, NO new sections (D-01b)."
    - "D-02b: Founders is the third-largest non-hero offender — 1114 px / 12.5% of page (RESEARCH §3). Trim is targeted (two-column outer grid gap + portrait-grid → quote-block gap), backed by side-by-side proof; saves ~20 px on height (RESEARCH §7 Offender #3). NO global tighten-everything-by-N% pass."
    - "FounderPortrait `size={240}` at Founders.tsx:27, 37 MUST be preserved — UI-SPEC §Component Inventory."
    - "Quote-block `padding: 32` at Founders.tsx:48 MUST be preserved — UI-SPEC `32 = xl` token."
    - "Portrait-grid internal `gap: 28` at Founders.tsx:25 MUST be preserved — already tight, balances 240×240 portraits (RESEARCH §7 anti-recommendation)."
    - "D-04a: mobile @media (max-width: 720px) blocks in global.css OFF-LIMITS (this plan touches Founders.tsx ONLY)."
    - "D-04b: single revertable commit; one file, two surgical inline-style edits."
    - "No new component library, no shadcn init, no CSS-in-JS, no Tailwind, no new tokens."
  artifacts:
    - path: src/sections/Founders.tsx
      provides: "Founders section with tightened two-column outer gap and portrait-grid → quote-block gap"
      contains: "marginBottom: 28 }"
  key_links:
    - from: "src/sections/Founders.tsx:14"
      to: "outer two-column layout (text vs portraits+quote)"
      via: "inline gap on outer grid"
      pattern: "gap:\\s*56,\\s*alignItems:\\s*'flex-start'"
    - from: "src/sections/Founders.tsx:25"
      to: "portrait-grid → quote-block vertical rhythm"
      via: "inline marginBottom on portrait grid"
      pattern: "gap:\\s*28,\\s*marginBottom:\\s*28 \\}"
---

<objective>
Within-section trim #3 of 3 per RESEARCH.md §7. Cut Founders outer two-column grid gap from `gap: 80` to `gap: 56` (line 14) and portrait-grid → quote-block gap from `marginBottom: 48` to `marginBottom: 28` (line 25). Predicted savings: ~−20 px height (the outer grid `gap` is horizontal-only and does not save vertical space; included for visual rhythm consistency with the 56-px breathing on the new section padding scale).

Side-by-side proof for D-02b:
- Outer two-column grid `gap: 80` at line 14 governs horizontal space between the heading column (1fr) and the portraits+quote column (1.4fr). Reducing to `gap: 56` does NOT save height (column gap in CSS grid is horizontal-only) but tightens the visual rhythm so the two columns feel proportional to the new 72-px section padding from plan 02.1-01. UI-SPEC `56 = 7 × 8` token, valid spacing scale value. RESEARCH §7 Offender #3 explicitly notes this is rhythm-only (zero height delta) and is included for visual consistency.
- Portrait-grid `marginBottom: 48` at line 25 governs the vertical space between the 2-portrait grid and the brand-quote `<div>` below it. Each portrait is 240×240 plus mono-marker + h3-italic + 4-line bio (~120-140 px content) → portrait card ≈ 380 px. Reducing the gap from 48 to 28 keeps the quote distinct (quote-block has its own `padding: 32` per `:48` and a coral `«` glyph anchor) while removing 20 px of visible empty space. UI-SPEC `28 = 7 × 4` token, valid spacing scale value.

Note: This is the smallest within-section trim of the Phase 02.1 set (RESEARCH §7 explicitly notes this; planner may de-prioritize if Wave 1 budget tight). Including it here per PATTERNS.md "Pattern Lock Summary" recommendation — the plan is small, surgical, and independently revertable per D-04b.

Purpose: Completes the top-3 within-section trim set per RESEARCH.md §7. Brings Founders from 1114 px to ~1094 px and rounds out the Wave 1 density compression toward the 7000–8000 band per RESEARCH.md §5 Option C math.
Output: Modified `src/sections/Founders.tsx` with two single-property inline-style edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-CONTEXT.md
@.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-RESEARCH.md
@.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-PATTERNS.md
@.planning/phases/02-redesign-from-screenshot/02-UI-SPEC.md
@src/sections/Founders.tsx

<interfaces>
<!-- Two inline-style objects being modified. Surrounding lines must stay byte-identical. -->

From src/sections/Founders.tsx:13-14 (current state, verified 2026-05-07):
```tsx
        </Reveal>
        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1.4fr', gap: 80, alignItems: 'flex-start' }}>
```

After this plan (line 14):
```tsx
        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1.4fr', gap: 56, alignItems: 'flex-start' }}>
```

From src/sections/Founders.tsx:24-25 (current state, verified 2026-05-07):
```tsx
          <Reveal delay={120}>
            <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 28, marginBottom: 48 }}>
```

After this plan (line 25):
```tsx
            <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 28, marginBottom: 28 }}>
```

LOCKED — UI-SPEC §Component Inventory at lines 27, 37 MUST be preserved byte-identical:
```tsx
                <FounderPortrait name={settings.founderKr.name} accent="coral" size={240} />
                ...
                <FounderPortrait name={settings.founderRu.name} accent="cyan" size={240} />
```

LOCKED — Quote-block `padding: 32` at line 48 MUST be preserved byte-identical:
```tsx
            <div style={{ padding: 32, border: '1px solid var(--line)', background: 'rgba(255,255,255,0.02)', position: 'relative' }}>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cut Founders outer grid gap (line 14) and portrait-grid → quote-block gap (line 25)</name>

  <read_first>
    - `src/sections/Founders.tsx` (full file, ~68 lines) — outer 2-column grid + portrait grid + quote-block + Reveal wrappers
    - `.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-CONTEXT.md` `<decisions>` D-02b, D-04a, D-04b
    - `.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-RESEARCH.md` §3 (Founders 1114 px / 12.5%), §7 Offender #3 (specific values + anti-recommendations + caveat that gap is horizontal-only)
    - `.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-PATTERNS.md` `### src/sections/Founders.tsx:14, 25` (target state + acceptance grep + must-not-touch list)
    - `.planning/phases/02-redesign-from-screenshot/02-UI-SPEC.md` §Spacing Scale (4-px base + 8/12/16/20/24/28/32/40/56/72/96 multiples — confirms 28 and 56 are valid tokens) + §Component Inventory (FounderPortrait size={240})
  </read_first>

  <files>src/sections/Founders.tsx</files>

  <action>
    Open `src/sections/Founders.tsx`. Make exactly two inline-style edits.

    **Edit 1 — Outer two-column grid at line 14** (per D-02b + RESEARCH §7 Offender #3):

    Before (line 14, exactly as present 2026-05-07):
    ```tsx
        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1.4fr', gap: 80, alignItems: 'flex-start' }}>
    ```

    After (line 14):
    ```tsx
        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1.4fr', gap: 56, alignItems: 'flex-start' }}>
    ```

    Notes:
    - Single property change. The `display: 'grid'`, `gridTemplateColumns: '1fr 1.4fr'`, `alignItems: 'flex-start'` properties are UNCHANGED.
    - This `gap` is the column gap of the outer 2-column layout — purely horizontal, does not affect height. RESEARCH §7 explicitly notes this. Included for visual rhythm consistency with the new 72-px section padding from plan 02.1-01.

    **Edit 2 — Portrait grid at line 25** (per D-02b + RESEARCH §7 Offender #3):

    Before (line 25, exactly as present 2026-05-07):
    ```tsx
            <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 28, marginBottom: 48 }}>
    ```

    After (line 25):
    ```tsx
            <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 28, marginBottom: 28 }}>
    ```

    Notes:
    - Single property change (`marginBottom: 48` → `28`). The `display: 'grid'`, `gridTemplateColumns: '1fr 1fr'`, and `gap: 28` properties are UNCHANGED.
    - The portrait-grid internal `gap: 28` (horizontal between the two founder cards) stays — RESEARCH §7 anti-recommendation: already tight, balances 240×240 portraits.
    - This is the height-saving edit (~20 px); it tightens the rhythm between the two-portrait row and the brand-quote block below.

    **MUST-NOT-TOUCH (verified by acceptance greps below):**
    - Line 11 (`<Reveal>` opener for num-marker)
    - Line 12 (`num-marker` `marginBottom: 14`) — UI-SPEC `lg` micro-token
    - Line 15 (`<Reveal>` opener for heading column)
    - Lines 16-19 (heading h2 with founder names)
    - Line 20 (`p.body-lg` with `marginTop: 28, color: 'var(--mute)', maxWidth: 380`) — UI-SPEC body-lg + 28 token
    - Line 24 (`<Reveal delay={120}>` opener for portrait+quote column)
    - Line 27 (`<FounderPortrait ... size={240} />` — UI-SPEC §Component Inventory)
    - Line 28 (`marginTop: 18` on first founder card meta block)
    - Line 30 (`h3` with `fontStyle: 'italic'` for surname)
    - Line 31 (`p` with `fontSize: 14, marginTop: 8, lineHeight: 1.5` for bio)
    - Line 37 (`<FounderPortrait ... size={240} />` — UI-SPEC §Component Inventory)
    - Lines 38-44 (second founder card meta block — same structure as first)
    - Line 48 (quote-block `padding: 32, border: '1px solid var(--line)', background: 'rgba(255,255,255,0.02)'`) — UI-SPEC `xl` token + Phase 2 quote-block surface
    - Line 49 (coral `«` glyph anchor — `top: -18, left: 24, fontSize: 64, color: 'var(--coral)'`)
    - Line 50 (quote text `paddingTop: 8`)
    - Line 53 (`marginTop: 20, gap: 12, flexWrap: 'wrap'`)
    - Lines 54-58 (Telegram + WhatsApp `<a>` tags with `aria-label` if present and `btn` className)
    - Any `<Reveal>` wrapper or delay
  </action>

  <verify>
    <automated>
grep -nE "gridTemplateColumns: '1fr 1\.4fr', gap: 56, alignItems: 'flex-start'" src/sections/Founders.tsx \
  &amp;&amp; grep -nE "gridTemplateColumns: '1fr 1fr', gap: 28, marginBottom: 28 \}" src/sections/Founders.tsx \
  &amp;&amp; grep -cE "gap: 80, alignItems: 'flex-start'" src/sections/Founders.tsx | grep -qx 0 \
  &amp;&amp; grep -cE "gap: 28, marginBottom: 48" src/sections/Founders.tsx | grep -qx 0 \
  &amp;&amp; grep -c "size={240}" src/sections/Founders.tsx | grep -qx 2 \
  &amp;&amp; grep -nE "padding: 32, border: '1px solid var\(--line\)'" src/sections/Founders.tsx \
  &amp;&amp; pnpm test landing-page-golden.test.ts 2>&amp;1 | tail -20
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -nE "gridTemplateColumns: '1fr 1\.4fr', gap: 56, alignItems: 'flex-start'" src/sections/Founders.tsx` returns one line (line 14).
    - `grep -nE "gridTemplateColumns: '1fr 1fr', gap: 28, marginBottom: 28 \}" src/sections/Founders.tsx` returns one line (line 25).
    - `grep -cE "gap: 80, alignItems: 'flex-start'" src/sections/Founders.tsx` returns 0.
    - `grep -cE "gap: 28, marginBottom: 48" src/sections/Founders.tsx` returns 0.
    - `grep -c "size={240}" src/sections/Founders.tsx` returns 2 (lines 27 and 37 — UI-SPEC §Component Inventory preserved).
    - `grep -nE "padding: 32, border: '1px solid var\(--line\)'" src/sections/Founders.tsx` returns one line (line 48 — UI-SPEC `xl` token + Phase 2 quote-block surface preserved).
    - `git diff src/sections/Founders.tsx | grep -cE '^[+-]'` returns exactly 4 (two `-` lines and two `+` lines: line 14 swap + line 25 swap).
    - `pnpm test landing-page-golden.test.ts` runs to completion (golden still at 0.22 threshold; diff ratio expected to drop further from plan 02.1-04's value).
  </acceptance_criteria>

  <done>
    - `src/sections/Founders.tsx:14` reads exactly `        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1.4fr', gap: 56, alignItems: 'flex-start' }}>`.
    - `src/sections/Founders.tsx:25` reads exactly `            <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 28, marginBottom: 28 }}>`.
    - Lines 27 and 37 (`FounderPortrait size={240}`) byte-identical.
    - Line 48 (quote-block `padding: 32, border: '1px solid var(--line)'`) byte-identical.
    - All `<Reveal>` wrappers and delays preserved.
    - Total diff: two lines changed in `src/sections/Founders.tsx` only.
    - Golden test diff ratio drops further (or stays flat — this trim is rhythm-mostly) from plan 02.1-04's value.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Frontend layout values only — no new external IO, no new auth surface, no new user-controlled inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-05 | (n/a) | src/sections/Founders.tsx | accept | No new attack surface — frontend layout values only; no new external IO; no new secrets; no new user-controlled inputs. Inline-style numeric edits cannot change auth posture or data flows. |
</threat_model>

<verification>
- `grep -nE "gridTemplateColumns: '1fr 1\.4fr', gap: 56, alignItems: 'flex-start'" src/sections/Founders.tsx` → 1 hit on line 14
- `grep -nE "gridTemplateColumns: '1fr 1fr', gap: 28, marginBottom: 28 \}" src/sections/Founders.tsx` → 1 hit on line 25
- `grep -cE "gap: 80, alignItems: 'flex-start'|gap: 28, marginBottom: 48" src/sections/Founders.tsx` → 0
- `grep -c "size={240}" src/sections/Founders.tsx` → 2 (UI-SPEC §Component Inventory preserved)
- `grep -nE "padding: 32, border: '1px solid var\(--line\)'" src/sections/Founders.tsx` → 1 hit (UI-SPEC `xl` token preserved)
- `git diff src/sections/Founders.tsx | grep -cE '^[+-]'` → 4 (two-line surgical change)
- `pnpm test landing-page-golden.test.ts` runs; diff ratio drops or stays flat (rhythm-mostly trim)
</verification>

<success_criteria>
- Two inline-style edits landed at `src/sections/Founders.tsx:14` (`gap: 80` → `56`) and `:25` (`marginBottom: 48` → `28`)
- UI-SPEC §Component Inventory FounderPortrait `size={240}` byte-identical (lines 27, 37)
- UI-SPEC `xl` token quote-block `padding: 32` byte-identical (line 48)
- Portrait-grid horizontal `gap: 28` byte-identical (line 25 — only the marginBottom changed)
- All `<Reveal>` wrappers preserved
- No new tokens, no new className, no structural changes
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-05-SUMMARY.md` recording: the exact lines/values changed, before/after diff ratio when running the golden test, confirmation that FounderPortrait `size={240}` and quote-block `padding: 32` are byte-identical, and acknowledgment that the line-14 outer-grid gap change is rhythm-only (zero height delta) per RESEARCH §7.
</output>
