---
phase: 02.1
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sections/Process.tsx
autonomous: true
requirements: []
decisions_addressed:
  - D-02b
must_haves:
  truths:
    - "D-01: post-densification page height target 7000–8000 px (acceptable band 6500–8500 per D-01a). NO section consolidation, NO section dropping, NO new sections (D-01b)."
    - "D-02b: Process is the second-largest non-hero offender — 1305 px / 14.6% of page (RESEARCH §3). Trim is targeted (heading→content gap + per-step row padding × 6 steps), backed by side-by-side proof; saves ~128 px (RESEARCH §7 Offender #2 — 6 × 16 + 32). NO global tighten-everything-by-N% pass."
    - "Phase 2 / UI-SPEC §Animation step active-state contract MUST be preserved: `transition: 'padding-left .25s ease, background .25s'` (line 37) and `paddingLeft: active === i ? 24 : 0` (line 38) and `background: active === i ? 'rgba(213,121,89,0.06)' : 'transparent'` (line 39)."
    - "Sticky preview card `padding: 28` at Process.tsx:62 MUST be preserved — UI-SPEC §Surface Treatments component padding."
    - "Two-column grid `gap: 64` at Process.tsx:25 MUST be preserved — column gap is horizontal-only in CSS grid (RESEARCH §7 anti-recommendation)."
    - "House easing `cubic-bezier(.2,.7,.2,1)` per UI-SPEC §Animation — preserved via the unchanged transition string."
    - "D-04a: mobile @media (max-width: 720px) blocks in global.css OFF-LIMITS (this plan touches Process.tsx ONLY)."
    - "D-04b: single revertable commit; one file, two surgical inline-style edits."
    - "Step row padding stays on the locked 4-px-base spacing scale (CONTEXT.md D-02): `20` is `5 × 4`, valid scale value. NO scale exceptions introduced."
    - "No new component library, no shadcn init, no CSS-in-JS, no Tailwind, no new tokens."
  artifacts:
    - path: src/sections/Process.tsx
      provides: "Process section with tightened heading-margin and per-step row padding"
      contains: "padding: '20px 0'"
  key_links:
    - from: "src/sections/Process.tsx:17"
      to: "heading → step-list spacing"
      via: "inline marginBottom on heading wrapper div"
      pattern: "marginBottom:\\s*32 \\}"
    - from: "src/sections/Process.tsx:34"
      to: "all 6 step-button rows (vertical compactness)"
      via: "inline padding on multi-line style object"
      pattern: "padding: '20px 0'"
---

<objective>
Within-section trim #2 of 3 per RESEARCH.md §7. Cut Process heading→content gap from `marginBottom: 64` to `marginBottom: 32` (line 17) and per-step row vertical padding from `padding: '28px 0'` to `padding: '20px 0'` (line 34, multiplied across 6 steps). Predicted savings: ~−128 px (32 + 6 × 16 = 128). The largest within-section save in Phase 02.1.

Side-by-side proof for D-02b:
- Heading currently has 64px gap below it before the two-column step grid begins. The h1-sized heading (~76px content) plus the num-marker pre-heading (line 18, `marginBottom: 14`) provide their own visual weight; 32px gap (down from 64) keeps the heading distinct without visible empty space. UI-SPEC `32 = xl` token, valid spacing scale value.
- Per-step row currently has 28px padding above and below the row content. Each step's content is roughly 80px tall (60px-min mono code column + h2-sized title + dur badge + 1-line subtitle). With `padding: '20px 0'` (down from 28), each row is 80 + 40 = 120 px — still spacious, still readable per RESEARCH §7 math. `20` is on the locked 4-px-base spacing scale (CONTEXT.md D-02 spacing scale: 4-px base + 8/12/16/20/24/28/32/40/56/72/96 multiples).
- Active-state behavior preserved: when a step is hovered/clicked, `paddingLeft: 24` is added (line 38) and `background: 'rgba(213,121,89,0.06)'` is applied (line 39). These transitions remain unaffected by the vertical-padding cut.

Purpose: Highest-yield within-section trim of the Phase 02.1 set. Brings Process from 1305 px to ~1177 px and is the single largest contributor toward the 7000–8000 band per RESEARCH.md §5 Lever 3 + §7.
Output: Modified `src/sections/Process.tsx` with two single-property inline-style edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-CONTEXT.md
@.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-RESEARCH.md
@.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-PATTERNS.md
@.planning/phases/02-redesign-from-screenshot/02-UI-SPEC.md
@src/sections/Process.tsx

<interfaces>
<!-- Two inline-style edits. Multi-line style object on line 32-41 must keep all other properties byte-identical. -->

From src/sections/Process.tsx:16-22 (current state, verified 2026-05-07):
```tsx
        <Reveal>
          <div style={{ marginBottom: 64 }}>
            <div className="num-marker" style={{ marginBottom: 14 }}>03 / Как мы работаем</div>
            <h2 className="h1" style={{ maxWidth: 1000 }}>
              Шесть шагов от&nbsp;заявки<br />до&nbsp;ключей. <span className="outline-italic">Без сюрпризов.</span>
            </h2>
          </div>
        </Reveal>
```

After this plan (line 17):
```tsx
          <div style={{ marginBottom: 32 }}>
```

From src/sections/Process.tsx:29-41 (current state, verified 2026-05-07 — multi-line style object):
```tsx
                <button
                  onMouseEnter={() => setActive(i)}
                  onClick={() => setActive(i)}
                  style={{
                    width: '100%', textAlign: 'left',
                    display: 'flex', gap: 28, padding: '28px 0',
                    borderTop: '1px solid var(--line)',
                    borderBottom: i === steps.length - 1 ? '1px solid var(--line)' : 'none',
                    transition: 'padding-left .25s ease, background .25s',
                    paddingLeft: active === i ? 24 : 0,
                    background: active === i ? 'rgba(213,121,89,0.06)' : 'transparent',
                    color: '#fff',
                  }}>
```

After this plan (line 34):
```tsx
                    display: 'flex', gap: 28, padding: '20px 0',
```

LOCKED — UI-SPEC §Animation contract at lines 37-39 MUST be preserved byte-identical:
```tsx
                    transition: 'padding-left .25s ease, background .25s',
                    paddingLeft: active === i ? 24 : 0,
                    background: active === i ? 'rgba(213,121,89,0.06)' : 'transparent',
```

LOCKED — Sticky preview card at line 62 MUST be preserved byte-identical:
```tsx
              padding: 28,
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cut Process heading-margin (line 17) and step-row vertical padding (line 34)</name>

  <read_first>
    - `src/sections/Process.tsx` (full file, ~89 lines) — multi-line button style object + step-active animation contract + sticky preview card
    - `.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-CONTEXT.md` `<decisions>` D-02b, D-04a, D-04b (and D-02 spacing scale: 4-px base + 8/12/16/20/24/28/32/40/56/72/96)
    - `.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-RESEARCH.md` §3 (Process 1305 px / 14.6%), §7 Offender #2 (specific values + per-step math + anti-recommendations)
    - `.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-PATTERNS.md` `### src/sections/Process.tsx:17, 34` (target state + acceptance grep + must-not-touch list)
    - `.planning/phases/02-redesign-from-screenshot/02-UI-SPEC.md` §Animation (the `padding-left .25s ease, background .25s` contract for step active state) + §Spacing Scale
  </read_first>

  <files>src/sections/Process.tsx</files>

  <action>
    Open `src/sections/Process.tsx`. Make exactly two inline-style edits.

    **Edit 1 — Heading wrapper at line 17** (per D-02b + RESEARCH §7 Offender #2):

    Before (line 17, exactly as present 2026-05-07):
    ```tsx
          <div style={{ marginBottom: 64 }}>
    ```

    After (line 17):
    ```tsx
          <div style={{ marginBottom: 32 }}>
    ```

    Notes:
    - Single property change. The wrapping `<Reveal>` (line 16), the inner `num-marker` (line 18) with its `marginBottom: 14`, and the `<h2>` (line 19) with its `maxWidth: 1000` are all UNCHANGED.

    **Edit 2 — Step button vertical padding at line 34** (per D-02b + RESEARCH §7 Offender #2):

    Before (line 34, exactly as present 2026-05-07 — line is part of a multi-line style object spanning :32-41):
    ```tsx
                    display: 'flex', gap: 28, padding: '28px 0',
    ```

    After (line 34):
    ```tsx
                    display: 'flex', gap: 28, padding: '20px 0',
    ```

    Notes:
    - Single property change inside the multi-line style object. ALL other lines (`:32 width: '100%'`, `:33 textAlign: 'left'`, `:35 borderTop`, `:36 borderBottom`, `:37 transition`, `:38 paddingLeft`, `:39 background`, `:40 color`) MUST remain byte-identical.
    - The horizontal `gap: 28` between mono code column and content column stays UNCHANGED (visual breathing within the row).
    - The padding string format is `'20px 0'` (matching the existing single-quote convention on line 34); do NOT change to double-quotes or to a numeric-only value.
    - This edit cascades naturally to all 6 steps because the `<button>` is rendered inside `steps.map((t, i) => ...)` at line 27. Each step row goes from 28+80+28 = 136 px to 20+80+20 = 120 px — saves 6 × 16 = 96 px.
    - `20` is on the locked spacing scale (CONTEXT.md `<decisions>` D-02: 4-px base + 8/12/16/20/24/28/32/40/56/72/96 multiples). NO scale exception introduced.

    **MUST-NOT-TOUCH (verified by acceptance greps below):**
    - Line 25 (two-column grid `gap: 64`) — RESEARCH §7 anti-recommendation; column gap is horizontal-only
    - Lines 35-40 (the rest of the multi-line style object — borderTop/borderBottom logic, the locked transition string, the active-state paddingLeft/background ternaries, color)
    - Line 42 (mono code column `paddingTop: 10` and other inline styles)
    - Line 47 (h3 `fontSize: 32`)
    - Line 50 (paragraph `fontSize: 15, marginTop: 10, lineHeight: 1.5`)
    - Line 62 (sticky preview card `padding: 28`) — UI-SPEC component padding
    - Line 64 (preview card `marginBottom: 24`)
    - Line 65 (`CarPlaceholder height={300}`)
    - Lines 67-89 (everything below the steps loop — sticky preview content)
    - Any `<Reveal>` wrapper or delay
  </action>

  <verify>
    <automated>
grep -nE "^\s*<div style=\{\{ marginBottom: 32 \}\}>" src/sections/Process.tsx \
  &amp;&amp; grep -nE "display: 'flex', gap: 28, padding: '20px 0'," src/sections/Process.tsx \
  &amp;&amp; grep -cE "<div style=\{\{ marginBottom: 64 \}\}>" src/sections/Process.tsx | grep -qx 0 \
  &amp;&amp; grep -cE "padding: '28px 0'," src/sections/Process.tsx | grep -qx 0 \
  &amp;&amp; grep -nE "transition: 'padding-left \.25s ease, background \.25s'" src/sections/Process.tsx \
  &amp;&amp; grep -nE "paddingLeft: active === i \? 24 : 0" src/sections/Process.tsx \
  &amp;&amp; grep -nE "background: active === i \? 'rgba\(213,121,89,0\.06\)' : 'transparent'" src/sections/Process.tsx \
  &amp;&amp; grep -nE "^\s*padding: 28," src/sections/Process.tsx \
  &amp;&amp; pnpm test landing-page-golden.test.ts 2>&amp;1 | tail -20
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -nE "^\s*<div style=\{\{ marginBottom: 32 \}\}>" src/sections/Process.tsx` returns one line (line 17).
    - `grep -nE "display: 'flex', gap: 28, padding: '20px 0'," src/sections/Process.tsx` returns one line (line 34).
    - `grep -cE "<div style=\{\{ marginBottom: 64 \}\}>" src/sections/Process.tsx` returns 0.
    - `grep -cE "padding: '28px 0'," src/sections/Process.tsx` returns 0.
    - `grep -nE "transition: 'padding-left \.25s ease, background \.25s'" src/sections/Process.tsx` returns one line (line 37 — UI-SPEC §Animation contract preserved).
    - `grep -nE "paddingLeft: active === i \? 24 : 0" src/sections/Process.tsx` returns one line (line 38 — active-state contract preserved).
    - `grep -nE "background: active === i \? 'rgba\(213,121,89,0\.06\)' : 'transparent'" src/sections/Process.tsx` returns one line (line 39 — active-state contract preserved).
    - `grep -nE "^\s*padding: 28," src/sections/Process.tsx` returns one line (line 62 — sticky preview card padding preserved).
    - `git diff src/sections/Process.tsx | grep -cE '^[+-]'` returns exactly 4 (two `-` lines and two `+` lines: line 17 swap + line 34 swap).
    - `pnpm test landing-page-golden.test.ts` runs to completion (golden still at 0.22 threshold; diff ratio expected to drop further from plan 02.1-03's value).
  </acceptance_criteria>

  <done>
    - `src/sections/Process.tsx:17` reads exactly `          <div style={{ marginBottom: 32 }}>`.
    - `src/sections/Process.tsx:34` reads exactly `                    display: 'flex', gap: 28, padding: '20px 0',`.
    - Lines 37, 38, 39 (UI-SPEC §Animation contract) byte-identical.
    - Line 62 (sticky preview card `padding: 28`) byte-identical.
    - Line 25 (two-column grid `gap: 64`) byte-identical.
    - All `<Reveal>` wrappers and delays preserved.
    - Total diff: two lines changed in `src/sections/Process.tsx` only.
    - Golden test diff ratio drops further from plan 02.1-03's value.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Frontend layout values only — no new external IO, no new auth surface, no new user-controlled inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-04 | (n/a) | src/sections/Process.tsx | accept | No new attack surface — frontend layout values only; no new external IO; no new secrets; no new user-controlled inputs. Inline-style numeric/string edits cannot change auth posture or data flows. |
</threat_model>

<verification>
- `grep -nE "<div style=\{\{ marginBottom: 32 \}\}>" src/sections/Process.tsx` → 1 hit on line 17
- `grep -nE "padding: '20px 0'" src/sections/Process.tsx` → 1 hit on line 34
- `grep -cE "marginBottom: 64 \}|padding: '28px 0'" src/sections/Process.tsx` → 0
- `grep -nE "transition: 'padding-left \.25s ease, background \.25s'" src/sections/Process.tsx` → 1 hit (UI-SPEC §Animation preserved)
- `grep -nE "paddingLeft: active === i \? 24 : 0" src/sections/Process.tsx` → 1 hit (active-state preserved)
- `git diff src/sections/Process.tsx | grep -cE '^[+-]'` → 4 (two-line surgical change)
- `pnpm test landing-page-golden.test.ts` runs; diff ratio drops further
</verification>

<success_criteria>
- Two inline-style edits landed at `src/sections/Process.tsx:17` (`marginBottom: 64` → `32`) and `:34` (`padding: '28px 0'` → `'20px 0'`)
- Predicted within-section saving: 32 + 6 × 16 = 128 px (heading 32 + step rows 96)
- UI-SPEC §Animation step active-state contract preserved (lines 37-39 byte-identical)
- Sticky preview card padding (line 62) byte-identical
- Two-column grid `gap: 64` (line 25) byte-identical
- House easing `cubic-bezier(.2,.7,.2,1)` preserved via the unchanged transition string
- All `<Reveal>` wrappers preserved
- Step padding `20` is on the locked 4-px-base spacing scale per CONTEXT.md D-02 — NO scale exception
- No new tokens, no new className, no structural changes
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-04-SUMMARY.md` recording: the exact lines/values changed, before/after diff ratio when running the golden test, confirmation that the UI-SPEC §Animation step active-state contract (transition, paddingLeft, background) is byte-identical, and confirmation that the sticky preview card padding is preserved.
</output>
