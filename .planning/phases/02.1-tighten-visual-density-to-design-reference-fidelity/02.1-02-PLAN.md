---
phase: 02.1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sections/Hero.tsx
autonomous: true
requirements: []
decisions_addressed:
  - D-02a
  - D-02b
must_haves:
  truths:
    - "D-01: post-densification page height target 7000–8000 px (acceptable band 6500–8500 per D-01a). Hero is the largest single offender via min-height: 100vh on the screenshot harness viewport (4000 px tall) per RESEARCH.md §3."
    - "D-02a: Hero `min-height: 100vh` becomes `minHeight: 640` (numeric); `paddingTop: 140` becomes `paddingTop: 72` to stay aligned with the new section padding scale (RESEARCH R-6)."
    - "D-02b: stats-strip rhythm trim (`marginTop: 96 → 56`, `paddingTop: 32 → 20` at Hero.tsx:125) is a within-section trim with side-by-side proof in this plan; saves ~52 px and prevents the stats grid from feeling cramped against Marquee after the hero shrinks (RESEARCH R-8)."
    - "Phase 2 G-01 coral-glow blob at Hero.tsx lines 50-58 with `pointerEvents: 'none'` MUST be preserved verbatim."
    - "Phase 2 DVApro outline-italic watermark at Hero.tsx lines 60-67 MUST be preserved verbatim."
    - "Phase 2 background-image + linear-gradient overlays at Hero.tsx lines 40-48 MUST be preserved verbatim."
    - "D-04a: mobile @media (max-width: 720px) hero override at global.css:584-588 is OFF-LIMITS (NOT touched by this plan — only Hero.tsx inline desktop styles change)."
    - "D-04b: single revertable commit; one file, all hero density tweaks bundled into one logical change."
    - "QuizModal D-03 is OFF-LIMITS — Hero.tsx does not reach into src/quiz/QuizModal.tsx."
    - "No new component library, no shadcn init, no CSS-in-JS, no Tailwind, no new tokens."
  artifacts:
    - path: src/sections/Hero.tsx
      provides: "Hero <header> with fixed min-height: 640, paddingTop: 72, plus tightened stats-strip rhythm"
      contains: "minHeight: 640"
  key_links:
    - from: "src/sections/Hero.tsx:37"
      to: "above-the-fold layout"
      via: "<header style={...minHeight: 640, paddingTop: 72...}>"
      pattern: "minHeight:\\s*640"
    - from: "src/sections/Hero.tsx:125"
      to: "stats strip rhythm vs Marquee section that follows"
      via: "stats grid marginTop + paddingTop"
      pattern: "marginTop:\\s*56,\\s*paddingTop:\\s*20"
---

<objective>
Replace the Hero `<header>` inline `min-height: '100vh'` with a fixed `minHeight: 640` and update inline `paddingTop: 140` to `paddingTop: 72` (per D-02a + R-6), and tighten the bottom stats-strip rhythm at line 125 from `marginTop: 96, paddingTop: 32` to `marginTop: 56, paddingTop: 20` (per D-02b + R-8). All other hero contents — Phase 2 G-01 coral-glow blob (lines 50-58), background-image + dual gradient overlays (lines 40-48), DVApro outline-italic watermark (lines 60-67), pill row (lines 71-79), headline (lines 81-86), two-column body grid (line 88), and Reveal animations — preserved byte-identical.

Purpose: Removes the harness-viewport-amplified hero balloon (`100vh = 4000px` at puppeteer capture viewport) and tightens stats rhythm so the bottom edge of the hero meets Marquee with proper visual breathing room. Combined with plan 02.1-01's section padding cut, lands the page in the 7000–8000 band per RESEARCH.md §5 Option C.

Output: Modified `src/sections/Hero.tsx` with two inline-style edits (lines 37 and 125). Total file lines changed: 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-CONTEXT.md
@.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-RESEARCH.md
@.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-PATTERNS.md
@.planning/phases/02-redesign-from-screenshot/02-UI-SPEC.md
@.planning/phases/02-redesign-from-screenshot/02-PATTERNS.md
@src/sections/Hero.tsx

<interfaces>
<!-- Two inline-style objects being modified. Surrounding lines must stay byte-identical. -->

From src/sections/Hero.tsx:31-37 (current state, verified 2026-05-07):
```tsx
  return (
    <header
      id="top"
      ref={ref}
      onMouseMove={onMove}
      className="grain spotlight-host"
      style={{ position: 'relative', minHeight: '100vh', background: 'var(--ink)', color: '#fff', overflow: 'hidden', paddingTop: 140 }}>
```

From src/sections/Hero.tsx:124-135 (current state, verified 2026-05-07):
```tsx
          <Reveal delay={420}>
            <div style={{ display: 'grid', gridTemplateColumns: 'repeat(4, 1fr)', gap: 0, marginTop: 96, paddingTop: 32, borderTop: '1px solid var(--line)' }}>
              {stats.map((s, i) => (
                <div key={s.label} style={{ paddingRight: 24, borderRight: i < 3 ? '1px solid var(--line)' : 'none', paddingLeft: i > 0 ? 24 : 0 }}>
                  <div className="stat-num" style={{ color: i % 2 ? 'var(--cyan)' : 'var(--coral)' }}>
                    <Counter to={s.n} suffix={s.suf} />
                  </div>
                  <div style={{ fontSize: 13, color: 'var(--mute)', marginTop: 8, letterSpacing: '0.04em' }}>{s.label}</div>
                </div>
              ))}
            </div>
          </Reveal>
```

LOCKED — Phase 2 G-01 coral-glow blob (Hero.tsx:50-58) MUST be preserved byte-identical:
```tsx
      {/* G-01: static radial coral glow blob (matches LeadMagnet treatment per UI-SPEC).
          Position chosen to match design-reference.png: top-right of hero.
          Pattern verbatim from LeadMagnet.tsx:6-9; pointerEvents added so it doesn't
          intercept clicks on the right-column pipeline card. */}
      <div style={{
        position: 'absolute', top: -100, right: -150, width: 700, height: 700, borderRadius: '50%',
        background: 'radial-gradient(circle, rgba(213,121,89,0.4), transparent 60%)', filter: 'blur(80px)',
        pointerEvents: 'none',
      }} />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cut Hero inline min-height + paddingTop (line 37) and stats-strip rhythm (line 125)</name>

  <read_first>
    - `src/sections/Hero.tsx` (full file, ~140 lines) — inline-style context + Phase 2 G-01 preservation block
    - `.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-CONTEXT.md` `<decisions>` D-02a, D-02b, D-04a, D-04b
    - `.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-RESEARCH.md` §2 (Hero `min-height: 100vh` location), §5 (Lever 2 math), §7 (anti-recommendations + R-8 stats strip), §8 R-6 (Hero paddingTop reminder)
    - `.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-PATTERNS.md` `### src/sections/Hero.tsx:37` (target state + MUST-PRESERVE-VERBATIM block + acceptance grep)
    - `.planning/phases/02-redesign-from-screenshot/02-PATTERNS.md` `### src/sections/Hero.tsx (MODIFY — G-01)` for the G-01 coral-glow contract
    - `.planning/phases/02-redesign-from-screenshot/02-UI-SPEC.md` §Spacing Scale (the 4-px base + 8/12/16/20/24/28/32/40/56/72/96 multiples — confirms 56, 20, 72, 640 are valid)
  </read_first>

  <files>src/sections/Hero.tsx</files>

  <action>
    Open `src/sections/Hero.tsx`. Make exactly two inline-style edits.

    **Edit 1 — Hero `<header>` style at line 37** (per D-02a + RESEARCH R-6):

    Before (line 37, exactly as present 2026-05-07):
    ```tsx
          style={{ position: 'relative', minHeight: '100vh', background: 'var(--ink)', color: '#fff', overflow: 'hidden', paddingTop: 140 }}>
    ```

    After (line 37):
    ```tsx
          style={{ position: 'relative', minHeight: 640, background: 'var(--ink)', color: '#fff', overflow: 'hidden', paddingTop: 72 }}>
    ```

    Notes:
    - `minHeight: 640` is numeric (px implied) — matches the codebase house style for inline numeric pixel values (cf. `width: 700`, `height: 700` at line 55 in the G-01 blob).
    - `paddingTop: 72` aligns the hero's top spacing rhythm with the new canonical `section { padding: 72px 0 }` cut from plan 02.1-01.
    - `640px` is on the locked spacing scale (`640 = 80 × 8`, multiple of 4 + power-of-eight); `720px` is the upper alternative within D-02a's band but `640px` is recommended (RESEARCH.md §5: hero content stack ≈ 1012 px — exceeds 640, so `min-height: 640` becomes a non-binding lower bound and the hero collapses to natural content height; this is the desired behavior for the densification target).
    - Do NOT change any other property in the style object: `position: 'relative'`, `background: 'var(--ink)'`, `color: '#fff'`, `overflow: 'hidden'` MUST remain in the same order with the same values.

    **Edit 2 — Stats grid at line 125** (per D-02b + RESEARCH R-8):

    Before (line 125, exactly as present 2026-05-07):
    ```tsx
            <div style={{ display: 'grid', gridTemplateColumns: 'repeat(4, 1fr)', gap: 0, marginTop: 96, paddingTop: 32, borderTop: '1px solid var(--line)' }}>
    ```

    After (line 125):
    ```tsx
            <div style={{ display: 'grid', gridTemplateColumns: 'repeat(4, 1fr)', gap: 0, marginTop: 56, paddingTop: 20, borderTop: '1px solid var(--line)' }}>
    ```

    Notes:
    - `marginTop: 56` is on the spacing scale (`56 = 7 × 8`); `paddingTop: 20` is on the scale (`20 = 5 × 4`). Both UI-SPEC §Spacing Scale-conformant.
    - Side-by-side proof for D-02b: with hero shrinking from 100vh (1080 px on 1080-vp / 4000 px on harness vp) to ~1012 px content stack, the stats strip's bottom edge moves from "viewport-bottom-anchored" to "natural top-aligned." Without this trim, stats abut the next section (Marquee) too tightly. With `marginTop: 56` (down from 96) and `paddingTop: 20` (down from 32), the stats grid retains visual separation from the headline above (>= 56 px gap) AND from Marquee below (Marquee adds its own `padding: '36px 0'` per Marquee.tsx:4).
    - Do NOT change `gap: 0`, `borderTop: '1px solid var(--line)'`, or the `display: 'grid', gridTemplateColumns: 'repeat(4, 1fr)'` portion — those are visual-rhythm anchors.

    **MUST-NOT-TOUCH (verified by acceptance greps below):**
    - Lines 40-48 (background-image + dual gradient overlays — Phase 2 visual contract)
    - Lines 50-58 (Phase 2 G-01 coral-glow blob with `pointerEvents: 'none'`)
    - Lines 60-67 (DVApro outline-italic watermark)
    - Lines 71-79 (pill row inside `<Reveal>` — `marginBottom: 28`)
    - Lines 81-86 (headline `<h1 className="h-display">` with `marginBottom: 28`)
    - Line 88 (two-column body grid `gridTemplateColumns: '1fr 1fr', gap: 64, marginTop: 20`)
    - Lines 127-133 (per-stat `<div>` wrappers — borderRight, paddingLeft/Right, Counter, label)
    - Any `<Reveal>` wrapper or its `delay` prop
    - `global.css:584-588` (mobile hero override) — D-04a; this plan touches Hero.tsx ONLY.
  </action>

  <verify>
    <automated>
grep -nE "minHeight: 640, background: 'var\(--ink\)', color: '#fff', overflow: 'hidden', paddingTop: 72 \}" src/sections/Hero.tsx \
  &amp;&amp; grep -cE "minHeight: '100vh'" src/sections/Hero.tsx | grep -qx 0 \
  &amp;&amp; grep -cE "paddingTop: 140 \}" src/sections/Hero.tsx | grep -qx 0 \
  &amp;&amp; grep -nE "marginTop: 56, paddingTop: 20, borderTop: '1px solid var\(--line\)'" src/sections/Hero.tsx \
  &amp;&amp; grep -cE "marginTop: 96, paddingTop: 32" src/sections/Hero.tsx | grep -qx 0 \
  &amp;&amp; grep -cE "rgba\(213,121,89,0\.4\)" src/sections/Hero.tsx | grep -qx 1 \
  &amp;&amp; grep -cE "pointerEvents: 'none'" src/sections/Hero.tsx | grep -qx 2 \
  &amp;&amp; pnpm test landing-page-golden.test.ts 2>&amp;1 | tail -20
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -nE "minHeight: 640, background: 'var\(--ink\)', color: '#fff', overflow: 'hidden', paddingTop: 72 \}" src/sections/Hero.tsx` returns one line (line 37).
    - `grep -cE "minHeight: '100vh'" src/sections/Hero.tsx` returns 0.
    - `grep -cE "paddingTop: 140 \}" src/sections/Hero.tsx` returns 0.
    - `grep -nE "marginTop: 56, paddingTop: 20, borderTop: '1px solid var\(--line\)'" src/sections/Hero.tsx` returns one line (line 125).
    - `grep -cE "marginTop: 96, paddingTop: 32" src/sections/Hero.tsx` returns 0.
    - `grep -c "rgba(213,121,89,0.4)" src/sections/Hero.tsx` returns 1 (Phase 2 G-01 coral-glow blob preserved at line 56).
    - `grep -c "pointerEvents: 'none'" src/sections/Hero.tsx` returns 2 (G-01 blob at line 57 + DVApro watermark at line 62).
    - `git diff src/sections/Hero.tsx | grep -cE '^[+-]'` returns exactly 4 (two `-` lines and two `+` lines: line 37 swap + line 125 swap).
    - `git diff src/sections/Hero.tsx | grep -E 'rgba\(213,121,89'` returns 0 lines (G-01 blob untouched).
    - `git diff src/styles/global.css | grep -E '^[+-].*@media|max-width: 720'` returns 0 (no mobile hero override edited; D-04a — note this plan does NOT modify global.css; this assertion is paranoia-grade).
    - `pnpm test landing-page-golden.test.ts` runs to completion. The golden's threshold is still 0.22 (re-tuned in plan 02.1-06); diff ratio should drop further from plan 02.1-01's value.
  </acceptance_criteria>

  <done>
    - `src/sections/Hero.tsx` line 37 reads exactly `      style={{ position: 'relative', minHeight: 640, background: 'var(--ink)', color: '#fff', overflow: 'hidden', paddingTop: 72 }}>`.
    - `src/sections/Hero.tsx` line 125 reads exactly `            <div style={{ display: 'grid', gridTemplateColumns: 'repeat(4, 1fr)', gap: 0, marginTop: 56, paddingTop: 20, borderTop: '1px solid var(--line)' }}>`.
    - All other lines of `Hero.tsx` byte-identical to pre-edit state (G-01 blob, watermark, gradients, Reveal wrappers, pill row, headline, body grid, per-stat wrappers).
    - Total diff: two lines changed in `src/sections/Hero.tsx` only.
    - Golden test still asserts at 0.22 threshold; diff ratio LOWER than plan 02.1-01's measured value (further densification improvement).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Frontend layout values only — no new external IO, no new auth surface, no new user-controlled inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-02 | (n/a) | src/sections/Hero.tsx | accept | No new attack surface — frontend layout values only; no new external IO; no new secrets; no new user-controlled inputs. Inline-style numeric edits cannot change auth posture or data flows. |
</threat_model>

<verification>
- `grep -nE "minHeight: 640, background: 'var\(--ink\)', color: '#fff', overflow: 'hidden', paddingTop: 72 \}" src/sections/Hero.tsx` → 1 hit on line 37
- `grep -cE "minHeight: '100vh'" src/sections/Hero.tsx` → 0
- `grep -cE "paddingTop: 140 \}" src/sections/Hero.tsx` → 0
- `grep -nE "marginTop: 56, paddingTop: 20, borderTop: '1px solid var\(--line\)'" src/sections/Hero.tsx` → 1 hit on line 125
- `grep -cE "marginTop: 96, paddingTop: 32" src/sections/Hero.tsx` → 0
- `grep -c "rgba(213,121,89,0.4)" src/sections/Hero.tsx` → 1 (G-01 preserved)
- `grep -c "pointerEvents: 'none'" src/sections/Hero.tsx` → 2 (G-01 + watermark preserved)
- `git diff src/sections/Hero.tsx | grep -cE '^[+-]'` → 4 (two-line surgical change)
- `pnpm test landing-page-golden.test.ts` runs; diff ratio LOWER than 19.57%
</verification>

<success_criteria>
- Two inline-style edits landed at `src/sections/Hero.tsx:37` (`minHeight: '100vh'` → `640`; `paddingTop: 140` → `72`) and `:125` (`marginTop: 96` → `56`; `paddingTop: 32` → `20`).
- Phase 2 G-01 coral-glow blob (lines 50-58, including `rgba(213,121,89,0.4)` and `pointerEvents: 'none'`) byte-identical
- DVApro outline-italic watermark (lines 60-67) byte-identical
- Background-image + dual gradient overlays (lines 40-48) byte-identical
- All `<Reveal>` wrappers and delays preserved
- Mobile `header[class*="grain"]` override at `global.css:584-588` untouched (this plan does not modify global.css)
- No new tokens, no new className, no structural changes
- Golden test diff ratio drops further below plan 02.1-01's value
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-02-SUMMARY.md` recording: the exact lines/values changed, before/after diff ratio when running the golden test, confirmation that Phase 2 G-01 coral-glow blob, DVApro watermark, and gradient overlays are byte-identical to pre-edit state, and confirmation that no `<Reveal>` wrapper was modified.
</output>
