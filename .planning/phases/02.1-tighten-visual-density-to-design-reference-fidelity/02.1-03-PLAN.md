---
phase: 02.1
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sections/Catalog.tsx
autonomous: true
requirements: []
decisions_addressed:
  - D-02b
must_haves:
  truths:
    - "D-01: post-densification page height target 7000–8000 px (acceptable band 6500–8500 per D-01a). NO section consolidation, NO section dropping, NO new sections (D-01b)."
    - "D-02b: Catalog is the largest non-hero offender — 1699 px / 19.0% of page (RESEARCH §3). Trim is targeted (heading→content gap + bottom CTA gap), backed by side-by-side proof; saves ~56 px (RESEARCH §7 Offender #1). NO global tighten-everything-by-N% pass."
    - "Phase 2 G-03 Catalog filter pill `borderRadius: 999` at Catalog.tsx:39 MUST be preserved — UI-SPEC pill token."
    - "UI-SPEC `padding: 24` (lg token) on car cards at Catalog.tsx:81 MUST be preserved."
    - "UI-SPEC `gap: 24` on the 3-column car grid at Catalog.tsx:63 MUST be preserved (balanced with 240px car height per RESEARCH §7 anti-recommendations)."
    - "UI-SPEC `CarPlaceholder height={240}` at Catalog.tsx:68 MUST be preserved."
    - "D-04a: mobile @media (max-width: 720px) blocks in global.css OFF-LIMITS (this plan touches Catalog.tsx ONLY)."
    - "D-04b: single revertable commit; one file, two surgical inline-style edits."
    - "No new component library, no shadcn init, no CSS-in-JS, no Tailwind, no new tokens."
  artifacts:
    - path: src/sections/Catalog.tsx
      provides: "Catalog section with tightened heading-row and bottom-CTA gaps"
      contains: "marginBottom: 32, flexWrap"
  key_links:
    - from: "src/sections/Catalog.tsx:26"
      to: "heading-row → cards-grid spacing"
      via: "inline marginBottom on flex container"
      pattern: "marginBottom:\\s*32,\\s*flexWrap"
    - from: "src/sections/Catalog.tsx:107"
      to: "cards-grid → bottom-CTA spacing"
      via: "inline marginTop on flex container"
      pattern: "marginTop:\\s*32 \\}"
---

<objective>
Within-section trim #1 of 3 per RESEARCH.md §7. Cut Catalog heading→content gap from `marginBottom: 56` to `marginBottom: 32` (line 26) and bottom-CTA gap from `marginTop: 64` to `marginTop: 32` (line 107). Predicted savings: −56 px. Two surgical inline-style edits, no structural changes, no new tokens.

Side-by-side proof for D-02b:
- Heading row currently has 56px gap below it before the 3-column car-card grid begins. Heading is `h1`-sized (~76px content); 32px gap (down from 56) keeps the cards readable while removing visible empty space. UI-SPEC `32 = xl` token, valid spacing scale value.
- Bottom CTA currently has 64px gap above it after the cards grid ends. The CTA is a `btn-lg` (single-line); 32px gap (down from 64) keeps the CTA distinct from the cards while removing visible whitespace. Cards' own bottom padding (24px on the card body per `:81`) provides additional rhythm.

Purpose: Largest within-section trim of the Phase 02.1 set. Bringing Catalog from 1699 px to ~1643 px helps land the page in the 7000–8000 band per RESEARCH.md §5 Option C math.
Output: Modified `src/sections/Catalog.tsx` with two single-property inline-style edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-CONTEXT.md
@.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-RESEARCH.md
@.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-PATTERNS.md
@.planning/phases/02-redesign-from-screenshot/02-UI-SPEC.md
@src/sections/Catalog.tsx

<interfaces>
<!-- Two inline-style objects being modified. Surrounding lines must stay byte-identical. -->

From src/sections/Catalog.tsx:24-26 (current state, verified 2026-05-07):
```tsx
      <div className="container">
        <Reveal>
          <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-end', marginBottom: 56, flexWrap: 'wrap', gap: 24 }}>
```

After this plan (line 26):
```tsx
          <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-end', marginBottom: 32, flexWrap: 'wrap', gap: 24 }}>
```

From src/sections/Catalog.tsx:106-107 (current state, verified 2026-05-07):
```tsx
        <Reveal>
          <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', gap: 14, marginTop: 64 }}>
```

After this plan (line 107):
```tsx
          <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', gap: 14, marginTop: 32 }}>
```

LOCKED — Phase 2 G-03 Catalog filter pill at line 39 MUST be preserved byte-identical:
```tsx
                      padding: '10px 18px', borderRadius: 999,
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cut Catalog heading→content gap (line 26) and bottom-CTA gap (line 107)</name>

  <read_first>
    - `src/sections/Catalog.tsx` (full file, ~117 lines) — inline-style context + Phase 2 G-03 pill, card padding, grid gap, CarPlaceholder size
    - `.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-CONTEXT.md` `<decisions>` D-02b, D-04a, D-04b
    - `.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-RESEARCH.md` §3 (Catalog 1699 px / 19.0%), §7 Offender #1 (specific values + anti-recommendations)
    - `.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-PATTERNS.md` `### src/sections/Catalog.tsx:26, 107` (target state + acceptance grep + must-not-touch list)
    - `.planning/phases/02-redesign-from-screenshot/02-UI-SPEC.md` §Spacing Scale (4-px base + 8/12/16/20/24/28/32/40/56/72/96 multiples — confirms 32 is a valid token)
  </read_first>

  <files>src/sections/Catalog.tsx</files>

  <action>
    Open `src/sections/Catalog.tsx`. Make exactly two inline-style edits.

    **Edit 1 — Heading-row container at line 26** (per D-02b + RESEARCH §7 Offender #1):

    Before (line 26, exactly as present 2026-05-07):
    ```tsx
          <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-end', marginBottom: 56, flexWrap: 'wrap', gap: 24 }}>
    ```

    After (line 26):
    ```tsx
          <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-end', marginBottom: 32, flexWrap: 'wrap', gap: 24 }}>
    ```

    Notes:
    - `32` is the only changed numeric. `gap: 24` (filter-pill horizontal gap) is UNCHANGED — RESEARCH §7 anti-recommendation: balanced with the pill row's natural width.
    - The `display`, `justifyContent`, `alignItems`, `flexWrap` properties are UNCHANGED.

    **Edit 2 — Bottom-CTA container at line 107** (per D-02b + RESEARCH §7 Offender #1):

    Before (line 107, exactly as present 2026-05-07):
    ```tsx
          <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', gap: 14, marginTop: 64 }}>
    ```

    After (line 107):
    ```tsx
          <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', gap: 14, marginTop: 32 }}>
    ```

    Notes:
    - `32` is the only changed numeric. `gap: 14` (CTA inner spacing for arrow icon) is UNCHANGED.
    - The `display`, `justifyContent`, `alignItems` properties are UNCHANGED.

    **MUST-NOT-TOUCH (verified by acceptance greps below):**
    - Line 39 (Phase 2 G-03 pill `borderRadius: 999`) — UI-SPEC pill token
    - Line 63 (3-column grid `gap: 24`) — RESEARCH §7 anti-recommendation
    - Line 68 (`CarPlaceholder height={240}`) — UI-SPEC §Component Inventory
    - Line 81 (card body `padding: 24`) — UI-SPEC `24 = lg` token
    - Lines 87 (card-internal price-row `marginBottom: 18, paddingTop: 14`) — UI-SPEC `lg` token
    - Any `<Reveal>` wrapper or delay
    - Any `<button>` filter-pill style block (lines 35-46)
    - The empty-state `<div>` at line 58 (`padding: 64`) — that's intentional empty-state breathing room
  </action>

  <verify>
    <automated>
grep -nE "marginBottom: 32, flexWrap: 'wrap', gap: 24" src/sections/Catalog.tsx \
  &amp;&amp; grep -nE "gap: 14, marginTop: 32 \}" src/sections/Catalog.tsx \
  &amp;&amp; grep -cE "marginBottom: 56, flexWrap" src/sections/Catalog.tsx | grep -qx 0 \
  &amp;&amp; grep -cE "gap: 14, marginTop: 64" src/sections/Catalog.tsx | grep -qx 0 \
  &amp;&amp; grep -nE "borderRadius: 999" src/sections/Catalog.tsx \
  &amp;&amp; grep -cE "padding: 24 \}" src/sections/Catalog.tsx | grep -qx 1 \
  &amp;&amp; grep -cE "height=\{240\}" src/sections/Catalog.tsx | grep -qx 1 \
  &amp;&amp; pnpm test landing-page-golden.test.ts 2>&amp;1 | tail -20
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -nE "marginBottom: 32, flexWrap: 'wrap', gap: 24" src/sections/Catalog.tsx` returns one line (line 26).
    - `grep -nE "gap: 14, marginTop: 32 \}" src/sections/Catalog.tsx` returns one line (line 107).
    - `grep -cE "marginBottom: 56, flexWrap" src/sections/Catalog.tsx` returns 0.
    - `grep -cE "gap: 14, marginTop: 64" src/sections/Catalog.tsx` returns 0.
    - `grep -nE "borderRadius: 999" src/sections/Catalog.tsx` returns one line (line 39 — Phase 2 G-03 pill preserved).
    - `grep -cE "padding: 24 \}" src/sections/Catalog.tsx` returns 1 (card body padding preserved at line 81).
    - `grep -cE "height=\{240\}" src/sections/Catalog.tsx` returns 1 (CarPlaceholder size preserved at line 68).
    - `git diff src/sections/Catalog.tsx | grep -cE '^[+-]'` returns exactly 4 (two `-` lines and two `+` lines: line 26 swap + line 107 swap).
    - `pnpm test landing-page-golden.test.ts` runs to completion (golden still at 0.22 threshold; diff ratio expected to drop further from plan 02.1-02's value).
  </acceptance_criteria>

  <done>
    - `src/sections/Catalog.tsx:26` reads exactly `          <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-end', marginBottom: 32, flexWrap: 'wrap', gap: 24 }}>`.
    - `src/sections/Catalog.tsx:107` reads exactly `          <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', gap: 14, marginTop: 32 }}>`.
    - Lines 39 (G-03 pill), 63 (grid gap), 68 (CarPlaceholder), 81 (card padding) byte-identical.
    - All `<Reveal>` wrappers preserved.
    - Total diff: two lines changed in `src/sections/Catalog.tsx` only.
    - Golden test diff ratio drops further from plan 02.1-02's value.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Frontend layout values only — no new external IO, no new auth surface, no new user-controlled inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-03 | (n/a) | src/sections/Catalog.tsx | accept | No new attack surface — frontend layout values only; no new external IO; no new secrets; no new user-controlled inputs. Inline-style numeric edits cannot change auth posture or data flows. |
</threat_model>

<verification>
- `grep -nE "marginBottom: 32, flexWrap: 'wrap', gap: 24" src/sections/Catalog.tsx` → 1 hit on line 26
- `grep -nE "gap: 14, marginTop: 32 \}" src/sections/Catalog.tsx` → 1 hit on line 107
- `grep -cE "marginBottom: 56, flexWrap|gap: 14, marginTop: 64" src/sections/Catalog.tsx` → 0
- `grep -nE "borderRadius: 999" src/sections/Catalog.tsx` → 1 hit (G-03 preserved)
- `grep -c "height={240}" src/sections/Catalog.tsx` → 1 (UI-SPEC component inventory preserved)
- `git diff src/sections/Catalog.tsx | grep -cE '^[+-]'` → 4 (two-line surgical change)
- `pnpm test landing-page-golden.test.ts` runs; diff ratio drops further
</verification>

<success_criteria>
- Two inline-style edits landed at `src/sections/Catalog.tsx:26` (`marginBottom: 56` → `32`) and `:107` (`marginTop: 64` → `32`)
- Phase 2 G-03 filter pill `borderRadius: 999` at line 39 byte-identical
- UI-SPEC tokens preserved: car-grid `gap: 24` (line 63), CarPlaceholder `size={240}` (line 68), card body `padding: 24` (line 81)
- All `<Reveal>` wrappers preserved
- No new tokens, no new className, no structural changes
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-03-SUMMARY.md` recording: the exact lines/values changed, before/after diff ratio when running the golden test, confirmation that G-03 pill `borderRadius: 999` and all UI-SPEC card tokens are byte-identical to pre-edit state.
</output>
