# Phase 02.1: Tighten Visual Density to Design-Reference Fidelity — Pattern Map

**Mapped:** 2026-05-07
**Files analyzed:** 6 (all MODIFY — no new files in Phase 02.1)
**Analogs found:** 6 / 6 (100% — every file is its own primary analog because Phase 02.1 is a tightening pass on Phase 2's just-shipped state)
**Primary cross-phase analog source:** `.planning/phases/02-redesign-from-screenshot/02-PATTERNS.md` (same files, same edit-style as Phase 2)

> **Rosetta:** Phase 02.1 does **not** introduce new files, components, or libraries. Every "pattern" here is the **existing line about to be modified** plus the surrounding code that must be **preserved verbatim**. The planner's job is to construct surgical `<action>` blocks that replace single tokens (e.g. `140px → 72px`) without disturbing the carefully-built Phase 2 visual contract.

---

## Locked Anti-Patterns (carry forward from Phase 2 UI-SPEC §Pre-existing System Note)

These rules apply to **every** Phase 02.1 plan. Any deviation requires founder confirmation.

| Anti-pattern | Forbidden | Reference |
|---|---|---|
| New font sizes | Use only the 12 sizes already declared in UI-SPEC §Typography | UI-SPEC §Pre-existing System Note |
| New font weights | Use only `400 / 500 / 700 / 900` (NO 300, NO 600, NO 800) | UI-SPEC §Pre-existing System Note |
| New color hex literals | Use `var(--*)` tokens; only `rgba(213,121,89,...)` is allowed for coral-with-alpha | UI-SPEC §Color |
| New `border-radius` values | Use only `0`, `999`/`999px`, or `50%` | UI-SPEC §Surface Treatments |
| Component libraries | No shadcn, no radix-ui, no mui, no chakra, no headlessui | CLAUDE.md, UI-SPEC §Pre-existing System Note |
| CSS-in-JS | No styled-components, no emotion, no @emotion/react | CLAUDE.md |
| Tailwind | No utility-class CSS framework migration | CLAUDE.md |
| Next.js migration | React 18 + Vite + react-router stays | CLAUDE.md |
| Mobile breakpoint edits | `@media (max-width: 720px)` is **OFF-LIMITS** (D-04a) — touched lines `global.css:235`, `:567+`, `:584-588` | CONTEXT.md D-04a |
| New CSS tokens | Use only existing spacing scale (4-px base, 8/12/16/20/24/28/32/40/56/72/96) | UI-SPEC §Spacing Scale |
| New section structure | No add/remove/reorder of the 10 sections; no consolidation | CONTEXT.md D-01b |
| Quiz scope creep | `src/quiz/QuizModal.tsx` is **OFF-LIMITS** in Phase 02.1 (Phase 8 polish) | CONTEXT.md D-04 + RESEARCH.md R-7 |

**House easing (preserve when touching transitions):** `cubic-bezier(.2,.7,.2,1)` — used in `Process.tsx:37` (`'padding-left .25s ease, background .25s'` is the existing string and stays unchanged unless explicitly retuned).

---

## File Classification

| File | Role | Data Flow | Change Type | Wave | Closest Analog | Match Quality |
|------|------|-----------|-------------|------|----------------|---------------|
| `src/styles/global.css:234` | config (CSS) | render | single-token edit (`140px` → planner-chosen 72px or 80px) | 1 | `src/styles/global.css:234` itself (Phase 2 edited :599 in identical surgical-token style; G-11 pattern) | exact |
| `src/sections/Hero.tsx:37` | component (section / `<header>`) | render | inline-style edit (`minHeight: '100vh'` + `paddingTop: 140` → planner-chosen pair within 640-720 + 72-80) | 1 | `Hero.tsx:37` itself (Phase 2 G-01 added the coral-glow at lines 50-58 — must be preserved verbatim) | exact |
| `src/sections/Catalog.tsx:26, 107` | component (section) | render | inline-style edits (`marginBottom: 56` → 32; `marginTop: 64` → 32) | 1 | `Catalog.tsx:26, 107` itself; same inline-style + Reveal pattern as siblings | exact |
| `src/sections/Process.tsx:17, 34` | component (section) | render | inline-style edits (heading `marginBottom: 64` → 32; step `padding: '28px 0'` → `'18px 0'`) | 1 | `Process.tsx:17, 34` itself; same inline-style pattern | exact |
| `src/sections/Founders.tsx:14, 25` | component (section) | render | inline-style edits (column `gap: 80` → 56; portrait grid `marginBottom: 48` → 28) | 1 | `Founders.tsx:14, 25` itself; same grid + Reveal pattern | exact |
| `server/tests/landing-page-golden.test.ts:20` | test (vitest integration) | request-response | single-constant edit (`DIFF_THRESHOLD = 0.22` → planner-chosen value 0.10-0.16) + header comment update (lines 1-4) | 2 | `landing-page-golden.test.ts:20` itself; verbatim re-use of Phase 2 Wave 0 harness | exact |

**Total file count:** 6 (1 CSS + 4 TSX + 1 test). Wave 1 = 5 files in parallel; Wave 2 = 1 file (the test).

---

## Pattern Assignments

### `src/styles/global.css:234` — Section padding cut (D-02, Wave 1)

**Role:** config (CSS) · **Data flow:** render · **Change scope:** ONE LINE.

**Cross-phase analog (Phase 2 precedent):** `02-PATTERNS.md` `### src/styles/global.css:599 (MODIFY — G-11)` — same pattern: surgical single-numeric-token swap inside an existing CSS rule, no rule restructuring, no new selector.

#### Current state (verified 2026-05-07)

```css
/* global.css:234 */
section { padding: 140px 0; position: relative; }
/* global.css:235 — OFF-LIMITS per D-04a */
@media (max-width: 720px){ section { padding: 80px 0; } }
```

#### Target state (planner picks within D-02 band)

The planner picks **one** value from `{72, 80}` (D-02 suggested). RESEARCH.md §5 recommends `72px`:

```css
/* global.css:234 — REPLACE */
section { padding: 72px 0; position: relative; }
/* global.css:235 — UNCHANGED (mobile gate) */
@media (max-width: 720px){ section { padding: 80px 0; } }
```

#### Cascade behavior (RESEARCH.md §2 audit)

This single line cascades to **6 sections** that have NO inline padding override:
- `#catalog`, `#process`, `#founders`, `#lead-magnet`, `#reviews`, `#faq`

These sections have inline overrides and are **NOT cascaded** by this edit:
- `Hero.tsx:37` — `<header>` not `<section>`; inline `paddingTop: 140` (handled in separate Hero plan)
- `Marquee.tsx:4` — inline `padding: '36px 0'` (intentional; UI-SPEC §Spacing Scale Exceptions)
- `FeedStrip.tsx:9` — inline `padding: '80px 0'` (intentional; optional Plan P3-bis trim per RESEARCH.md §7)
- `Footer.tsx:9` — `<footer>` not `<section>`; inline `padding: '100px 0 48px'` (intentional asymmetric)

#### Acceptance grep (paste into plan's `<verify>`)

```bash
# After edit:
grep -nE '^section \{ padding: 72px 0; position: relative; \}$' src/styles/global.css
# Expected: 1 hit on line 234

grep -nE 'padding: 140px 0' src/styles/global.css
# Expected: 0 hits

# D-04a safety check — mobile gate untouched:
grep -nE '@media \(max-width: 720px\)\{ section \{ padding: 80px 0; \} \}' src/styles/global.css
# Expected: 1 hit on line 235
```

#### DO-NOT-do

- Do **NOT** change line 235 (`@media (max-width: 720px){ section { padding: 80px 0; } }`) — D-04a explicit anti-edit
- Do **NOT** change line 567+ (the second `@media (max-width: 720px)` block) — D-04a
- Do **NOT** change line 598 (the `!important` mobile override) — D-04a
- Do **NOT** introduce a new spacing token (e.g., `74px`) — D-02 locks values to existing scale
- Do **NOT** remove `position: relative` — required for absolutely-positioned section children (e.g., FeedStrip background)

---

### `src/sections/Hero.tsx:37` — Hero `min-height` + `paddingTop` cut (D-02a, Wave 1)

**Role:** component (section / `<header>`) · **Data flow:** render · **Change scope:** ONE LINE (the inline `style={{...}}` prop on `<header>`).

**Cross-phase analog (Phase 2 precedent):** `02-PATTERNS.md` `### src/sections/Hero.tsx (MODIFY — G-01)` — same file, same approach (surgical inline-style edit, preserve all surrounding G-01 work). The Phase 2 G-01 coral-glow blob at lines 50-58 **MUST NOT BE TOUCHED**.

#### Current state (verified 2026-05-07)

```tsx
// Hero.tsx:31-37 — the <header> element opening
return (
  <header
    id="top"
    ref={ref}
    onMouseMove={onMove}
    className="grain spotlight-host"
    style={{ position: 'relative', minHeight: '100vh', background: 'var(--ink)', color: '#fff', overflow: 'hidden', paddingTop: 140 }}>
```

#### Target state (planner picks within D-02a band)

Per RESEARCH.md §5 + R-6: update **both** `minHeight` AND `paddingTop` together. RESEARCH.md recommends `640px` + `72`:

```tsx
// Hero.tsx:37 — REPLACE
    style={{ position: 'relative', minHeight: 640, background: 'var(--ink)', color: '#fff', overflow: 'hidden', paddingTop: 72 }}>
```

Note `minHeight: 640` is a numeric (px implied) — matches the existing house style for inline numeric pixel values. The string form `'640px'` is also acceptable but the codebase prefers numeric.

#### MUST-PRESERVE-VERBATIM block (Phase 2 G-01 coral glow, Hero.tsx:50-58)

```tsx
{/* G-01: static radial coral glow blob (matches LeadMagnet treatment per UI-SPEC).
    Position chosen to match design-reference.png: top-right of hero.
    Pattern verbatim from LeadMagnet.tsx:6-9; pointerEvents added so it doesn't
    intercept clicks on the right-column pipeline card. */}
<div style={{
  position: 'absolute', top: -100, right: -150, width: 700, height: 700, borderRadius: '50%',
  background: 'radial-gradient(circle, rgba(213,121,89,0.4), transparent 60%)', filter: 'blur(80px)',
  pointerEvents: 'none',
}} />
```

This block was added by Phase 2 G-01 and is part of the locked visual contract. **The Hero plan must NOT touch lines 50-58.**

#### Optional companion edit (RESEARCH.md §7 R-8) — stats strip rhythm

After hero shrinks, the bottom stats strip becomes top-aligned and may feel cramped against Marquee. RESEARCH.md recommends:

```tsx
// Hero.tsx:125 — current
<div style={{ display: 'grid', gridTemplateColumns: 'repeat(4, 1fr)', gap: 0, marginTop: 96, paddingTop: 32, borderTop: '1px solid var(--line)' }}>

// Hero.tsx:125 — recommended (D-02b within-section trim, saves ~52px)
<div style={{ display: 'grid', gridTemplateColumns: 'repeat(4, 1fr)', gap: 0, marginTop: 56, paddingTop: 20, borderTop: '1px solid var(--line)' }}>
```

`marginTop: 56` is a valid spacing token (`56 = 7 × 8`). `paddingTop: 20` is a valid spacing token (`20 = 5 × 4`). This is OPTIONAL per D-02b; planner decides based on Wave 1 measurement. If included, it folds into the Hero plan (single commit, single revertable unit per D-04b).

#### Acceptance grep (paste into plan's `<verify>`)

```bash
# After edit:
grep -nE "minHeight:\s*640" src/sections/Hero.tsx
# Expected: 1 hit on line 37

grep -nE "paddingTop:\s*72\s*\}" src/sections/Hero.tsx
# Expected: 1 hit on line 37

grep -nE "minHeight:\s*'100vh'" src/sections/Hero.tsx
# Expected: 0 hits

grep -nE "paddingTop:\s*140" src/sections/Hero.tsx
# Expected: 0 hits

# G-01 coral glow MUST be preserved:
grep -c "rgba(213,121,89,0.4)" src/sections/Hero.tsx
# Expected: 1 (the existing G-01 blob at line 56)

grep -c "pointerEvents: 'none'" src/sections/Hero.tsx
# Expected: ≥2 (G-01 blob at :57 + DVApro watermark at :62)
```

#### DO-NOT-do

- Do **NOT** modify lines 50-58 (Phase 2 G-01 coral-glow blob; locked)
- Do **NOT** modify line 47 or 48 (linear-gradient overlays; locked Phase 2 visuals)
- Do **NOT** modify line 60-67 (DVApro outline-italic watermark; locked)
- Do **NOT** introduce `min-height: 720px` if `640px` is also viable — pick one and stop
- Do **NOT** edit `global.css:584-588` (mobile-only `header[class*="grain"]` override) — D-04a

---

### `src/sections/Catalog.tsx:26, 107` — Within-section trim #1 (D-02b, Wave 1)

**Role:** component (section) · **Data flow:** render · **Change scope:** TWO INLINE-STYLE EDITS.

**Section height:** 1 699 px / 19.0% of page (largest non-hero offender per RESEARCH.md §3). Targeted savings: **−56 px**.

#### Current state — Catalog.tsx:25-26 (heading→content gap)

```tsx
// Catalog.tsx:25-26
<Reveal>
  <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-end', marginBottom: 56, flexWrap: 'wrap', gap: 24 }}>
```

#### Target state — Catalog.tsx:26

```tsx
// Catalog.tsx:26 — REPLACE (single property change)
  <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-end', marginBottom: 32, flexWrap: 'wrap', gap: 24 }}>
```

#### Current state — Catalog.tsx:106-107 (bottom CTA gap)

```tsx
// Catalog.tsx:106-107
<Reveal>
  <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', gap: 14, marginTop: 64 }}>
```

#### Target state — Catalog.tsx:107

```tsx
// Catalog.tsx:107 — REPLACE
  <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', gap: 14, marginTop: 32 }}>
```

#### MUST-NOT-TOUCH within Catalog.tsx (RESEARCH.md §7 anti-recommendations)

- **`Catalog.tsx:39`** — filter-pill `borderRadius: 999` (Phase 2 G-03 verify-only; UI-SPEC pill token)
- **`Catalog.tsx:63`** — 3-column grid `gap: 24` (RESEARCH.md §7: balanced with 240px car height)
- **`Catalog.tsx:81`** — car card `padding: 24` (UI-SPEC `24 = lg` token)
- **`Catalog.tsx:68`** — `CarPlaceholder height={240}` (UI-SPEC §Component Inventory)

#### Acceptance grep (paste into plan's `<verify>`)

```bash
grep -nE "marginBottom:\s*32, flexWrap" src/sections/Catalog.tsx
# Expected: 1 hit on line 26

grep -nE "marginTop:\s*32 \}" src/sections/Catalog.tsx
# Expected: 1 hit on line 107

grep -nE "marginBottom:\s*56|marginTop:\s*64" src/sections/Catalog.tsx
# Expected: 0 hits

# G-03 preservation (Phase 2 contract):
grep -nE "borderRadius:\s*999" src/sections/Catalog.tsx
# Expected: 1 hit on line 39
```

---

### `src/sections/Process.tsx:17, 34` — Within-section trim #2 (D-02b, Wave 1)

**Role:** component (section) · **Data flow:** render · **Change scope:** TWO INLINE-STYLE EDITS. **Highest savings of the within-section trims: −152 px** (the per-step padding cut alone saves 6 × 20 = 120 px).

#### Current state — Process.tsx:16-17 (heading→content gap)

```tsx
// Process.tsx:16-17
<Reveal>
  <div style={{ marginBottom: 64 }}>
```

#### Target state — Process.tsx:17

```tsx
// Process.tsx:17 — REPLACE
  <div style={{ marginBottom: 32 }}>
```

#### Current state — Process.tsx:32-41 (step button padding)

```tsx
// Process.tsx:29-41 — full <button> opening for context
<button
  onMouseEnter={() => setActive(i)}
  onClick={() => setActive(i)}
  style={{
    width: '100%', textAlign: 'left',
    display: 'flex', gap: 28, padding: '28px 0',
    borderTop: '1px solid var(--line)',
    borderBottom: i === steps.length - 1 ? '1px solid var(--line)' : 'none',
    transition: 'padding-left .25s ease, background .25s',
    paddingLeft: active === i ? 24 : 0,
    background: active === i ? 'rgba(213,121,89,0.06)' : 'transparent',
    color: '#fff',
  }}>
```

#### Target state — Process.tsx:34

```tsx
// Process.tsx:34 — REPLACE (single string change inside the multi-line style object)
    display: 'flex', gap: 28, padding: '18px 0',
```

#### Per-step row math (RESEARCH.md §7)

Step row content height ≈ 80 px (60-px-min mono code column + h2 + dur + 1-line subtitle). With `padding: '18px 0'`, total step row = 80 + 36 = 116 px — still spacious, still readable. Multiplied across 6 steps: 6 × 20 px saved = **120 px section savings**.

#### MUST-NOT-TOUCH within Process.tsx (RESEARCH.md §7 anti-recommendations)

- **`Process.tsx:25`** — `gap: 64` on the two-column grid (column gap is horizontal-only, doesn't affect height)
- **`Process.tsx:37`** — `transition: 'padding-left .25s ease, background .25s'` (UI-SPEC §Animation; locked easing)
- **`Process.tsx:38`** — `paddingLeft: active === i ? 24 : 0` (UI-SPEC §Animation step-active state)
- **`Process.tsx:39`** — `background: active === i ? 'rgba(213,121,89,0.06)' : 'transparent'` (active state)
- **`Process.tsx:62`** — sticky preview card `padding: 28` (component padding, locked)

#### Acceptance grep (paste into plan's `<verify>`)

```bash
grep -nE "marginBottom:\s*32 \}" src/sections/Process.tsx
# Expected: 1 hit on line 17

grep -nE "padding: '18px 0'" src/sections/Process.tsx
# Expected: 1 hit on line 34

grep -nE "marginBottom:\s*64 \}|padding: '28px 0'" src/sections/Process.tsx
# Expected: 0 hits

# Active-state preservation (UI-SPEC §Animation):
grep -nE "paddingLeft:\s*active === i \? 24 : 0" src/sections/Process.tsx
# Expected: 1 hit on line 38

grep -nE "transition: 'padding-left .25s ease, background .25s'" src/sections/Process.tsx
# Expected: 1 hit on line 37
```

#### Optional follow-on (NOT in this plan — for planner awareness)

`Faq.tsx:33` accordion-button `padding: '28px 0'` mirrors Process step padding. RESEARCH.md §7 notes that for visual consistency, FAQ should drop to `'18px 0'` IF Process drops. This is OUT OF SCOPE for the Process plan but the planner may add a separate Plan P3-bis if the Wave 1 measurement still misses target.

---

### `src/sections/Founders.tsx:14, 25` — Within-section trim #3 (D-02b, Wave 1)

**Role:** component (section) · **Data flow:** render · **Change scope:** TWO INLINE-STYLE EDITS. **Lowest savings: −20 px.** Plan is optional per RESEARCH.md §7 — planner may de-prioritize if Wave 1 budget is tight.

#### Current state — Founders.tsx:14 (two-column grid gap)

```tsx
// Founders.tsx:14
<div style={{ display: 'grid', gridTemplateColumns: '1fr 1.4fr', gap: 80, alignItems: 'flex-start' }}>
```

#### Target state — Founders.tsx:14

```tsx
// Founders.tsx:14 — REPLACE
<div style={{ display: 'grid', gridTemplateColumns: '1fr 1.4fr', gap: 56, alignItems: 'flex-start' }}>
```

**Note:** column gap is horizontal-only in CSS grid; does **NOT** save height directly. Listed because RESEARCH.md §7 recommends it for visual rhythm when adjacent reductions land. If planner wants strict savings only, this edit can be skipped.

#### Current state — Founders.tsx:24-25 (portrait grid → quote-block gap)

```tsx
// Founders.tsx:24-25
<Reveal delay={120}>
  <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 28, marginBottom: 48 }}>
```

#### Target state — Founders.tsx:25

```tsx
// Founders.tsx:25 — REPLACE
  <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 28, marginBottom: 28 }}>
```

#### MUST-NOT-TOUCH within Founders.tsx (RESEARCH.md §7 anti-recommendations)

- **`Founders.tsx:25`** — `gap: 28` on portrait grid (already tight, balances 240×240 portraits)
- **`Founders.tsx:27, 37`** — `<FounderPortrait ... size={240} />` (UI-SPEC §Component Inventory)
- **`Founders.tsx:48`** — quote-block `padding: 32` (UI-SPEC `32 = xl` token)

#### Acceptance grep (paste into plan's `<verify>`)

```bash
grep -nE "gap:\s*56, alignItems: 'flex-start'" src/sections/Founders.tsx
# Expected: 1 hit on line 14

grep -nE "gap:\s*28, marginBottom:\s*28 \}" src/sections/Founders.tsx
# Expected: 1 hit on line 25

grep -nE "gap:\s*80, alignItems: 'flex-start'|marginBottom:\s*48 \}" src/sections/Founders.tsx
# Expected: 0 hits

# Portrait size preservation (UI-SPEC §Component Inventory):
grep -c "size={240}" src/sections/Founders.tsx
# Expected: 2 (lines 27 + 37)
```

---

### `server/tests/landing-page-golden.test.ts:20` — Threshold re-tune (D-03/D-03a, Wave 2)

**Role:** test (vitest integration) · **Data flow:** request-response (puppeteer→sharp→pixelmatch) · **Change scope:** ONE CONSTANT + HEADER COMMENT.

**Cross-phase analog:** the file itself, just-shipped from Phase 2 commit (Wave 0 of Phase 2). Phase 02.1 reuses **every line of pipeline verbatim** — this is the textbook "tunable change" pattern.

#### Current state (verified 2026-05-07) — full preserved-verbatim regions

```ts
// landing-page-golden.test.ts:1-4 (header comment — UPDATE in Wave 2)
// server/tests/landing-page-golden.test.ts
// Phase 02 D-02 / D-02a: structural-drift guard for the public landing page.
// Threshold tuned to 0.22 against as-shipped baseline of 18.74% (see 02-RESEARCH.md §3).
// Test self-orchestrates Vite dev server bring-up; no external orchestration required.
```

```ts
// landing-page-golden.test.ts:20 (THE single tunable)
const DIFF_THRESHOLD = 0.22; // 22% — empirical as-shipped floor 18.74% + 3.26pp safety band; see 02-RESEARCH.md §3
```

#### Target state — Wave 2

The planner picks the value AFTER Wave 1 lands and the empirical floor is re-measured. Predicted band: **0.10–0.16** (RESEARCH.md §6). Procedure:

1. After Wave 1 commits (P1+P2+P3 merged), run a temporary measurement (Phase 2's pattern from `02-RESEARCH.md §3`) against the new build → get measured floor `F`
2. Set `DIFF_THRESHOLD = round_up_to_2_decimals(F + 0.03)` (per Phase 2's +3pp safety convention)
3. Update header comment lines 1-4 to reference Phase 02.1
4. Run `pnpm test landing-page-golden.test.ts` to confirm

```ts
// landing-page-golden.test.ts:1-4 — REPLACE
// server/tests/landing-page-golden.test.ts
// Phase 02.1 D-03 / D-03a: structural-drift guard, re-tuned post-densification.
// Threshold tuned to <NEW_VALUE> against post-Wave-1 baseline of <MEASURED_FLOOR>% (see 02.1-RESEARCH.md §6).
// Test self-orchestrates Vite dev server bring-up; no external orchestration required.
```

```ts
// landing-page-golden.test.ts:20 — REPLACE
const DIFF_THRESHOLD = <NEW_VALUE>; // <NEW_VALUE * 100>% — empirical post-Wave-1 floor <MEASURED_FLOOR>% + 3pp safety band; see 02.1-RESEARCH.md §6
```

#### Pipeline lines that MUST NOT change (lines 5-97 except line 20)

```ts
// landing-page-golden.test.ts:5-19, 21-97 — preserved VERBATIM
import { describe, it, expect } from 'vitest';
import puppeteer from 'puppeteer';
import sharp from 'sharp';
import { spawn, type ChildProcess } from 'node:child_process';
import { readFile, writeFile } from 'node:fs/promises';
import { resolve } from 'node:path';
import { PNG } from 'pngjs';
import pixelmatch from 'pixelmatch';

const REFERENCE = resolve('.planning/phases/02-redesign-from-screenshot/design-reference.png');
const DIFF_PATH = resolve('server/tests/__snapshots__/landing-page-golden.diff.png');
const VIEWPORT = { width: 1280, height: 4000 };
const REF_W = 605;
const REF_H = 1280;
const DEV_URL = 'http://127.0.0.1:5174/';

// ...all 70+ lines of the test body, the bringUpDevServer helper, the puppeteer launch,
// the sharp.resize call, the pixelmatch invocation, the diff write, the expect assertion,
// the try/finally graceful shutdown — all PRESERVED VERBATIM.
```

#### Pre-merge safety check (D-03b)

Before committing the new threshold, capture the current floor and confirm it's NOT > 18.74% (Phase 2's value). If it is, **STOP** and route to discuss-phase (densification regressed; falsifiable contract violated).

#### Acceptance grep (paste into plan's `<verify>`)

```bash
# New threshold landed:
grep -nE "DIFF_THRESHOLD\s*=\s*0\.(0[7-9]|1[0-6])" server/tests/landing-page-golden.test.ts
# Expected: 1 hit on line 20

# Old threshold gone:
grep -nE "DIFF_THRESHOLD\s*=\s*0\.22" server/tests/landing-page-golden.test.ts
# Expected: 0 hits

# Header comment updated to reference Phase 02.1:
grep -nE "Phase 02\.1" server/tests/landing-page-golden.test.ts
# Expected: ≥1 hit (in header comment block)

# Pipeline preservation — these MUST still match:
grep -c "pixelmatch" server/tests/landing-page-golden.test.ts
# Expected: 2 (1 import + 1 call)

grep -c "sharp(captureBuf)" server/tests/landing-page-golden.test.ts
# Expected: 1 (resize call)

grep -c "bringUpDevServer" server/tests/landing-page-golden.test.ts
# Expected: 2 (1 declaration + 1 call)
```

#### DO-NOT-do

- Do **NOT** create a parallel test file (D-03a explicit)
- Do **NOT** change `VIEWPORT`, `REF_W`, `REF_H`, `DEV_URL` constants (test capture geometry locked)
- Do **NOT** change the per-pixel `{ threshold: 0.1 }` argument to `pixelmatch` — that's the YIQ tolerance, distinct from `DIFF_THRESHOLD` (the ratio gate). See `02-PATTERNS.md` §"Per-pixel pixelmatch Threshold (do NOT change)"
- Do **NOT** remove the diff-PNG-on-failure write (lines 83-88) — operator inspection signal
- Do **NOT** change `60_000` test timeout (line 95) — dev-server bring-up needs the headroom

---

## Shared Patterns

### Inline-Style + Utility-Class Convention (applies to ALL TSX edits in Wave 1)

**Source:** `src/sections/Hero.tsx`, `src/sections/Catalog.tsx`, `src/sections/Process.tsx`, `src/sections/Founders.tsx` (verified)

**Apply to:** Every TSX edit in this phase.

**Rule:** Phase 02.1 does NOT introduce any new className. All edits are surgical numeric changes inside existing inline `style={{...}}` objects. The pattern surrounding each edit must be preserved 1:1. Concretely:

```tsx
// House pattern across all section files (verified):
<section id="<id>" style={{ background: 'var(--ink)', color: '#fff' }}>
  <div className="container">
    <Reveal>
      {/* heading block — inline marginBottom for layout, className for typography */}
      <h2 className="h1" style={{ marginBottom: <CHANGE_HERE> }}>...</h2>
    </Reveal>
    {/* content grid — inline gap + grid template */}
    <div style={{ display: 'grid', gridTemplateColumns: '...', gap: <PRESERVE> }}>...</div>
  </div>
</section>
```

**Applies cleanly to:**
- `Catalog.tsx:26, 107` (heading-row + bottom-CTA flex containers)
- `Process.tsx:17, 34` (heading-margin div + step-button padding)
- `Founders.tsx:14, 25` (two-column grid + portrait grid)
- `Hero.tsx:37` (header-element style object) + `Hero.tsx:125` (stats grid style object, optional)

### Reveal Wrapper Preservation

**Source:** Every section file (verified across `Hero.tsx:71, 81, 89, 103, 124`, `Catalog.tsx:25, 57, 65, 106`, `Process.tsx:16, 28, 57`, `Founders.tsx:11, 15, 24`)

**Apply to:** Every TSX edit. The `<Reveal>` and `<Reveal delay={N}>` wrappers carry the IntersectionObserver-driven entrance animation. **Never** remove or restructure these wrappers — RESEARCH.md §R-3 notes that Reveal timing is part of the visual rhythm budget.

```tsx
// Pattern — preserve verbatim:
<Reveal>
  <div style={{ ...layout..., marginBottom: <CHANGE> }}>
    ...
  </div>
</Reveal>

<Reveal delay={120}>
  <div style={{ ...layout..., gap: <CHANGE> }}>
    ...
  </div>
</Reveal>
```

### Token-purity check (per UI-SPEC §Pre-existing System Note)

**Source:** UI-SPEC + verified across all section files

**Apply to:** Every TSX edit before commit. Run the same token-purity grep that Phase 2 used:

```bash
# Token-purity sweep — should reveal NO new violations after edit:
grep -nE '#[0-9A-Fa-f]{3,6}' src/sections/<file>.tsx | grep -v -E "rgba\(213,121,89|//|/\*"
# Expected: only pre-existing hex literals; no NEW hex literals introduced

# Border-radius purity — should reveal only existing 0/999/50%:
grep -nE 'borderRadius:\s*[^09]' src/sections/<file>.tsx | grep -v -E "999|50%|borderRadius:\s*0"
# Expected: 0 hits
```

### Mobile breakpoint guard (D-04a — applies to ALL Phase 02.1 commits)

**Source:** CONTEXT.md D-04a

**Apply to:** Every commit. Wave 2 close plan **MUST** assert this:

```bash
# Across the full Phase 02.1 commit range:
git diff <phase-02.1-base>..HEAD -- src/styles/global.css | grep -E '^[+-].*@media|max-width: 720'
# Expected: 0 lines (or only pure context lines, never +/-)

# Also check no hero mobile override edits:
git diff <phase-02.1-base>..HEAD -- src/styles/global.css | grep -A 4 "header\[class\*=\"grain\"\]"
# Expected: 0 hunks (the existing :584-588 block stays untouched)
```

### Phase 2 G-* contract preservation (carry-forward audit)

**Source:** `02-PATTERNS.md` Phase 2 G-01..G-11 closures

**Apply to:** Wave 2 close plan. After densification, the post-Wave-1 build must still satisfy every G-* contract. The golden test catches structural drift but the planner should also explicitly verify:

| Phase 2 contract | Verification command for Wave 2 close |
|---|---|
| G-01 (Hero coral glow) | `grep -c "rgba(213,121,89,0.4)" src/sections/Hero.tsx` → expect 1 |
| G-03 (Catalog filter pills) | `grep -nE "borderRadius:\s*999" src/sections/Catalog.tsx` → expect 1 hit on :39 |
| G-06 (Reviews stars coral) | `grep -c "var(--coral)" src/sections/Reviews.tsx` → expect ≥1 |
| G-08 (font-display: swap) | `grep -c "font-display:\s*swap" src/styles/global.css` → expect ≥5 |
| G-09 (FeedStrip null guard) | `grep -nE "state\.feed\.length === 0" src/sections/FeedStrip.tsx` → expect 1 |
| G-11 (.container :599 padding 0 20px) | `grep -nE "padding:\s*0\s*20px\s*!important" src/styles/global.css` → expect 1 |
| R-5 (Footer aria-labels) | `grep -c 'aria-label="Telegram"' src/sections/Footer.tsx` → expect 2; same for WhatsApp |

---

## No Analog Found

**None.** Every file Phase 02.1 modifies is a file Phase 2 already shipped or worked with. Phase 02.1 is a tightening pass on a known stable surface — there are no green-field components.

---

## Pattern Lock Summary (for the planner)

When constructing each plan's `<read_first>` and `<action>` blocks, reference these analog files with line numbers:

| Plan | Required reads (`<read_first>`) | Verbatim-paste sources |
|------|-------------------------------|------------------------|
| **Wave 1 — P1 (global.css padding)** | `src/styles/global.css:230-240` (the section rule + mobile gate); `02.1-CONTEXT.md` D-02 + D-04a; `02.1-RESEARCH.md` §2 + §5; `02.1-PATTERNS.md` `### src/styles/global.css:234` | Single-numeric swap at line 234: `140px` → `72px` (or `80px`) |
| **Wave 1 — P2 (Hero min-height + paddingTop)** | `src/sections/Hero.tsx:1-140` (full file; inline-style context + G-01 preservation); `02.1-CONTEXT.md` D-02a; `02.1-RESEARCH.md` §2 + §5 + R-6 + R-8; `02.1-PATTERNS.md` `### src/sections/Hero.tsx:37`; `02-PATTERNS.md` `### src/sections/Hero.tsx (MODIFY — G-01)` for G-01 verbatim | Inline-style edit at line 37: `minHeight: '100vh'` → `minHeight: 640`, `paddingTop: 140` → `paddingTop: 72`. Optional: line 125 `marginTop: 96 → 56`, `paddingTop: 32 → 20` |
| **Wave 1 — P3a (Catalog within-section)** | `src/sections/Catalog.tsx:1-117` (full file); `02.1-CONTEXT.md` D-02b; `02.1-RESEARCH.md` §7 Offender #1; `02.1-PATTERNS.md` `### src/sections/Catalog.tsx:26, 107` | Two single-property edits: `:26 marginBottom: 56 → 32`, `:107 marginTop: 64 → 32` |
| **Wave 1 — P3b (Process within-section)** | `src/sections/Process.tsx:1-89` (full file); `02.1-CONTEXT.md` D-02b; `02.1-RESEARCH.md` §7 Offender #2; `02.1-PATTERNS.md` `### src/sections/Process.tsx:17, 34` | Two single-property edits: `:17 marginBottom: 64 → 32`, `:34 padding: '28px 0' → '18px 0'` |
| **Wave 1 — P3c (Founders within-section)** | `src/sections/Founders.tsx:1-68` (full file); `02.1-CONTEXT.md` D-02b; `02.1-RESEARCH.md` §7 Offender #3; `02.1-PATTERNS.md` `### src/sections/Founders.tsx:14, 25` | Two single-property edits: `:14 gap: 80 → 56`, `:25 marginBottom: 48 → 28`. **Optional plan** — de-prioritize if budget tight |
| **Wave 2 — P4 (threshold re-tune + close)** | `server/tests/landing-page-golden.test.ts:1-97` (full file; for header comment + line 20 context); `02.1-CONTEXT.md` D-03 + D-03a + D-03b; `02.1-RESEARCH.md` §1 + §6; `02.1-VALIDATION.md`; `02.1-PATTERNS.md` `### server/tests/landing-page-golden.test.ts:20` | Empirical re-measurement → single-constant edit at line 20 + header-comment update at lines 1-4. Then run `pnpm test landing-page-golden.test.ts` |

---

## Metadata

**Analog search scope:**
- `src/styles/global.css` (1 file — the canonical CSS source)
- `src/sections/` (4 files directly: `Hero.tsx`, `Catalog.tsx`, `Process.tsx`, `Founders.tsx`; siblings cross-referenced for inline-style convention: `LeadMagnet.tsx`, `Reviews.tsx`, `Marquee.tsx`, `FeedStrip.tsx`, `Faq.tsx`, `Footer.tsx`)
- `server/tests/` (1 file: `landing-page-golden.test.ts`)
- `.planning/phases/02-redesign-from-screenshot/02-PATTERNS.md` (cross-phase precedent for the same files)

**Files scanned:** 7 (within scope of Phase 02.1 file list) + 1 cross-phase patterns doc.

**Pattern extraction date:** 2026-05-07

**Confidence:** HIGH — every code excerpt verified against live files this session; every line number cross-referenced with RESEARCH.md §3 + §7 paste-ready data; Phase 2 G-* preservation rules carried forward verbatim from `02-PATTERNS.md`.

**Honest tradeoff surfaced (RESEARCH.md §5 Option C):** All five Wave 1 patterns combined land at predicted ~7 741 px (real-device) — within D-01's locked 7 000–8 000 band. Pattern is sound; planner does not need to push deeper unless founder authorizes additional within-section cuts (Option D) at Wave 2 close.

---

## PATTERN MAPPING COMPLETE
