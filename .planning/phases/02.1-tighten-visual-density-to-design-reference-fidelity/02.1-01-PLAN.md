---
phase: 02.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
autonomous: true
requirements: []
decisions_addressed:
  - D-02
must_haves:
  truths:
    - "D-01: post-densification page height target 7000–8000 px (acceptable band 6500–8500 per D-01a). NO section consolidation, NO section dropping, NO new sections (D-01b)."
    - "D-02: canonical desktop section padding cut from 140px 0 to 72px 0 at src/styles/global.css:234. Value 72 is on the locked 4-px spacing scale (72 = 9 × 8)."
    - "D-02b: NO global tighten-everything-by-N% passes; this plan is one targeted CSS-token swap."
    - "D-04a: mobile @media (max-width: 720px) blocks at global.css:235, :567+, :584-588, :599 are OFF-LIMITS. Plan touches line 234 ONLY."
    - "D-04b: single revertable commit; one file, one logical change."
    - "Phase 2 G-11 mobile padding 0 20px !important at global.css:599 MUST be preserved (untouched here — only line 234 changes, but verified by grep)."
    - "No new component library, no shadcn init, no CSS-in-JS, no Tailwind, no new font weights, no new tokens."
  artifacts:
    - path: src/styles/global.css
      provides: "Canonical desktop section vertical padding rule (72px 0)"
      contains: "section { padding: 72px 0; position: relative; }"
  key_links:
    - from: "src/styles/global.css:234"
      to: "Catalog/Process/Founders/LeadMagnet/Reviews/Faq sections (no inline padding override)"
      via: "bare `section` selector cascade"
      pattern: "^section \\{ padding: 72px 0"
---

<objective>
Cut canonical desktop section vertical padding in `src/styles/global.css` from `padding: 140px 0` to `padding: 72px 0` (per D-02). Single-line edit. Cascades to 6 sections (#catalog, #process, #founders, #lead-magnet, #reviews, #faq) — predicted savings ≈ 6 × 136 = 816 px on real-device viewport (RESEARCH.md §5 Lever 1). Mobile breakpoint blocks (line 235, the `:567+` block, the `:584-588` hero override, the `:599` G-11 container override) MUST remain untouched per D-04a.

Purpose: Largest single-token change in Phase 02.1 — the foundation lever for moving page height from current ~8 927 px (real-device) toward the 7 000–8 000 px target band.
Output: Modified `src/styles/global.css` with line 234 cut to 72px 0; mobile gates intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-CONTEXT.md
@.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-RESEARCH.md
@.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-PATTERNS.md
@.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-VALIDATION.md
@.planning/phases/02-redesign-from-screenshot/02-UI-SPEC.md
@src/styles/global.css

<interfaces>
<!-- The single rule being modified, with the surrounding context that MUST stay byte-identical. -->

From src/styles/global.css (lines 232-235, current state verified 2026-05-07):
```css

.card:hover {
  transform: translateY(-6px);
  border-color: var(--line-strong);
  background: var(--ink-3);
}

section { padding: 140px 0; position: relative; }
@media (max-width: 720px){ section { padding: 80px 0; } }
```

After this plan (lines 232-235):
```css

.card:hover {
  transform: translateY(-6px);
  border-color: var(--line-strong);
  background: var(--ink-3);
}

section { padding: 72px 0; position: relative; }
@media (max-width: 720px){ section { padding: 80px 0; } }
```

Lines 232-233 (the `.card:hover` rule), line 235 (mobile gate), and every other line in the file (including `:599` G-11 container override) MUST be byte-identical before/after.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cut section padding 140px 0 → 72px 0 at global.css:234</name>

  <read_first>
    - `src/styles/global.css` (lines 230-240) — the rule + adjacent context
    - `.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-CONTEXT.md` `<decisions>` D-02, D-02b, D-04a, D-04b
    - `.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-RESEARCH.md` §2 (Section Padding Selector — quoted lines), §5 (Lever 1 math)
    - `.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-PATTERNS.md` `### src/styles/global.css:234 — Section padding cut`
    - `.planning/phases/02-redesign-from-screenshot/02-UI-SPEC.md` §Spacing Scale (the locked 4-px base + 8/12/16/20/24/28/32/40/56/72/96 multiples)
  </read_first>

  <files>src/styles/global.css</files>

  <action>
    Open `src/styles/global.css`. Replace **only** line 234 in place.

    Before (line 234, exactly as present 2026-05-07):
    ```css
    section { padding: 140px 0; position: relative; }
    ```

    After (line 234):
    ```css
    section { padding: 72px 0; position: relative; }
    ```

    Rules:
    - `72` is selected per D-02 (`72px` is on the locked spacing scale — `72 = 9 × 8`, multiple of 4) and per RESEARCH.md §5: `64px` is acceptable but already the mobile `!important` floor at line 599 — converging desktop and mobile would erase the responsive rhythm distinction; `80px` falls short of the 7000–8000 target. `72px` is the recommended balance.
    - Do NOT change the rule's structure: `position: relative;` MUST remain (required for absolutely-positioned section children, e.g., FeedStrip background).
    - Do NOT modify line 235 (`@media (max-width: 720px){ section { padding: 80px 0; } }`) — D-04a explicit anti-edit.
    - Do NOT touch any line in the `@media (max-width: 720px)` block starting at line 567 (mobile container/typography overrides) or lines 584-588 (mobile hero `header[class*="grain"]` override) or line 599 (G-11 `.container { padding: 0 20px !important; }`) — D-04a + Phase 2 G-11 contract.
    - Do NOT introduce a new spacing token (e.g., `74px`, `68px`) — D-02 + UI-SPEC §Pre-existing System Note.
    - This is a one-line surgical edit. The `<header>` (Hero), `Marquee.tsx`, `FeedStrip.tsx`, and `Footer.tsx` have inline padding overrides and are NOT affected by this rule (per RESEARCH.md §2 audit).
  </action>

  <verify>
    <automated>
grep -nE '^section \{ padding: 72px 0; position: relative; \}$' src/styles/global.css \
  &amp;&amp; grep -cE 'padding: 140px 0' src/styles/global.css | grep -qx 0 \
  &amp;&amp; grep -nE '^@media \(max-width: 720px\)\{ section \{ padding: 80px 0; \} \}$' src/styles/global.css \
  &amp;&amp; grep -nE '^  \.container \{ padding: 0 20px !important; \}$' src/styles/global.css \
  &amp;&amp; pnpm test landing-page-golden.test.ts 2>&amp;1 | tail -20
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -nE '^section \{ padding: 72px 0; position: relative; \}$' src/styles/global.css` returns exactly one line (line 234).
    - `grep -cE 'padding: 140px 0' src/styles/global.css` returns 0.
    - `grep -nE '^@media \(max-width: 720px\)\{ section \{ padding: 80px 0; \} \}$' src/styles/global.css` returns one line (line 235 mobile gate untouched, D-04a).
    - `grep -nE '^  \.container \{ padding: 0 20px !important; \}$' src/styles/global.css` returns one line (line 599 G-11 mobile container preserved).
    - `git diff src/styles/global.css | grep -cE '^[+-]'` returns exactly 2 (one `-` and one `+`, single-line surgical change).
    - `git diff src/styles/global.css | grep -E '^[+-].*@media|max-width: 720'` returns 0 lines (no mobile breakpoint hunks per D-04a).
    - `pnpm test landing-page-golden.test.ts` may FAIL because the golden's threshold is still 0.22 against pre-densification baseline; the FAILURE message must show diff ratio LOWER than the previous 19.57% (densification improving the floor, per D-03b safety check). The test threshold is re-tuned in plan 02.1-06.
  </acceptance_criteria>

  <done>
    - `src/styles/global.css` line 234 reads exactly `section { padding: 72px 0; position: relative; }`.
    - Lines 235, 567+, 584-588, 599 byte-identical to pre-edit state.
    - Total diff: one line removed, one line added, in `src/styles/global.css` only.
    - The golden test, when run, prints a diff ratio LOWER than 19.57% (Phase 2 close-out value) — empirical evidence that densification is moving the floor in the right direction. Test still asserts at 0.22 threshold (re-tune deferred to plan 02.1-06).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Frontend layout values only — no new external IO, no new auth surface, no new user-controlled inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-01 | (n/a) | src/styles/global.css | accept | No new attack surface — frontend layout values only; no new external IO; no new secrets; no new user-controlled inputs. CSS edits cannot change auth posture or data flows. |
</threat_model>

<verification>
- `grep -nE '^section \{ padding: 72px 0; position: relative; \}$' src/styles/global.css` → 1 hit on line 234
- `grep -cE 'padding: 140px 0' src/styles/global.css` → 0
- `grep -nE '^@media \(max-width: 720px\)\{ section \{ padding: 80px 0; \} \}$' src/styles/global.css` → 1 hit on line 235 (D-04a)
- `git diff src/styles/global.css` shows exactly one `-` line and one `+` line
- `git diff src/styles/global.css | grep -E '^[+-].*@media|max-width: 720'` → 0 lines
- `pnpm test landing-page-golden.test.ts` runs to completion (may fail or pass against the still-at-0.22 threshold; expected behavior is a LOWER diff ratio than 19.57%)
</verification>

<success_criteria>
- Single-line CSS-token swap landed at `src/styles/global.css:234` (`140px` → `72px`)
- Mobile breakpoint blocks (line 235, line 567+, lines 584-588, line 599) byte-identical
- Phase 2 G-11 container mobile override at line 599 preserved
- No new tokens, selectors, rules, or media queries introduced
- Golden test diff ratio drops below 19.57% post-edit (densification working)
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-tighten-visual-density-to-design-reference-fidelity/02.1-01-SUMMARY.md` recording: the exact line/value changed, before/after diff ratio when running the golden test (still at 0.22 threshold), confirmation that mobile gates were not touched, and confirmation that lines 235/567+/584-588/599 are byte-identical to pre-edit state.
</output>
