---
plan: 01.2-05
status: deferred
deferred_to: 01.2-06 (live re-scrape)
deferred_at: 2026-05-03
deferred_reason: operator-confirmed
---

# Plan 01.2-05 — DEFERRED

## Decision

Operator declined the manual hybrid X5 fixture capture. Replied "Parse all comps of x5" and confirmed: *Skip Plan 05 — the live re-scrape covers it.*

## Rationale

Plan 06 (Wave 3) does a full BMW X5 backfill (~2000 polite-delayed requests, ~5–7h, ~38 generations × ~12 trims/gen). That run will produce a complete dataset including all current hybrid trims (xDrive45e, xDrive50e). A fixture for unit tests can be extracted from the live snapshot post-run if Plan 02's parser exhibits hybrid-typed-slot regressions.

## Impact on downstream plans

- **Plan 02 (parser):** Hybrid-typed-slot assertions (`drivetrain.hybrid_type`, `drivetrain.battery_capacity_kwh`, `drivetrain.electric_range_km`) cannot be unit-tested against a fixture. Plan 02 was dispatched with weakened test guidance — assertions become *"if the value is non-null on a fixture row, it must match the expected type and units"*; the existence of a hybrid-trim fixture is no longer required.
- **Plan 04 (coverage gate):** `features_density >= 50` floor is unaffected (does not depend on hybrid coverage; ICE fixtures already have ~140+ features).
- **Plan 06 (live re-scrape):** Becomes the de-facto integration test for hybrid-typed-slot extraction. Verification step in Plan 06 grep-asserts on `report.json` for at least one trim with `hybrid_type !== null`.

## Follow-up

After Plan 06 lands, optionally capture a hybrid fixture from `data/scraped/drom/current/<plug-in-trim>.html` (or by re-running the scraper with `--capture-fixture <comp_id>` against a known hybrid comp_id from the new run). Track as a quick task post-phase, not blocking.
