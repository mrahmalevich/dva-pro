---
phase: 260503-j62
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/scrapers/shared/report-html.ts
  - server/scrapers/drom/parse-complectation-page.ts
  - server/scrapers/shared/coverage.ts
  - data/scraped/drom/2026-05-03T05-05-16Z/index.html
autonomous: true
requirements:
  - QUICK-260503-j62
---

<objective>
Improve the drom HTML viewer so reviewers can drill into per-complectation detail (Task 1), see all model-level info correctly laid out — including the broken engine display (Task 2), and resolve the pricing=0.08 coverage failure recorded in `data/scraped/drom/2026-05-03T05-05-16Z/report.json` (Task 3).

Purpose: The viewer is the operator-facing source of truth for QA-ing scraped catalog data. Today the comp grid is a static, 3-field summary and the engine block on the model panel renders blanks because of a schema-vs-renderer mismatch (`e.label / e.volume_l / e.power_hp / e.transmission` vs the actual `{cc, hp, fuel}` shape). The pricing coverage gate is also failing at 0.08, blocking confidence in the dataset.

Output:
- Updated `server/scrapers/shared/report-html.ts` with clickable comp cards, a new `openCompModal()` flow, fixed engine rendering, and a reorganized model-detail layout.
- Either a parser fix in `server/scrapers/drom/parse-complectation-page.ts` (H1) or a justified threshold tune in `server/scrapers/shared/coverage.ts` (H3), with a regenerated `index.html` in the existing run dir.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@server/scrapers/shared/report-html.ts
@server/scrapers/shared/types.ts
@server/scrapers/shared/coverage.ts
@server/scrapers/drom/parse-complectation-page.ts

<interfaces>
<!-- Authoritative shapes the executor must render. Extracted from server/scrapers/shared/types.ts. -->
<!-- DO NOT re-explore — these are the contracts. -->

ModelRecord (top-level fields used by the modal):
- brand, brand_slug, model, model_slug: string
- generation: string
- year_from, year_to: number | null
- body_types: string[]
- engine_options: Array<{ cc: number; hp: number; fuel: 'gas' | 'diesel' | 'hybrid' | 'electric' }>
  * NOTE: current renderer reads e.label/e.volume_l/e.power_hp/e.transmission — ALL UNDEFINED. This is the Task 2 bug.
- drive_options: string[]
- description_ru: string
- price_min_rub, price_max_rub: number | null
- image_paths: string[]
- source: 'drom-catalog'
- source_url: string (URL)
- scraped_at: string (ISO datetime)
- complectations: Complectation[]  (default [])

Complectation (six groups + optional errors):
- identity:    { name, period_from, period_to, tier ('Базовая'|'Предмаксимальная'|'Максимальная'),
                 engine_code, frame_code, source_url } — all string|null (source_url is URL|null)
- pricing:     { price_new_rub, price_used_from_rub } — number|null
- drivetrain:  { engine_cc, engine_hp,
                 engine_fuel ('gas'|'diesel'|'hybrid'|'electric'),
                 drive: string|null,
                 transmission_type ('AT'|'MT'|'CVT'|'AMT'),
                 transmission_gears: int } — all nullable
- dimensions:  { length_mm, width_mm, height_mm, wheelbase_mm, clearance_mm,
                 trunk_min_l, trunk_max_l, curb_weight_kg, gross_weight_kg } — all number|null
- comfort:     { seats: int, doors: int,
                 fuel_consumption_city_l, fuel_consumption_highway_l, fuel_consumption_combined_l,
                 tank_l: number } — all nullable
- tires:       { tires_front, tires_rear } — string|null
- _extraction_errors?: Array<{ group: string; message: string }>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clickable complectations + comp-detail modal</name>
  <files>server/scrapers/shared/report-html.ts</files>
  <action>
Edit the embedded HTML/JS inside `renderHtml()`. Do NOT touch `writeReportHtml()`, `escapeHtml`, `formatDuration`, `formatDate`, or the data-loading code.

1. CSS additions (inside the `<style>` block, after the existing `.modal a.source-link:hover` rule, before `</style>`):
   - `.comp-card`: same visual language as `.card` (background var(--panel), 1px border var(--border), border-radius 10px, cursor pointer, padding 12px, display flex column, gap 6px, text-align left, font/color inherit, transition border-color/transform 120ms).
   - `.comp-card:hover`: border-color var(--accent); transform: translateY(-1px).
   - `.comp-card:focus-visible`: outline 2px solid var(--accent); outline-offset 2px.
   - `.comp-grid`: display grid; grid-template-columns: repeat(auto-fill, minmax(220px, 1fr)); gap 12px.
   - `.comp-back`: same look as `.modal-close` but auto width with text padding (`padding: 4px 12px; border-radius: 6px; width: auto; font-size: 13px; margin-right: 8px;`).
   - `.comp-topbar`: position sticky; top 0; background var(--panel); padding 12px; display flex; align-items center; justify-content space-between; border-bottom 1px solid var(--border); z-index 1.
   - `.extraction-errors`: background rgba(210,153,34,0.08); border 1px solid var(--warn); color var(--warn); border-radius 8px; padding 10px 14px; margin-top 16px; font-size 12px; font-family ui-monospace, monospace; white-space pre-wrap.

2. JS — module-scoped state. Inside the `<script>` block, immediately after `const models = data.models;`, add:
   ```js
   let currentModel = null;  // set when a model modal opens; lets the comp-back button reopen it
   ```

3. JS — replace the static comp grid (current lines ~447-473 inside `renderModalBody`) with clickable buttons rendered as a placeholder `<div id="comp-grid-${slug}">`, BUT prefer the simpler approach: keep the comp grid generation inline in `renderModalBody` BUT switch each card to a `<button class="comp-card" data-comp-index="${i}" type="button">` and stop using inline `style="cursor: default"`. Each button keeps the existing 3 facts (tier, name, price pill, length, seats, tires_front).

4. JS — wire the click handlers. Because `renderModalBody` returns an HTML string (not DOM nodes), do the wiring in `openModal(m)` AFTER `modalContent.innerHTML = renderModalBody(m)`:
   ```js
   currentModel = m;
   const compButtons = modalContent.querySelectorAll('.comp-card[data-comp-index]');
   compButtons.forEach(btn => {
     const idx = parseInt(btn.dataset.compIndex, 10);
     btn.addEventListener('click', () => openCompModal(m, m.complectations[idx]));
   });
   ```
   Set `currentModel = m` at the top of `openModal`. Set `currentModel = null` inside `closeModal()`.

5. JS — add `openCompModal(model, comp)` function (place after `closeModal`). It renders into the SAME `#modal-content` (no second backdrop):
   - Sticky topbar div with class `comp-topbar`: a "← Назад к модели" button (class `comp-back`) on the left, "×" close button (class `modal-close`, sized normally) on the right. The back button calls `openModal(model)` (which re-renders the model view AND re-wires comp clicks via step 4). The close button calls `closeModal()`.
   - `<div class="modal-body">` containing:
     a. `<h2 class="modal-title">${esc(comp.identity.name || '?')}</h2>`
     b. If `comp.identity.tier`: `<div class="modal-subtitle"><span class="badge">${esc(comp.identity.tier)}</span></div>`
     c. Six `<div class="modal-section"><h3>${title}</h3><dl class="kv">…</dl></div>` blocks. Build each via a small helper `renderKvSection(title, rows)` where rows is `Array<[label, value]>` and the helper SKIPS rows whose value is null/undefined/'' AND skips the entire section if zero rows survive.
     d. Sections (in order, with formatters):
        - `Идентификация`:
          • Название → identity.name
          • Период → if both period_from/period_to: `${period_from} – ${period_to}`; else either alone; else skip
          • Тариф → identity.tier
          • Код двигателя → identity.engine_code
          • Код кузова → identity.frame_code
          • Источник → if identity.source_url: `<a href="${esc(url)}" target="_blank" rel="noopener">${esc(url)}</a>` (raw HTML, NOT esc'd around the anchor — pass through `renderKvSection` with a flag or just inline this row)
        - `Цена`:
          • Новая → pricing.price_new_rub formatted via `formatPrice(price_new_rub, null)`
          • Б/у от → pricing.price_used_from_rub formatted via `formatPrice(price_used_from_rub, null)`
          (entire section auto-skipped if both null)
        - `Двигатель и привод`:
          • Объём → drivetrain.engine_cc → `${cc} см³`
          • Мощность → drivetrain.engine_hp → `${hp} л.с.`
          • Топливо → drivetrain.engine_fuel → fuelRu(fuel)  (helper from Task 2)
          • Привод → drivetrain.drive
          • Коробка → drivetrain.transmission_type → e.g. `AT`
          • Передач → drivetrain.transmission_gears
        - `Размеры`:
          • Длина/Ширина/Высота → length_mm/width_mm/height_mm → `${n} мм`
          • Колёсная база → wheelbase_mm → `${n} мм`
          • Клиренс → clearance_mm → `${n} мм`
          • Багажник (мин) → trunk_min_l → `${n} л`
          • Багажник (макс) → trunk_max_l → `${n} л`
          • Снаряжённая масса → curb_weight_kg → `${n} кг`
          • Полная масса → gross_weight_kg → `${n} кг`
        - `Комфорт`:
          • Места → comfort.seats
          • Двери → comfort.doors
          • Расход (город) → fuel_consumption_city_l → `${n} л/100км`
          • Расход (трасса) → fuel_consumption_highway_l → `${n} л/100км`
          • Расход (смешанный) → fuel_consumption_combined_l → `${n} л/100км`
          • Бак → tank_l → `${n} л`
        - `Шины`:
          • Передние → tires.tires_front
          • Задние → tires.tires_rear
   - If `comp._extraction_errors?.length > 0`: append an `<div class="extraction-errors">` listing each `${err.group}: ${err.message}`, one per line.
   - After setting `modalContent.innerHTML = …`, focus the back button so ESC/Tab work naturally.

6. JS — ensure existing modal close mechanisms still work for the comp view: the backdrop click handler on `#modal` and the global ESC keydown both call `closeModal`, which already clears `modalContent.innerHTML`. NO CHANGE NEEDED — verify by tracing, do not duplicate.

7. TypeScript correctness: this file is a `.ts` file but the script block is a string literal — there are no TS type changes here. Just keep `tsc` happy by not introducing `any` in any helper function added at the TS level (none expected — all new code lives in the embedded JS string).
  </action>
  <verify>
    <automated>pnpm typecheck:server</automated>
  </verify>
  <done>
- `pnpm typecheck:server` exits 0.
- `grep -c 'comp-card' server/scrapers/shared/report-html.ts` returns >= 4 (CSS rule, button class, querySelector, helper).
- `grep -c 'openCompModal' server/scrapers/shared/report-html.ts` returns >= 2 (definition + call site).
- `grep -c 'currentModel' server/scrapers/shared/report-html.ts` returns >= 3 (declaration + assignment in openModal + reset in closeModal).
- No `style="cursor: default"` on comp cards anymore: `grep "cursor: default" server/scrapers/shared/report-html.ts` returns 0 matches.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix engine display bug + reorganize model-detail layout</name>
  <files>server/scrapers/shared/report-html.ts</files>
  <action>
Continue editing the same file. The bug: lines ~437-444 read `e.label`, `e.volume_l`, `e.power_hp`, `e.transmission` — none of which exist on the `engine_options` schema. Actual shape per types.ts L80-83: `{ cc: number; hp: number; fuel: 'gas'|'diesel'|'hybrid'|'electric' }`.

1. JS — add `fuelRu(fuel)` helper (place near the existing `esc` and `formatPrice` helpers in the script block):
   ```js
   function fuelRu(f) {
     return ({ gas: 'бензин', diesel: 'дизель', hybrid: 'гибрид', electric: 'электро' })[f] || f || '';
   }
   ```
   This helper is also consumed by Task 1's drivetrain section.

2. JS — fix the engine block in `renderModalBody`. Replace the entire engine_options rendering (the `if (m.engine_options && m.engine_options.length)` block) with:
   ```js
   if (m.engine_options && m.engine_options.length) {
     html += '<div class="modal-section"><h3>Двигатели (' + m.engine_options.length + ')</h3><dl class="kv">';
     for (const e of m.engine_options) {
       const dt = (e.cc != null ? e.cc + ' см³' : '—');
       const dd = [e.hp != null ? e.hp + ' л.с.' : null, fuelRu(e.fuel)].filter(Boolean).join(' · ') || '—';
       html += '<dt>' + esc(dt) + '</dt><dd>' + esc(dd) + '</dd>';
     }
     html += '</dl></div>';
   }
   ```
   No references to `e.label`, `e.volume_l`, `e.power_hp`, or `e.transmission` may remain.

3. JS — reorganize `renderModalBody(m)` body order. Keep all existing helpers; only change the order in which sections are appended. Final order INSIDE the `<div class="modal-body">`:
   1. Brand line (existing).
   2. Title h2 (existing).
   3. Subtitle (existing — generation · years).
   4. NEW "Обзор" card: a `<div class="modal-section">` containing:
      - badges-row: existing body_types badges + price-pill if `priceSummary` (move the existing badges block here).
      - `<dl class="kv">` with rows (each row only emitted if its source array length > 0):
        • `Комплектаций` → `${m.complectations.length}` (only if > 0)
        • `Двигателей` → `${m.engine_options.length}` (only if > 0)
        • `Привод` → `${m.drive_options.join(', ')}` (only if > 0)
   5. Описание (existing — only if `m.description_ru`).
   6. Двигатели — fixed renderer from step 2.
   7. Комплектации — clickable grid from Task 1 (already in this file after Task 1).
   8. Метаданные (existing — keep entire block as-is).
   9. Изображения / Дополнительные изображения (existing gallery).
   10. Source link (existing).

   The standalone "Привод" section that exists today (`if (m.drive_options && m.drive_options.length)`) MUST be removed — its content now lives inside the "Обзор" card.

4. Because the body_types badges and price-pill move into the "Обзор" card, REMOVE the standalone `if (badges.length) { html += '<div class="modal-section badges-row">' + … }` block — it was the previous home of those badges.
  </action>
  <verify>
    <automated>pnpm typecheck:server &amp;&amp; ! grep -E 'volume_l|power_hp|e\.label|e\.transmission' server/scrapers/shared/report-html.ts</automated>
  </verify>
  <done>
- `pnpm typecheck:server` exits 0.
- `grep -c 'fuelRu' server/scrapers/shared/report-html.ts` returns >= 3 (definition + two call sites — engine block + drivetrain section in Task 1).
- `grep -E 'volume_l|power_hp|e\.label|e\.transmission' server/scrapers/shared/report-html.ts` returns ZERO matches.
- `grep -c 'Обзор' server/scrapers/shared/report-html.ts` returns >= 1.
- The standalone "Привод" `<h3>Привод</h3>` heading is gone (its data is in the Обзор kv list now): `grep -c '>Привод<' server/scrapers/shared/report-html.ts` returns 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: Investigate + resolve pricing=0.08 coverage failure, then regenerate index.html</name>
  <files>
server/scrapers/drom/parse-complectation-page.ts
server/scrapers/shared/coverage.ts
data/scraped/drom/2026-05-03T05-05-16Z/index.html
  </files>
  <action>
Hypothesis-driven investigation. Run H1 first, then H2 always (sanity gate), then H3 only if H1 is FALSE.

== H1 — Parser bug? ==

(a) Read both files first, fully:
    - `server/scrapers/drom/parse-complectation-page.ts`
    - `server/scrapers/shared/coverage.ts`
   Locate the pricing extractor (the function that populates `pricing.price_new_rub` / `pricing.price_used_from_rub`). Note the CSS selectors / regex it relies on.

(b) Pick three comp_ids to spot-check. Use jq on the existing run's models.json:
    ```bash
    jq -r '
      .[]
      | select((.year_to == null) or (.year_to >= 2024))
      | .complectations[]
      | select(.pricing.price_new_rub == null and .identity.source_url != null)
      | .identity.source_url
    ' data/scraped/drom/2026-05-03T05-05-16Z/models.json | head -3
    ```
    Capture the three URLs.

(c) Capture HTML fixtures via the existing CLI (see `server/scrapers/cli.ts` lines ~25-42 for the `--capture-fixture` flag — confirm exact flag name by reading those lines):
    ```bash
    pnpm scrape drom --capture-fixture <comp_id_or_url>
    ```
    If the flag/argument shape differs from the description above, USE WHAT THE CLI ACTUALLY ACCEPTS — read the source, do not invent.

(d) For each captured fixture, grep the raw HTML:
    ```bash
    grep -i -E 'цена|price|руб|₽' server/tests/fixtures/drom/complectation/<id>.html | head -20
    ```
    Decision rule:
    - If the live HTML clearly contains a "от X ₽" / "Цена нового" / "Цена с пробегом от" block AND our extractor missed it → **H1 CONFIRMED**.
    - If the HTML lacks any such block on all 3 fixtures → H1 REJECTED, proceed to H2/H3.

(e) If H1 confirmed: fix the extractor in `parse-complectation-page.ts`. Add the missing selector(s). Re-run on the 3 fixtures via whatever unit-test or repl mechanism exists (check `server/scrapers/drom/__tests__/` or `parse-complectation-page.test.ts` if present — add a fixture-based test if one already exists in that style; do NOT add a new test framework). After fix, re-compute coverage by re-running the scraper against just those URLs (or, if expensive, manually re-parse via a one-off `pnpm exec tsx -e "…"` snippet that calls the extractor on the fixture HTML).

== H2 — Schema/legacy distribution check (always run) ==

```bash
jq -r '
  [
    .[]
    | . as $m
    | .complectations[]
    | { has_price: (.pricing.price_new_rub != null),
        year_to: $m.year_to,
        in_production: ($m.year_to == null) }
  ]
  | group_by(.in_production)
  | map({ in_production: .[0].in_production,
          total: length,
          priced: ([.[] | select(.has_price)] | length),
          ratio: (([.[] | select(.has_price)] | length) / length) })
' data/scraped/drom/2026-05-03T05-05-16Z/models.json
```

Record the output in the SUMMARY (Task done step). Expected H2 signal: priced ratio is much higher for `in_production: true` rows (e.g., >0.5) than for legacy gens (~0). This proves drom only carries pricing for current-production gens.

== H3 — Threshold tune (only if H1 FALSE) ==

If H1 was rejected and H2 confirms structural absence of legacy pricing:
- Open `server/scrapers/shared/coverage.ts`. Locate the `pricing` threshold constant.
- Compute the new floor from H2 numbers: take `priced(in_production=true) / total(in_production=true)`, multiply by ~0.8 for safety margin, round down to a clean value (e.g., 0.30, 0.25). Pick a value that the CURRENT 0.08 dataset would NOT pass but a healthy one (>=80% of in-production gens priced) WOULD pass.
- Wait — re-read: the gate is computed ACROSS ALL comps, not just in-production. So the new threshold must be ≥ the actual ratio across all comps in a healthy dataset, but ≤ what the 0.08 failure case represents. If after H2 you find the realistic ratio is ~0.10-0.15 (because most comps are legacy), set the threshold to 0.08 → 0.10 floor and add a comment that explains drom's behavior. If realistic ratio is ~0.25 set threshold to 0.20.
- Make ONE change: update only the `pricing` threshold. Do NOT touch identity/drivetrain/dimensions/comfort/tires.
- Add a code comment ABOVE the changed line: `// drom catalog only publishes pricing for in-production generations; legacy gens have null pricing by design (see PLAN 260503-j62 H2 analysis).`

== Regenerate the viewer ==

After whichever fix lands (H1 OR H3), regenerate the run's HTML so the user can inspect both the new viewer (Tasks 1+2) and the new pricing situation:

```bash
pnpm exec tsx scripts/generate-report-html.mjs data/scraped/drom/2026-05-03T05-05-16Z
```

(If that script path is slightly different, find it: `ls scripts/ | grep -i report`; it is referenced by `server/scrapers/shared/report-html.ts:7`.)

== Commit ==

Single commit at the end of this task, with type chosen by outcome:
- H1 confirmed: `fix(260503-j62): correct drom pricing extractor — was missing <selector>`
- H3 only:     `fix(260503-j62): tune pricing coverage threshold to <X> (drom prices in-production gens only)`

Commit body MUST include a one-liner summarizing the new pricing coverage % expected after the change (re-run `jq` on a fresh models.json if H1, or just cite the H2 ratio if H3).

DO NOT delete or mutate `data/scraped/drom/2026-05-03T05-05-16Z/models.json` or `report.json`. Only `index.html` in that dir is overwritten by the regen script.
  </action>
  <verify>
    <automated>pnpm typecheck:server &amp;&amp; test -f data/scraped/drom/2026-05-03T05-05-16Z/index.html &amp;&amp; test "$(wc -c < data/scraped/drom/2026-05-03T05-05-16Z/index.html)" -gt 102400 &amp;&amp; grep -q 'comp-card' data/scraped/drom/2026-05-03T05-05-16Z/index.html &amp;&amp; grep -q 'fuelRu' data/scraped/drom/2026-05-03T05-05-16Z/index.html</automated>
  </verify>
  <done>
- `pnpm typecheck:server` exits 0.
- H1/H2 evidence captured in commit message body (which selector / which ratio).
- Either parser fix OR coverage threshold change committed (NOT both — H1 fix obviates H3).
- Regenerated `data/scraped/drom/2026-05-03T05-05-16Z/index.html` exists, is > 100 KB, contains both `comp-card` (Task 1 wired) and `fuelRu` (Task 2 wired) substrings.
- `data/scraped/drom/2026-05-03T05-05-16Z/models.json` and `report.json` are unchanged on disk (no diff from HEAD for those two files).
  </done>
</task>

</tasks>

<verification>
After all three tasks:
- `pnpm typecheck:server` passes (run once at the end).
- Three atomic commits exist on the branch in this order: feat (Task 1), feat-or-fix (Task 2 — single combined commit since both touch the same modal renderer; commit type `feat(260503-j62): improve drom HTML viewer — clickable comps + fixed engine display + reorganized layout`), fix-or-chore (Task 3, type per outcome rule).
- Manual smoke (operator): open `data/scraped/drom/2026-05-03T05-05-16Z/index.html` in a browser. Click a model card → modal opens with Обзор/Описание/Двигатели/Комплектации/Метаданные/Изображения in that order. Engine block shows `<cc> см³` / `<hp> л.с. · <fuel>`. Click a comp card → comp modal replaces content; "← Назад к модели" returns. ESC closes either view.
</verification>

<success_criteria>
- Reviewers can drill from a model card → comp grid → per-comp detail with all six schema groups visible (no manual JSON inspection).
- The engine block on the model panel renders real values, not blanks.
- The pricing coverage gate either passes (parser fix) or has a justified, documented threshold reflecting drom's actual data shape.
- All changes committed; no scratch files; existing run dir's models.json/report.json untouched.
</success_criteria>

<output>
After completion, create `.planning/quick/260503-j62-improve-drom-html-viewer-clickable-compl/260503-j62-SUMMARY.md` with:
- Which hypothesis (H1 or H3) won, and the evidence (file paths to fixtures grepped, jq ratios computed).
- The exact selector added (H1) OR the exact threshold delta (H3).
- New pricing coverage % after the change.
- Three commit SHAs in order.
</output>
